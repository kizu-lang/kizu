test "slice syntax" {
    let bytes = "lexer token";
    let first = bytes[0];
    let lexer = bytes[0..5];
    let token = bytes[6..];
    let prefix = bytes[..5];

    std::testing::expect_equal<i64>(108, cast<i64>(first));
    std::testing::expect_equal<[]u8>("lexer", lexer);
    std::testing::expect_equal<[]u8>("token", token);
    std::testing::expect_equal<[]u8>("lexer", prefix);
}
