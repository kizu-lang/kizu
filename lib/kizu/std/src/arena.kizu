// Arena storage is provided by the Go implementation for now. The module has no
// Kizu source of its own: `std::arena::Arena<T>` and `std::arena::Handle<T>` are
// types the compiler knows, and this file is what makes the module exist so a
// program can import it.
