extern "c" fn raw_byte(p: ptr<const u8>) -> u8
extern "c" fn maybe_byte() -> ?ptr<const u8>

fn read_byte(p: ptr<const u8>) -> u8 {
    // SAFETY: p が 1 バイト以上読める有効な pointer であることは呼び出し側が保証する
    return unsafe raw_byte(p);
}

fn write_byte(p: ptr<u8>, value: u8) -> void {
    // SAFETY: p が 1 バイト以上書ける有効な pointer であることは呼び出し側が保証する
    unsafe ptr_write(p, value);
}

fn cast_to_mut(p: ptr<const u8>) -> ptr<u8> {
    // SAFETY: 返した pointer に書き込んでよいかは呼び出し側が判断する
    return unsafe cast<ptr<u8>>(p);
}

// check
// features: unsafe raw-pointer

// llvm
// emit:
// ; Kizu LLVM IR
// declare void @kizu_print_string(ptr, i64)
// declare void @kizu_print_int(i64)
// declare void @kizu_print_bool(i1)
// declare void @kizu_main_error_message(ptr, i64)
// 
// declare void @kizu_runtime_init_args(i32, ptr)
// 
// declare i8 @raw_byte(ptr)
// 
// define i8 @read_byte(ptr %kizu.p) {
// entry:
//   %kizu.1 = call i8 @raw_byte(ptr %kizu.p)
//   ret i8 %kizu.1
// }
// 
// define void @write_byte(ptr %kizu.p, i8 %kizu.value) {
// entry:
//   store i8 %kizu.value, ptr %kizu.p
//   ret void
// }
// 
// define ptr @cast_to_mut(ptr %kizu.p) {
// entry:
//   ret ptr %kizu.p
// }
// opt:
// ; Kizu LLVM IR
// declare void @kizu_print_string(ptr, i64)
// declare void @kizu_print_int(i64)
// declare void @kizu_print_bool(i1)
// declare void @kizu_main_error_message(ptr, i64)
// 
// declare void @kizu_runtime_init_args(i32, ptr)
// 
// declare i8 @raw_byte(ptr)
// 
// define i8 @read_byte(ptr %kizu.p) {
// entry:
//   %kizu.1 = call i8 @raw_byte(ptr %kizu.p)
//   ret i8 %kizu.1
// }
// 
// define void @write_byte(ptr %kizu.p, i8 %kizu.value) {
// entry:
//   store i8 %kizu.value, ptr %kizu.p
//   ret void
// }
// 
// define ptr @cast_to_mut(ptr %kizu.p) {
// entry:
//   ret ptr %kizu.p
// }
