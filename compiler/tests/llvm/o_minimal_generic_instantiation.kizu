struct Name { value: []u8 }
fn pass<T>(value: T) -> T {
    return move value;
}
fn main() {
    let name = Name { value: "alice" };
    let other = pass<Name>(name);
    print(other.value);
}


// llvm
// emit:
// check failed: move error: `name` is handed off here; write `move name` at 7:28
// opt:
// check failed: move error: `name` is handed off here; write `move name` at 7:28
