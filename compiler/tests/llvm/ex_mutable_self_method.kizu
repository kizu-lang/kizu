struct Counter {
    count: i64,
}

// A `&var self` method receives the caller's storage, so the write lands in
// the caller's binding rather than in a copy.
fn (self: &var Counter) bump() -> void {
    self.count = self.count + 1;
    return;
}

fn (self: &var Counter) bump_twice() -> void {
    self.bump();
    self.bump();
    return;
}

fn (self: Counter) value() -> i64 {
    return self.count;
}

fn main() {
    var hits = Counter { count: 0 };
    hits.bump_twice();
    hits.bump();
    print(hits.value());
}

// run
// features: method receiver mutable-borrow struct
// output:
// 3

// llvm
// emit:
// ; Kizu LLVM IR
// %kizu.struct.Counter = type { i64 }
// 
// declare void @kizu_print_string(ptr, i64)
// declare void @kizu_print_int(i64)
// declare void @kizu_print_bool(i1)
// declare void @kizu_main_error_message(ptr, i64)
// 
// declare void @kizu_runtime_init_args(i32, ptr)
// 
// define internal void @Counter_bump(ptr %kizu.self) {
// entry:
//   %kizu.1.ptr = getelementptr %kizu.struct.Counter, ptr %kizu.self, i32 0, i32 0
//   %kizu.1 = load i64, ptr %kizu.1.ptr
//   %kizu.3 = add i64 %kizu.1, 1
//   %kizu.4.ptr = getelementptr %kizu.struct.Counter, ptr %kizu.self, i32 0, i32 0
//   store i64 %kizu.3, ptr %kizu.4.ptr
//   ret void
// }
// 
// define internal void @Counter_bump_twice(ptr %kizu.self) {
// entry:
//   call void @Counter_bump(ptr %kizu.self)
//   call void @Counter_bump(ptr %kizu.self)
//   ret void
// }
// 
// define internal i64 @Counter_value(ptr byval(%kizu.struct.Counter) %kizu.self.addr) {
// entry:
//   %kizu.self = load %kizu.struct.Counter, ptr %kizu.self.addr
//   %kizu.1 = extractvalue %kizu.struct.Counter %kizu.self, 0
//   ret i64 %kizu.1
// }
// 
// define i32 @main(i32 %kizu.argc, ptr %kizu.argv) {
// entry:
//   %kizu.3 = alloca %kizu.struct.Counter
//   %kizu.arg.0.1 = alloca %kizu.struct.Counter
//   call void @kizu_runtime_init_args(i32 %kizu.argc, ptr %kizu.argv)
//   %kizu.2 = insertvalue %kizu.struct.Counter zeroinitializer, i64 0, 0
//   store %kizu.struct.Counter %kizu.2, ptr %kizu.3
//   call void @Counter_bump_twice(ptr %kizu.3)
//   call void @Counter_bump(ptr %kizu.3)
//   %kizu.6 = load %kizu.struct.Counter, ptr %kizu.3
//   store %kizu.struct.Counter %kizu.6, ptr %kizu.arg.0.1
//   %kizu.7 = call i64 @Counter_value(ptr byval(%kizu.struct.Counter) %kizu.arg.0.1)
//   call void @kizu_print_int(i64 %kizu.7)
//   ret i32 0
// }
// opt:
// ; Kizu LLVM IR
// %kizu.struct.Counter = type { i64 }
// 
// declare void @kizu_print_string(ptr, i64)
// declare void @kizu_print_int(i64)
// declare void @kizu_print_bool(i1)
// declare void @kizu_main_error_message(ptr, i64)
// 
// declare void @kizu_runtime_init_args(i32, ptr)
// 
// define internal void @Counter_bump(ptr %kizu.self) {
// entry:
//   %kizu.1.ptr = getelementptr %kizu.struct.Counter, ptr %kizu.self, i32 0, i32 0
//   %kizu.1 = load i64, ptr %kizu.1.ptr
//   %kizu.3 = add i64 %kizu.1, 1
//   %kizu.4.ptr = getelementptr %kizu.struct.Counter, ptr %kizu.self, i32 0, i32 0
//   store i64 %kizu.3, ptr %kizu.4.ptr
//   ret void
// }
// 
// define internal void @Counter_bump_twice(ptr %kizu.self) {
// entry:
//   call void @Counter_bump(ptr %kizu.self)
//   call void @Counter_bump(ptr %kizu.self)
//   ret void
// }
// 
// define internal i64 @Counter_value(ptr byval(%kizu.struct.Counter) %kizu.self.addr) {
// entry:
//   %kizu.self = load %kizu.struct.Counter, ptr %kizu.self.addr
//   %kizu.1 = extractvalue %kizu.struct.Counter %kizu.self, 0
//   ret i64 %kizu.1
// }
// 
// define i32 @main(i32 %kizu.argc, ptr %kizu.argv) {
// entry:
//   %kizu.3 = alloca %kizu.struct.Counter
//   %kizu.arg.0.1 = alloca %kizu.struct.Counter
//   call void @kizu_runtime_init_args(i32 %kizu.argc, ptr %kizu.argv)
//   %kizu.2 = insertvalue %kizu.struct.Counter zeroinitializer, i64 0, 0
//   store %kizu.struct.Counter %kizu.2, ptr %kizu.3
//   call void @Counter_bump_twice(ptr %kizu.3)
//   call void @Counter_bump(ptr %kizu.3)
//   %kizu.6 = load %kizu.struct.Counter, ptr %kizu.3
//   store %kizu.struct.Counter %kizu.6, ptr %kizu.arg.0.1
//   %kizu.7 = call i64 @Counter_value(ptr byval(%kizu.struct.Counter) %kizu.arg.0.1)
//   call void @kizu_print_int(i64 %kizu.7)
//   ret i32 0
// }
