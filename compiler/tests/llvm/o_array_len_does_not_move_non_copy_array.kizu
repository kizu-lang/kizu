import std;
struct Name { value: []u8 }
fn main(allocator: Allocator, values: std::array::Array<Name>) {
    let count = values.len();
    print(count);
    values.deinit(allocator);
}


// llvm
// emit:
// llvm error: emitter bug: function `main` reads `%kizu.array` but nothing defines it
// opt:
// llvm error: emitter bug: function `main` reads `%kizu.array` but nothing defines it
