enum Step {
    Advance,
    Stop,
}

fn main() {
    var current = 0;
    let step = Step::Advance;
    while current < 2 {
        match step {
            Advance => current = current + 1;,
            Stop => current = 2;,
        }
    }
    print(current);
}

// llvm
// emit:
// ; Kizu LLVM IR
// declare void @kizu_print_string(ptr, i64)
// declare void @kizu_print_int(i64)
// declare void @kizu_print_bool(i1)
// declare void @kizu_main_error_message(ptr, i64)
// 
// declare void @kizu_runtime_init_args(i32, ptr)
// 
// attributes #0 = { "probe-stack"="inline-asm" "stack-probe-size"="4096" }
// 
// define i32 @main(i32 %kizu.argc, ptr %kizu.argv) #0 {
// entry:
//   call void @kizu_runtime_init_args(i32 %kizu.argc, ptr %kizu.argv)
//   br label %while.header.1
// while.header.1:
//   %kizu.3 = phi i64 [ 0, %entry ], [ %kizu.11, %match.end.4 ]
//   %kizu.5 = icmp slt i64 %kizu.3, 2
//   br i1 %kizu.5, label %while.body.2, label %while.end.3
// while.body.2:
//   br label %match.check.5
// while.end.3:
//   call void @kizu_print_int(i64 %kizu.3)
//   ret i32 0
// match.end.4:
//   %kizu.11 = phi i64 [ %kizu.8, %match.arm.6 ], [ 2, %match.arm.8 ]
//   br label %while.header.1
// match.check.5:
//   %kizu.6 = icmp eq i64 0, 0
//   br i1 %kizu.6, label %match.arm.6, label %match.check.7
// match.arm.6:
//   %kizu.8 = add i64 %kizu.3, 1
//   br label %match.end.4
// match.check.7:
//   %kizu.9 = icmp eq i64 0, 1
//   br i1 %kizu.9, label %match.arm.8, label %match.unreachable.9
// match.arm.8:
//   br label %match.end.4
// match.unreachable.9:
//   unreachable
// }
// opt:
// ; Kizu LLVM IR
// declare void @kizu_print_string(ptr, i64)
// declare void @kizu_print_int(i64)
// declare void @kizu_print_bool(i1)
// declare void @kizu_main_error_message(ptr, i64)
// 
// declare void @kizu_runtime_init_args(i32, ptr)
// 
// attributes #0 = { "probe-stack"="inline-asm" "stack-probe-size"="4096" }
// 
// define i32 @main(i32 %kizu.argc, ptr %kizu.argv) #0 {
// entry:
//   call void @kizu_runtime_init_args(i32 %kizu.argc, ptr %kizu.argv)
//   br label %while.header.1
// while.header.1:
//   %kizu.3 = phi i64 [ 0, %entry ], [ %kizu.11, %match.end.4 ]
//   %kizu.5 = icmp slt i64 %kizu.3, 2
//   br i1 %kizu.5, label %while.body.2, label %while.end.3
// while.body.2:
//   br label %match.check.5
// while.end.3:
//   call void @kizu_print_int(i64 %kizu.3)
//   ret i32 0
// match.end.4:
//   %kizu.11 = phi i64 [ %kizu.8, %match.arm.6 ], [ 2, %match.arm.8 ]
//   br label %while.header.1
// match.check.5:
//   %kizu.6 = icmp eq i64 0, 0
//   br i1 %kizu.6, label %match.arm.6, label %match.check.7
// match.arm.6:
//   %kizu.8 = add i64 %kizu.3, 1
//   br label %match.end.4
// match.check.7:
//   %kizu.9 = icmp eq i64 0, 1
//   br i1 %kizu.9, label %match.arm.8, label %match.unreachable.9
// match.arm.8:
//   br label %match.end.4
// match.unreachable.9:
//   unreachable
// }
