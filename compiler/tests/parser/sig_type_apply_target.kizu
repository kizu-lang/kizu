fn main() {
    let namespace = f::call<T>(x);
    let instance = f.call<T>(x);
    let integer = 1<T>(x);
}

// parse
// ast:
// fn main() { let namespace = f::call<T>(x); let instance = ((f.call < T) > x); let integer = ((1 < T) > x); }
