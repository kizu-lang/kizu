import app::lexer;
pub struct Token {
    pub kind: TokenKind,
    start: i64,
}
pub enum TokenKind {
    Ident,
}
pub fn lex(source: []u8) -> void {}

// parse
// ast:
// import app::lexer
// pub struct Token { pub kind: TokenKind, start: i64 }
// pub enum TokenKind { Ident }
// pub fn lex(source: []u8) -> void {  }
