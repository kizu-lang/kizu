fn lookup(table: std::map::Map<[]u8, i64>) -> i64 {
    return table.get("main");
}
fn main() {
    let table = std::map::new<[]u8, i64>(allocator);
}

// parse
// ast:
// fn lookup(table: std::map::Map<[]u8, i64>) -> i64 { return table.get("main"); }
// fn main() { let table = std::map::new<[]u8, i64>(allocator); }
