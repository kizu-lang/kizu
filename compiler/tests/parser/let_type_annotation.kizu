fn main() {
    var found: ?i64 = null;
    let byte: u8 = 13;
    let values: std::array::Array<i64> = std::array::new<i64>(allocator);
}

// parse
// ast:
// fn main() { var found: ?i64 = null; let byte: u8 = 13; let values: std::array::Array<i64> = std::array::new<i64>(allocator); }
