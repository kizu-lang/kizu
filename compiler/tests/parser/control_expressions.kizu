enum Color { Red, Green }
fn main() {
    let color = Color::Green;
    let value = if true { 1 } else { 2 };
    let name = match color { Red => "red", Green => "green", };
    print(value);
    print(name);
}

// parse
// ast:
// enum Color { Red, Green }
// fn main() { let color = Color::Green; let value = if true { 1; } else { 2; }; let name = match color { Red => "red", Green => "green" }; print(value); print(name); }
