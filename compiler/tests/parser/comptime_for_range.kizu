fn main() {
    var total = 0;
    comptime for 0..3 |i| {
        comptime for std::meta::public_fields<Pair>() |f| {
            total = total + i;
        }
    }
    print(total);
}

// parse
// ast:
// fn main() { var total = 0; comptime for 0..3 |i| { comptime for std::meta::public_fields<Pair>() |f| { total = (total + i); } } print(total); }
