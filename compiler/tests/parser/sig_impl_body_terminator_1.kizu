impl Show for User {

// parse
// diagnostics:
// 1:15-1:19 `impl Show for User` takes no body; write the methods as `fn (self: User) name(...)` and end this with `;`
// 1:20-1:21 expected declaration (fn, test, import, struct, enum, union, contract, impl, extern, pub, or unsafe), got `{`
