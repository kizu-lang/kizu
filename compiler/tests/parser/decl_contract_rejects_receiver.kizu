contract C { fn (self: C) read(); }

// parse
// diagnostics:
// 1:32-1:33 a contract method takes no receiver; it says what a method looks like, not what it is on
// 1:33-1:34 expected declaration (fn, test, import, struct, enum, union, contract, impl, extern, pub, or unsafe), got `;`
// 1:35-1:36 expected declaration (fn, test, import, struct, enum, union, contract, impl, extern, pub, or unsafe), got `}`
