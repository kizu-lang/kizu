fn main() {
    let color = Color::Red;
    match color { Red => print("red"), Green => print("green") }
    let label = match color { Red => "red", Green => { "green" } };
    print(label);
}

// parse
// ast:
// fn main() { let color = Color::Red; match color { Red => print("red"), Green => print("green") } let label = match color { Red => "red", Green => { "green"; } }; print(label); }
