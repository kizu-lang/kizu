fn main() {
    let semicolon = a orelse return;
    let comma = f(b orelse return, 1);
    let paren = f(c orelse return);
    let bracket = items[d orelse return];
    let value = e orelse return 1;
    g orelse return
}

// parse
// ast:
// fn main() { let semicolon = (a orelse return); let comma = f((b orelse return), 1); let paren = f((c orelse return)); let bracket = items[(d orelse return)]; let value = (e orelse return 1); (g orelse return); }
