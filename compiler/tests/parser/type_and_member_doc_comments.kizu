/// User record.
pub struct User {
    /// Display name.
    pub name: []u8,
}

/// User state.
enum State {
    /// Active user.
    Active,
}

/// User event.
union Event {
    /// Rename event.
    Rename([]u8),
}

// parse
// ast:
// pub struct User { pub name: []u8 }
// enum State { Active }
// union Event { Rename([]u8) }
