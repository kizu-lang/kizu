fn main() {
    // SAFETY: the caller checked the bound
    unsafe ptr_write(dst, unsafe ptr_read(src));
    unsafe ptr_write(dst, 0);
}

// parse
// ast:
// fn main() { unsafe ptr_write(dst, unsafe ptr_read(src)); unsafe ptr_write(dst, 0); }
