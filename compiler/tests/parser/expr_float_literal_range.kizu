fn main() {
    print(cast<i64>(1e300));
    return;
}

// parse-fails
// features: float diagnostics
// error: float literal `1e300` is outside the range this compiler converts exactly

// parse
// diagnostics:
// 2:21-2:26 float literal `1e300` is outside the range this compiler converts exactly (up to 19 significant digits and an exponent within 10^±22)
