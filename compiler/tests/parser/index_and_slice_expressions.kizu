fn main() -> !void {
    let byte = bytes[0];
    let part = bytes[0..5];
    let tail = bytes[2..];
    let head = bytes[..3];
    return;
}

// parse
// ast:
// fn main() -> !void { let byte = bytes[0]; let part = bytes[0..5]; let tail = bytes[2..]; let head = bytes[..3]; return; }
