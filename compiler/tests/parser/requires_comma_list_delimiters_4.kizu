fn main() { let user = User { name: "alice"; age: 1 }; }

// parse
// diagnostics:
// 1:37-1:42 expected `,` after struct literal field
// 1:51-1:52 label `age` must be attached to while or for
// 1:54-1:55 expected declaration (fn, test, import, struct, enum, union, contract, impl, extern, pub, or unsafe), got `;`
// 1:56-1:57 expected declaration (fn, test, import, struct, enum, union, contract, impl, extern, pub, or unsafe), got `}`
