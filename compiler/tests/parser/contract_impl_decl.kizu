contract Writer {
    fn write() -> !i64;
}
fn (self: &File) write() -> !i64 {
    return 1;
}
impl Writer for File;

// parse
// ast:
// contract Writer { fn write() -> !i64; }
// fn (self: &File) write() -> !i64 { return 1; }
// impl Writer for File;
