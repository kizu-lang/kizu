struct User {
    name: []u8,
}
fn main() {
    let users = std::arena::new<User>(allocator);
    let alice = users.add(allocator, User { name: "alice" });
    print(users.get(alice).name);
}

// parse
// ast:
// struct User { name: []u8 }
// fn main() { let users = std::arena::new<User>(allocator); let alice = users.add(allocator, User { name: "alice" }); print(users.get(alice).name); }
