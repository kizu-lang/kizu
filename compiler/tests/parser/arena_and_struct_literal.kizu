struct UserArena {}
struct UserArena2 {}

struct User {
    name: []u8,
}
fn main() {
    let users = std::arena::new<User, UserArena>(allocator);
    let alice = users.add(allocator, User { name: "alice" });
    print(users.get(alice).name);
}

// parse
// ast:
// struct UserArena {  }
// struct UserArena2 {  }
// struct User { name: []u8 }
// fn main() { let users = std::arena::new<User, UserArena>(allocator); let alice = users.add(allocator, User { name: "alice" }); print(users.get(alice).name); }
