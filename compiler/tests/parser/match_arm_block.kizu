enum Color { Red, Green }
fn main() {
    var count = 0;
    match Color::Red {
        Red => {
            print("red");
            count = count + 1;
        },
        Green => {},
    }
}

// parse
// ast:
// enum Color { Red, Green }
// fn main() { var count = 0; match Color::Red { Red => { print("red"); count = (count + 1); }, Green => {  } } }
