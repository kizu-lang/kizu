import std;
union Shape { Dot, Bar(i64) }
fn read_shape(s: &Shape) -> i64 {
    return match s {
        Dot => 0,
        Bar(n) => n,
    };
}
fn main() {
    let allocator = std::mem::page_allocator();
    var xs = std::array::new<Shape>(allocator);
    if xs.at_mut(0) |m| {
        print(read_shape(m));
    }
    xs.deinit();
}

// ir
// lower:
// fn read_shape(%s: &Shape) -> i64 {
// entry:
//   %1: Shape = union.load %s: &Shape
//   %2: i64 = union.tag %1: Shape
//   jump match.check.2
// match.end.1:
//   %7: i64 = phi [match.arm.3, %4: i64], [match.arm.5, %6: i64]
//   return %7: i64
// match.check.2:
//   %3: bool = binary.== %2: i64, 0: i64
//   branch %3: bool, match.arm.3, match.check.4
// match.arm.3:
//   %4: i64 = const 0
//   jump match.end.1
// match.check.4:
//   %5: bool = binary.== %2: i64, 1: i64
//   branch %5: bool, match.arm.5, match.unreachable.6
// match.arm.5:
//   %6: i64 = union.payload %1: Shape, Bar
//   jump match.end.1
// match.unreachable.6:
//   unreachable
// }
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::array::Array<Shape> = array.new %1: Allocator, Shape
//   %3: i64 = const 0
//   %4: ?&var Shape = call.std::array::Array.at_mut.Shape %2: std::array::Array<Shape>, %3: i64
//   %5: &var Shape = opt.value %4: ?&var Shape
//   %6: bool = opt.has %4: ?&var Shape
//   branch %6: bool, if.then.1, if.else.2
// if.then.1:
//   %7: Shape = ref.load %5: &var Shape
//   %8: i64 = call.read_shape %7: Shape
//   call.print %8: i64
//   jump if.end.3
// if.else.2:
//   jump if.end.3
// if.end.3:
//   call.std::array::Array.deinit.Shape %2: std::array::Array<Shape>
//   return void: void
// }
// opt:
// fn read_shape(%s: &Shape) -> i64 {
// entry:
//   %1: Shape = union.load %s: &Shape
//   %2: i64 = union.tag %1: Shape
//   jump match.check.2
// match.end.1:
//   %7: i64 = phi [match.arm.3, %4: i64], [match.arm.5, %6: i64]
//   return %7: i64
// match.check.2:
//   %3: bool = binary.== %2: i64, 0: i64
//   branch %3: bool, match.arm.3, match.check.4
// match.arm.3:
//   %4: i64 = const 0
//   jump match.end.1
// match.check.4:
//   %5: bool = binary.== %2: i64, 1: i64
//   branch %5: bool, match.arm.5, match.unreachable.6
// match.arm.5:
//   %6: i64 = union.payload %1: Shape, Bar
//   jump match.end.1
// match.unreachable.6:
//   unreachable
// }
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::array::Array<Shape> = array.new %1: Allocator, Shape
//   %3: i64 = const 0
//   %4: ?&var Shape = call.std::array::Array.at_mut.Shape %2: std::array::Array<Shape>, %3: i64
//   %5: &var Shape = opt.value %4: ?&var Shape
//   %6: bool = opt.has %4: ?&var Shape
//   branch %6: bool, if.then.1, if.else.2
// if.then.1:
//   %7: Shape = ref.load %5: &var Shape
//   %8: i64 = call.read_shape %7: Shape
//   call.print %8: i64
//   jump if.end.3
// if.else.2:
//   jump if.end.3
// if.end.3:
//   call.std::array::Array.deinit.Shape %2: std::array::Array<Shape>
//   return void: void
// }
