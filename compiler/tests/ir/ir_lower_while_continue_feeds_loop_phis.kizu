fn main() {
    var index = 0;
    var in_string = false;
    while index < 3 {
        if in_string {
            in_string = false;
            index = index + 1;
            continue;
        }
        in_string = true;
        index = index + 1;
    }
    print(in_string);
}

// ir
// lower:
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   %2: bool = const false
//   jump while.header.1
// while.header.1:
//   %3: bool = phi [entry, %2: bool], [if.end.6, %10: bool], [if.then.4, %7: bool]
//   %4: i64 = phi [entry, %1: i64], [if.end.6, %12: i64], [if.then.4, %9: i64]
//   %5: i64 = const 3
//   %6: bool = binary.< %4: i64, %5: i64
//   branch %6: bool, while.body.2, while.end.3
// while.body.2:
//   branch %3: bool, if.then.4, if.else.5
// while.end.3:
//   call.print %3: bool
//   return void: void
// if.then.4:
//   %7: bool = const false
//   %8: i64 = const 1
//   %9: i64 = binary.+ %4: i64, %8: i64
//   jump while.header.1
// if.else.5:
//   jump if.end.6
// if.end.6:
//   %10: bool = const true
//   %11: i64 = const 1
//   %12: i64 = binary.+ %4: i64, %11: i64
//   jump while.header.1
// }
// opt:
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   %2: bool = const false
//   jump while.header.1
// while.header.1:
//   %3: bool = phi [entry, %2: bool], [if.end.6, %10: bool], [if.then.4, %7: bool]
//   %4: i64 = phi [entry, %1: i64], [if.end.6, %12: i64], [if.then.4, %9: i64]
//   %5: i64 = const 3
//   %6: bool = binary.< %4: i64, %5: i64
//   branch %6: bool, while.body.2, while.end.3
// while.body.2:
//   branch %3: bool, if.then.4, if.else.5
// while.end.3:
//   call.print %3: bool
//   return void: void
// if.then.4:
//   %7: bool = const false
//   %8: i64 = const 1
//   %9: i64 = binary.+ %4: i64, %8: i64
//   jump while.header.1
// if.else.5:
//   jump if.end.6
// if.end.6:
//   %10: bool = const true
//   %11: i64 = const 1
//   %12: i64 = binary.+ %4: i64, %11: i64
//   jump while.header.1
// }
