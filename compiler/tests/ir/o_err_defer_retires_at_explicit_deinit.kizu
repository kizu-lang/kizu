import std;
error BuildError { Boom }
struct User { name: []u8 }
fn build() -> !std::arena::Arena<User> {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    errdefer users.deinit();
    users.deinit();
    return BuildError::Boom;
}


// ir
// lower:
// fn build() -> !std::arena::Arena<User> {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator, User
//   arena.deinit %2: std::arena::Arena<User>
//   %4: BuildError = const 37
//   %5: !std::arena::Arena<User> = error.error %4: BuildError
//   return %5: !std::arena::Arena<User>
// }
// opt:
// fn build() -> !std::arena::Arena<User> {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator, User
//   arena.deinit %2: std::arena::Arena<User>
//   %4: BuildError = const 37
//   %5: !std::arena::Arena<User> = error.error %4: BuildError
//   return %5: !std::arena::Arena<User>
// }
