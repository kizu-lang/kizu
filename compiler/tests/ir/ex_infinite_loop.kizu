fn main() -> void {
    var i = 0;
    while true {
        print(i);
        i = i + 1;
        if i == 2 {
            break;
        }
    }
}

// run
// features: while break
// output:
// 0
// 1

// ir
// lower:
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   jump while.header.1
// while.header.1:
//   %2: i64 = phi [entry, %1: i64], [if.end.6, %6: i64]
//   %3: bool = const true
//   branch %3: bool, while.body.2, while.end.3
// while.body.2:
//   call.std::fmt::print.i64 %2: i64
//   %5: i64 = const 1
//   %6: i64 = binary.+ %2: i64, %5: i64
//   %7: i64 = const 2
//   %8: bool = binary.== %6: i64, %7: i64
//   branch %8: bool, if.then.4, if.else.5
// while.end.3:
//   %9: i64 = phi [while.header.1, %2: i64], [if.then.4, %6: i64]
//   return void: void
// if.then.4:
//   jump while.end.3
// if.else.5:
//   jump if.end.6
// if.end.6:
//   jump while.header.1
// }
// opt:
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   jump while.header.1
// while.header.1:
//   %2: i64 = phi [entry, %1: i64], [if.end.6, %6: i64]
//   %3: bool = const true
//   branch %3: bool, while.body.2, while.end.3
// while.body.2:
//   call.std::fmt::print.i64 %2: i64
//   %5: i64 = const 1
//   %6: i64 = binary.+ %2: i64, %5: i64
//   %7: i64 = const 2
//   %8: bool = binary.== %6: i64, %7: i64
//   branch %8: bool, if.then.4, if.else.5
// while.end.3:
//   return void: void
// if.then.4:
//   jump while.end.3
// if.else.5:
//   jump if.end.6
// if.end.6:
//   jump while.header.1
// }
