import std;
error BuildError { Boom }
struct User { name: []u8 }
fn make() -> !std::arena::Arena<User> {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    errdefer users.deinit(allocator);
    return BuildError::Boom;
}
fn main() {}

// ir
// lower:
// fn make() -> !std::arena::Arena<User> {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator
//   %3: BuildError = const 69
//   %4: !std::arena::Arena<User> = error.error %3: BuildError
//   arena.deinit %2: std::arena::Arena<User>, %1: Allocator
//   return %4: !std::arena::Arena<User>
// }
// fn main() -> void {
// entry:
//   return void: void
// }
// opt:
// fn make() -> !std::arena::Arena<User> {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator
//   %3: BuildError = const 69
//   %4: !std::arena::Arena<User> = error.error %3: BuildError
//   arena.deinit %2: std::arena::Arena<User>, %1: Allocator
//   return %4: !std::arena::Arena<User>
// }
// fn main() -> void {
// entry:
//   return void: void
// }
