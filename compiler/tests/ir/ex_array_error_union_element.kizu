import std::array;
import std::mem;

error ComputeError {
    Nope,
}

fn ok(value: i64) -> !i64 {
    return value;
}

fn bad() -> !i64 {
    return ComputeError::Nope;
}

fn count(results: &array::Array<!i64>) -> i64 {
    return results.len();
}

fn main() -> !void {
    var results = array::new<!i64>(mem::page_allocator());
    defer results.deinit();
    try results.append(ok(7));
    try results.append(bad());
    print(count(&results));
    return;
}

// run
// features: std-array error-union generics
// output:
// 2


// ir
// lower:
// fn ok(%value: i64) -> !i64 {
// entry:
//   %1: !i64 = error.ok %value: i64
//   return %1: !i64
// }
// fn bad() -> !i64 {
// entry:
//   %1: ComputeError = const 37
//   %2: !i64 = error.error %1: ComputeError
//   return %2: !i64
// }
// fn count(%results: std::array::Array<!i64>) -> i64 {
// entry:
//   %1: i64 = call.std::array::Array.len._21i64 %results: std::array::Array<!i64>
//   return %1: i64
// }
// fn main() -> !void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::array::Array<!i64> = array.new %1: Allocator, !i64
//   %3: i64 = const 7
//   %4: !i64 = call.ok %3: i64
//   %5: !void = call.std::array::Array.append._21i64 %2: std::array::Array<!i64>, %4: !i64
//   error.try %5: !void, cleanup array.deinit %2: std::array::Array<!i64>
//   %7: !i64 = call.bad
//   %8: !void = call.std::array::Array.append._21i64 %2: std::array::Array<!i64>, %7: !i64
//   error.try %8: !void, cleanup array.deinit %2: std::array::Array<!i64>
//   %10: i64 = call.count %2: std::array::Array<!i64>
//   call.print %10: i64
//   array.deinit %2: std::array::Array<!i64>
//   %13: !void = error.ok
//   return %13: !void
// }
// opt:
// fn ok(%value: i64) -> !i64 {
// entry:
//   %1: !i64 = error.ok %value: i64
//   return %1: !i64
// }
// fn bad() -> !i64 {
// entry:
//   %1: ComputeError = const 37
//   %2: !i64 = error.error %1: ComputeError
//   return %2: !i64
// }
// fn count(%results: std::array::Array<!i64>) -> i64 {
// entry:
//   %1: i64 = call.std::array::Array.len._21i64 %results: std::array::Array<!i64>
//   return %1: i64
// }
// fn main() -> !void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::array::Array<!i64> = array.new %1: Allocator, !i64
//   %3: i64 = const 7
//   %4: !i64 = call.ok %3: i64
//   %5: !void = call.std::array::Array.append._21i64 %2: std::array::Array<!i64>, %4: !i64
//   error.try %5: !void, cleanup array.deinit %2: std::array::Array<!i64>
//   %7: !i64 = call.bad
//   %8: !void = call.std::array::Array.append._21i64 %2: std::array::Array<!i64>, %7: !i64
//   error.try %8: !void, cleanup array.deinit %2: std::array::Array<!i64>
//   %10: i64 = call.count %2: std::array::Array<!i64>
//   call.print %10: i64
//   array.deinit %2: std::array::Array<!i64>
//   %13: !void = error.ok
//   return %13: !void
// }
