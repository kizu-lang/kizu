import std::array;
import std::mem;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var values = array::new<i64>(allocator);
    defer values.deinit(allocator);

    values.get_or_panic(0);
    return;
}

// run-fails
// features: std-array bounds-check trap
// error: index out of bounds

// ir
// lower:
// fn main() -> !void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::array::Array<i64> = array.new %1: Allocator
//   %3: i64 = const 0
//   %4: i64 = call.std::array::Array.get_or_panic.i64 %2: std::array::Array<i64>, %3: i64
//   array.deinit %2: std::array::Array<i64>, %1: Allocator
//   %6: !void = error.ok
//   return %6: !void
// }
// opt:
// fn main() -> !void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::array::Array<i64> = array.new %1: Allocator
//   %3: i64 = const 0
//   %4: i64 = call.std::array::Array.get_or_panic.i64 %2: std::array::Array<i64>, %3: i64
//   array.deinit %2: std::array::Array<i64>, %1: Allocator
//   %6: !void = error.ok
//   return %6: !void
// }
