fn main() {
    var i = 0;
    while i < 3 {
        print(i);
        i = i + 1;
    }
}

// run
// features: while assignment
// output:
// 0
// 1
// 2

// ir
// lower:
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   jump while.header.1
// while.header.1:
//   %2: i64 = phi [entry, %1: i64], [while.body.2, %7: i64]
//   %3: i64 = const 3
//   %4: bool = binary.< %2: i64, %3: i64
//   branch %4: bool, while.body.2, while.end.3
// while.body.2:
//   call.std::fmt::print.i64 %2: i64
//   %6: i64 = const 1
//   %7: i64 = binary.+ %2: i64, %6: i64
//   jump while.header.1
// while.end.3:
//   return void: void
// }
// opt:
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   jump while.header.1
// while.header.1:
//   %2: i64 = phi [entry, %1: i64], [while.body.2, %7: i64]
//   %3: i64 = const 3
//   %4: bool = binary.< %2: i64, %3: i64
//   branch %4: bool, while.body.2, while.end.3
// while.body.2:
//   %in1.1: i64 = cast %2: i64, i64
//   call.std::fmt::write_i64 %in1.1: i64
//   %6: i64 = const 1
//   %7: i64 = binary.+ %2: i64, %6: i64
//   jump while.header.1
// while.end.3:
//   return void: void
// }
// opt wasm:
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   jump while.header.1
// while.header.1:
//   jump while.body.2
// while.body.2:
//   %rot1.b.2: i64 = phi [while.header.1, %1: i64], [while.body.2, %7: i64]
//   %in1.1: i64 = cast %rot1.b.2: i64, i64
//   call.std::fmt::write_i64 %in1.1: i64
//   %6: i64 = const 1
//   %7: i64 = binary.+ %rot1.b.2: i64, %6: i64
//   %rot1.l0.3: i64 = const 3
//   %rot1.l0.4: bool = binary.< %7: i64, %rot1.l0.3: i64
//   branch %rot1.l0.4: bool, while.body.2, while.end.3
// while.end.3:
//   return void: void
// }
