import std;
struct User {
    name: []u8,
}
fn main() {
    let allocator = std::mem::page_allocator();
    let left = std::arena::new<User>(allocator);
    let right = std::arena::new<User>(allocator);
    print(left);
    print(right);
    left.deinit(allocator);
    right.deinit(allocator);
}


// ir
// lower:
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator
//   %3: std::arena::Arena<User> = arena.new %1: Allocator
//   call.print %2: std::arena::Arena<User>
//   call.print %3: std::arena::Arena<User>
//   call.std::arena::Arena.deinit.User %2: std::arena::Arena<User>, %1: Allocator
//   call.std::arena::Arena.deinit.User %3: std::arena::Arena<User>, %1: Allocator
//   return void: void
// }
// opt:
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator
//   %3: std::arena::Arena<User> = arena.new %1: Allocator
//   call.print %2: std::arena::Arena<User>
//   call.print %3: std::arena::Arena<User>
//   call.std::arena::Arena.deinit.User %2: std::arena::Arena<User>, %1: Allocator
//   call.std::arena::Arena.deinit.User %3: std::arena::Arena<User>, %1: Allocator
//   return void: void
// }
