import std;
struct User {
    name: []u8,
}
fn main() {
    let allocator = std::mem::page_allocator();
    let left = std::arena::new<User>(allocator);
    let right = std::arena::new<User>(allocator);
    print(left);
    print(right);
    left.deinit();
    right.deinit();
}


// ir
// lower:
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator, User
//   %3: std::arena::Arena<User> = arena.new %1: Allocator, User
//   call.print %2: std::arena::Arena<User>
//   call.print %3: std::arena::Arena<User>
//   arena.deinit %2: std::arena::Arena<User>
//   arena.deinit %3: std::arena::Arena<User>
//   return void: void
// }
// opt:
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator, User
//   %3: std::arena::Arena<User> = arena.new %1: Allocator, User
//   call.print %2: std::arena::Arena<User>
//   call.print %3: std::arena::Arena<User>
//   arena.deinit %2: std::arena::Arena<User>
//   arena.deinit %3: std::arena::Arena<User>
//   return void: void
// }
