import std;
struct User { name: []u8 }
struct Parsed {
    users: std::arena::Arena<User>,
    ids: std::array::Array<i64>,
}
fn (self: Parsed) deinit() -> void {
    self.users.deinit();
    self.ids.deinit();
}
fn check(values: std::array::Array<Parsed>) -> !void {
    defer values.deinit();
    if values.pop() |item| {
        item.deinit();
    }
    return;
}
fn main() {}


// ir
// lower:
// fn Parsed.deinit(%self: Parsed) -> void {
// entry:
//   %1: std::arena::Arena<User> = field.users %self: Parsed
//   arena.deinit %1: std::arena::Arena<User>
//   %3: std::array::Array<i64> = field.ids %self: Parsed
//   call.std::array::Array.deinit.i64 %3: std::array::Array<i64>
//   return void: void
// }
// fn check(%values: std::array::Array<Parsed>) -> !void {
// entry:
//   %1: ?Parsed = call.std::array::Array.pop.Parsed %values: std::array::Array<Parsed>
//   %2: Parsed = opt.value %1: ?Parsed
//   %3: bool = opt.has %1: ?Parsed
//   branch %3: bool, if.then.1, if.else.2
// if.then.1:
//   call.Parsed.deinit %2: Parsed
//   jump if.end.3
// if.else.2:
//   jump if.end.3
// if.end.3:
//   call.std::array::Array.deinit.Parsed %values: std::array::Array<Parsed>
//   %6: !void = error.ok
//   return %6: !void
// }
// fn main() -> void {
// entry:
//   return void: void
// }
// opt:
// fn Parsed.deinit(%self: Parsed) -> void {
// entry:
//   %1: std::arena::Arena<User> = field.users %self: Parsed
//   arena.deinit %1: std::arena::Arena<User>
//   %3: std::array::Array<i64> = field.ids %self: Parsed
//   call.std::array::Array.deinit.i64 %3: std::array::Array<i64>
//   return void: void
// }
// fn check(%values: std::array::Array<Parsed>) -> !void {
// entry:
//   %1: ?Parsed = call.std::array::Array.pop.Parsed %values: std::array::Array<Parsed>
//   %2: Parsed = opt.value %1: ?Parsed
//   %3: bool = opt.has %1: ?Parsed
//   branch %3: bool, if.then.1, if.else.2
// if.then.1:
//   call.Parsed.deinit %2: Parsed
//   jump if.end.3
// if.else.2:
//   jump if.end.3
// if.end.3:
//   call.std::array::Array.deinit.Parsed %values: std::array::Array<Parsed>
//   %6: !void = error.ok
//   return %6: !void
// }
// fn main() -> void {
// entry:
//   return void: void
// }
