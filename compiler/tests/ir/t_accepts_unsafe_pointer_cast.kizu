extern "c" fn raw() -> ptr<const u8>
fn main() {
    // SAFETY: test source
    let p = unsafe raw();
    // SAFETY: test source
    let writable = unsafe cast<ptr<u8>>(p);
    // SAFETY: test source
    unsafe ptr_write(writable, 1);}

// ir
// lower:
// fn main() -> void {
// entry:
//   %1: ptr<const u8> = extern "c" call.raw
//   %2: ptr<u8> = cast %1: ptr<const u8>, ptr<u8>
//   %3: u8 = const 1
//   ref.store %2: ptr<u8>, %3: u8
//   return void: void
// }
// opt:
// fn main() -> void {
// entry:
//   %1: ptr<const u8> = extern "c" call.raw
//   %2: ptr<u8> = cast %1: ptr<const u8>, ptr<u8>
//   %3: u8 = const 1
//   ref.store %2: ptr<u8>, %3: u8
//   return void: void
// }
