import std;

fn main() -> !void {
    let io = std::io::blocking();
    let allocator = std::mem::page_allocator();
    let text = try std::fs::read_file(
        io, allocator, "examples/fixtures/config.txt", std::mem::Limit::Unlimited);
    defer text.deinit(allocator);
    let view = text.as_bytes();
    print(view);
    return;
}

// run
// features: io fs error-union
// output:
// kizu fs fixture
//

// ir
// lower:
// fn main() -> !void {
// entry:
//   %1: Io = call.std::io::blocking
//   %2: Allocator = call.std::mem::page_allocator
//   %3: []u8 = const "examples/fixtures/config.txt"
//   %4: std::mem::Limit = union.new Unlimited
//   %5: std::fs::Error!std::string::String = call.std::fs::read_file %1: Io, %2: Allocator, %3: []u8, %4: std::mem::Limit
//   %6: std::string::String = error.try %5: std::fs::Error!std::string::String
//   %7: []u8 = call.std::string::String.as_bytes %6: std::string::String
//   call.print %7: []u8
//   call.std::string::String.deinit %6: std::string::String, %2: Allocator
//   %10: !void = error.ok
//   return %10: !void
// }
// opt:
// fn main() -> !void {
// entry:
//   %1: Io = call.std::io::blocking
//   %2: Allocator = call.std::mem::page_allocator
//   %3: []u8 = const "examples/fixtures/config.txt"
//   %4: std::mem::Limit = union.new Unlimited
//   %5: std::fs::Error!std::string::String = call.std::fs::read_file %1: Io, %2: Allocator, %3: []u8, %4: std::mem::Limit
//   %6: std::string::String = error.try %5: std::fs::Error!std::string::String
//   %7: []u8 = call.std::string::String.as_bytes %6: std::string::String
//   call.print %7: []u8
//   call.std::string::String.deinit %6: std::string::String, %2: Allocator
//   %10: !void = error.ok
//   return %10: !void
// }
