/// test invariant
unsafe struct Slot { index: usize }
fn take(s: &Slot) -> usize {
    return s.index;
}
fn main() {}

// ir
// lower:
// fn take(%s: Slot) -> usize {
// entry:
//   %1: usize = field.index %s: Slot
//   return %1: usize
// }
// fn main() -> void {
// entry:
//   return void: void
// }
// opt:
// fn take(%s: Slot) -> usize {
// entry:
//   %1: usize = field.index %s: Slot
//   return %1: usize
// }
// fn main() -> void {
// entry:
//   return void: void
// }
