struct Counter { count: i64 }
fn (self: &var Counter) bump() -> void { self.count = self.count + 1; }
struct Inner { counter: Counter }
struct Outer { inner: Inner }
fn (self: &var Outer) go() -> void {
    self.inner.counter.bump();
}
fn main() -> void {
    var o = Outer { inner: Inner { counter: Counter { count: 0 } } };
    o.inner.counter.bump();
    o.go();
}

// ir
// lower:
// fn Counter.bump(%self: &var Counter) -> void {
// entry:
//   %1: i64 = field.ref.count %self: &var Counter
//   %2: i64 = const 1
//   %3: i64 = binary.+ %1: i64, %2: i64
//   field.ref.set.count %self: &var Counter, %3: i64
//   return void: void
// }
// fn Outer.go(%self: &var Outer) -> void {
// entry:
//   %1: &var Inner = field.addr.inner %self: &var Outer
//   %2: &var Counter = field.addr.counter %1: &var Inner
//   call.Counter.bump %2: &var Counter
//   return void: void
// }
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   %2: Counter = struct.new {count: %1: i64}
//   %3: Inner = struct.new {counter: %2: Counter}
//   %4: Outer = struct.new {inner: %3: Inner}
//   %5: &var Outer = local.slot %4: Outer
//   %6: &var Inner = field.addr.inner %5: &var Outer
//   %7: &var Counter = field.addr.counter %6: &var Inner
//   call.Counter.bump %7: &var Counter
//   call.Outer.go %5: &var Outer
//   return void: void
// }
// opt:
// fn Counter.bump(%self: &var Counter) -> void {
// entry:
//   %1: i64 = field.ref.count %self: &var Counter
//   %2: i64 = const 1
//   %3: i64 = binary.+ %1: i64, %2: i64
//   field.ref.set.count %self: &var Counter, %3: i64
//   return void: void
// }
// fn Outer.go(%self: &var Outer) -> void {
// entry:
//   %1: &var Inner = field.addr.inner %self: &var Outer
//   %2: &var Counter = field.addr.counter %1: &var Inner
//   call.Counter.bump %2: &var Counter
//   return void: void
// }
// fn main() -> void {
// entry:
//   %1: i64 = const 0
//   %2: Counter = struct.new {count: %1: i64}
//   %3: Inner = struct.new {counter: %2: Counter}
//   %4: Outer = struct.new {inner: %3: Inner}
//   %5: &var Outer = local.slot %4: Outer
//   %6: &var Inner = field.addr.inner %5: &var Outer
//   %7: &var Counter = field.addr.counter %6: &var Inner
//   call.Counter.bump %7: &var Counter
//   call.Outer.go %5: &var Outer
//   return void: void
// }
