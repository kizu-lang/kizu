import std::map;
import std::mem;

fn define(allocator: Allocator, symbols: &var map::Map<[]u8, i64>) -> !void {
    try symbols.insert(allocator, "main", 1);
    return;
}

fn main() -> !void {
    let allocator = mem::page_allocator();
    var symbols = map::new<[]u8, i64>(allocator);
    defer symbols.deinit(allocator);

    try define(allocator, symbols);
    print(symbols.get("main") orelse -1);

    return;
}

// run
// features: std-map mutable-borrow
// output:
// 1

// ir
// lower:
// fn define(%allocator: Allocator, %symbols: &var std::map::Map<[]u8, i64>) -> !void {
// entry:
//   %1: []u8 = const "main"
//   %2: i64 = const 1
//   %3: std::mem::Error!void = call.std::map::Map.insert._5b_5du8.i64 %symbols: &var std::map::Map<[]u8, i64>, %allocator: Allocator, %1: []u8, %2: i64
//   error.try %3: std::mem::Error!void
//   %5: !void = error.ok
//   return %5: !void
// }
// fn main() -> !void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::map::Map<[]u8, i64> = map.new %1: Allocator
//   %3: &var std::map::Map<[]u8, i64> = local.slot %2: std::map::Map<[]u8, i64>
//   %4: !void = call.define %1: Allocator, %3: &var std::map::Map<[]u8, i64>
//   %5: std::map::Map<[]u8, i64> = ref.load %3: &var std::map::Map<[]u8, i64>
//   error.try %4: !void, cleanup map.deinit %5: std::map::Map<[]u8, i64>, %1: Allocator
//   %7: []u8 = const "main"
//   %8: ?i64 = call.std::map::Map.get._5b_5du8.i64 %3: &var std::map::Map<[]u8, i64>, %7: []u8
//   %9: bool = opt.has %8: ?i64
//   branch %9: bool, orelse.some.1, orelse.else.2
// orelse.some.1:
//   %10: i64 = opt.value %8: ?i64
//   jump orelse.end.3
// orelse.else.2:
//   %11: i64 = const 1
//   %12: i64 = unary.- %11: i64
//   jump orelse.end.3
// orelse.end.3:
//   %13: i64 = phi [orelse.some.1, %10: i64], [orelse.else.2, %12: i64]
//   call.std::fmt::print.i64 %13: i64
//   %15: std::map::Map<[]u8, i64> = ref.load %3: &var std::map::Map<[]u8, i64>
//   map.deinit %15: std::map::Map<[]u8, i64>, %1: Allocator
//   %17: !void = error.ok
//   return %17: !void
// }
// opt:
// fn define(%allocator: Allocator, %symbols: &var std::map::Map<[]u8, i64>) -> !void {
// entry:
//   %1: []u8 = const "main"
//   %2: i64 = const 1
//   %3: std::mem::Error!void = call.std::map::Map.insert._5b_5du8.i64 %symbols: &var std::map::Map<[]u8, i64>, %allocator: Allocator, %1: []u8, %2: i64
//   error.try %3: std::mem::Error!void
//   %5: !void = error.ok
//   return %5: !void
// }
// fn main() -> !void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::map::Map<[]u8, i64> = map.new %1: Allocator
//   %3: &var std::map::Map<[]u8, i64> = local.slot %2: std::map::Map<[]u8, i64>
//   %4: !void = call.define %1: Allocator, %3: &var std::map::Map<[]u8, i64>
//   %5: std::map::Map<[]u8, i64> = ref.load %3: &var std::map::Map<[]u8, i64>
//   error.try %4: !void, cleanup map.deinit %5: std::map::Map<[]u8, i64>, %1: Allocator
//   %7: []u8 = const "main"
//   %8: ?i64 = call.std::map::Map.get._5b_5du8.i64 %3: &var std::map::Map<[]u8, i64>, %7: []u8
//   %9: bool = opt.has %8: ?i64
//   branch %9: bool, orelse.some.1, orelse.else.2
// orelse.some.1:
//   %10: i64 = opt.value %8: ?i64
//   jump orelse.end.3
// orelse.else.2:
//   %11: i64 = const 1
//   %12: i64 = unary.- %11: i64
//   jump orelse.end.3
// orelse.end.3:
//   %13: i64 = phi [orelse.some.1, %10: i64], [orelse.else.2, %12: i64]
//   call.std::fmt::print.i64 %13: i64
//   %15: std::map::Map<[]u8, i64> = ref.load %3: &var std::map::Map<[]u8, i64>
//   map.deinit %15: std::map::Map<[]u8, i64>, %1: Allocator
//   %17: !void = error.ok
//   return %17: !void
// }
