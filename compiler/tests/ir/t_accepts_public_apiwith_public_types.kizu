pub enum TokenKind {
    Ident,
}
pub struct Token {
    pub kind: TokenKind,
    start: i64,
}
pub fn take(token: Token) -> Token {
    return token;
}
fn main() {}

// ir
// lower:
// fn take(%token: Token) -> Token {
// entry:
//   return %token: Token
// }
// fn main() -> void {
// entry:
//   return void: void
// }
// opt:
// fn take(%token: Token) -> Token {
// entry:
//   return %token: Token
// }
// fn main() -> void {
// entry:
//   return void: void
// }
