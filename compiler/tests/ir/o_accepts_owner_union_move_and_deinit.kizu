import std;
union Node {
    Left(std::string::String),
    Right(std::array::Array<i64>),
}
fn (self: Node) deinit(allocator: Allocator) -> void {
    match self {
        Left(s) => s.deinit(allocator),
        Right(a) => a.deinit(allocator),
    }
}
fn consume(allocator: Allocator, s: std::string::String) -> void {
    let n = Node::Left(move s);
    n.deinit(allocator);
}


// ir
// lower:
// fn Node.deinit(%self: Node, %allocator: Allocator) -> void {
// entry:
//   %1: i64 = union.tag %self: Node
//   jump match.check.2
// match.end.1:
//   return void: void
// match.check.2:
//   %2: bool = binary.== %1: i64, 0: i64
//   branch %2: bool, match.arm.3, match.check.4
// match.arm.3:
//   %3: std::string::String = union.payload %self: Node, Left
//   call.std::string::String.deinit %3: std::string::String, %allocator: Allocator
//   jump match.end.1
// match.check.4:
//   %5: bool = binary.== %1: i64, 1: i64
//   branch %5: bool, match.arm.5, match.unreachable.6
// match.arm.5:
//   %6: std::array::Array<i64> = union.payload %self: Node, Right
//   call.std::array::Array.deinit.i64 %6: std::array::Array<i64>, %allocator: Allocator
//   jump match.end.1
// match.unreachable.6:
//   unreachable
// }
// fn consume(%allocator: Allocator, %s: std::string::String) -> void {
// entry:
//   %1: Node = union.new %s: std::string::String, Left
//   call.Node.deinit %1: Node, %allocator: Allocator
//   return void: void
// }
// opt:
// fn Node.deinit(%self: Node, %allocator: Allocator) -> void {
// entry:
//   %1: i64 = union.tag %self: Node
//   jump match.check.2
// match.end.1:
//   return void: void
// match.check.2:
//   %2: bool = binary.== %1: i64, 0: i64
//   branch %2: bool, match.arm.3, match.check.4
// match.arm.3:
//   %3: std::string::String = union.payload %self: Node, Left
//   call.std::string::String.deinit %3: std::string::String, %allocator: Allocator
//   jump match.end.1
// match.check.4:
//   %5: bool = binary.== %1: i64, 1: i64
//   branch %5: bool, match.arm.5, match.unreachable.6
// match.arm.5:
//   %6: std::array::Array<i64> = union.payload %self: Node, Right
//   call.std::array::Array.deinit.i64 %6: std::array::Array<i64>, %allocator: Allocator
//   jump match.end.1
// match.unreachable.6:
//   unreachable
// }
// fn consume(%allocator: Allocator, %s: std::string::String) -> void {
// entry:
//   %1: Node = union.new %s: std::string::String, Left
//   call.Node.deinit %1: Node, %allocator: Allocator
//   return void: void
// }
