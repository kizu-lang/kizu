import std::arena;
import std::mem;

struct User {
    name: []u8,
}

fn main() {
    let allocator = mem::page_allocator();
    let users = arena::new<User>(allocator);
    let alice = users.add(User { name: "alice" });
    let user = users.at(alice);
    print(user.name);
    users.deinit();
}

// run
// features: arena handle shared-borrow
// output:
// alice


// ir
// lower:
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator, User
//   %3: []u8 = const "alice"
//   %4: User = struct.new {name: %3: []u8}
//   %5: std::arena::Handle<User> = arena.add %2: std::arena::Arena<User>, %4: User
//   %6: User = arena.at %2: std::arena::Arena<User>, %5: std::arena::Handle<User>
//   %7: []u8 = field.name %6: User
//   call.print %7: []u8
//   arena.deinit %2: std::arena::Arena<User>
//   return void: void
// }
// opt:
// fn main() -> void {
// entry:
//   %1: Allocator = call.std::mem::page_allocator
//   %2: std::arena::Arena<User> = arena.new %1: Allocator, User
//   %3: []u8 = const "alice"
//   %4: User = struct.new {name: %3: []u8}
//   %5: std::arena::Handle<User> = arena.add %2: std::arena::Arena<User>, %4: User
//   %6: User = arena.at %2: std::arena::Arena<User>, %5: std::arena::Handle<User>
//   %7: []u8 = field.name %6: User
//   call.print %7: []u8
//   arena.deinit %2: std::arena::Arena<User>
//   return void: void
// }
