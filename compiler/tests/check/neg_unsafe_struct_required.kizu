struct Buf {
    data: ptr<u8>,
    len: usize,
}

fn size(b: &Buf) -> usize {
    return b.len;
}

// check-fails
// features: unsafe-struct raw-pointer
// error: must be declared `unsafe struct`

// check
// load:
// struct Buf { data: ptr<u8>, len: usize }
// fn size(b: &Buf) -> usize { return b.len; }
// check:
// - unsafe error: struct `Buf` holds a raw pointer in field `data`, so it must be declared `unsafe struct`
//   help: write `unsafe struct Buf` and document the invariant its fields carry
