struct Holder {
    element: type,
}

fn main() -> void {
    print(1);
}

// check-fails
// features: comptime type-value struct field
// error: struct field `Holder.element` cannot store type value


// check
// load:
// struct Holder { element: type }
// fn main() -> void { print(1); }
// check:
// - type error: struct field `Holder.element` cannot store type value
