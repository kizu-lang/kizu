fn take(x: u8) -> u8 { return x ;}
fn main() {
    print(take(256));
}

// check
// load:
// fn take(x: u8) -> u8 { return x; }
// fn main() { print(take(256)); }
// check:
// 3:5-3:22 type error: integer literal `256` does not fit u8
