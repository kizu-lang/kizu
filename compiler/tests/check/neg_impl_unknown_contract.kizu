struct File {
    name: []u8,
}

fn (self: &File) write() -> !i64 {
    return 1;
}

impl Nowhere for File;

fn main() -> void {
    print(1);
}

// check-fails
// features: contract impl
// error: unknown contract `Nowhere`

// check
// load:
// struct File { name: []u8 }
// fn (self: &File) File.write() -> !i64 { return 1; }
// impl Nowhere for File;
// fn main() -> void { print(1); }
// check:
// - type error: unknown contract `Nowhere`
