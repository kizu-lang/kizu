fn main() {
    let x = 1;
    let y = comptime x + 1;
    print(y);
}

// check
// load:
// fn main() { let x = 1; let y = comptime (x + 1); print(y); }
// check:
// - comptime error: runtime value cannot be used
