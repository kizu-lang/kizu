struct Node {
    tag: i64,
}

fn read(p: ptr<Node>) -> i64 {
    return unsafe p.tag;
}

// check-fails
// features: unsafe raw-pointer field-access
// error: `ptr<Node>` has no fields

// check
// load:
// struct Node { tag: i64 }
// fn read(p: ptr<Node>) -> i64 { return unsafe p.tag; }
// check:
// 6:5-6:25 type error: `ptr<Node>` has no fields
