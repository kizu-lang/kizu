import std;
struct User { name: []u8 }
struct Registry { users: std::arena::Arena<User> }
fn (self: Registry) deinit() -> void {
    self.users.deinit();
}
fn make() -> std::arena::Handle<User> {
    let allocator = std::mem::page_allocator();
    var registry = Registry { users: std::arena::new<User>(allocator) };
    return registry.users.add(User { name: "alice" });
}


// check
// load:
// struct User { name: []u8 }
// struct Registry { users: std::arena::Arena<User> }
// fn (self: Registry) Registry.deinit() -> void { self.users.deinit(); }
// fn make() -> std::arena::Handle<User> { let allocator = std::mem::page_allocator(); var registry = Registry { users: std::arena::new<User>(allocator) }; return registry.users.add(User { name: "alice" }); }
// check:
// - arena error: handle from `registry.users` cannot outlive its arena
