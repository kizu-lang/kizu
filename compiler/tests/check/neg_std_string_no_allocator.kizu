import std::string;

fn main() {
    let text = string::new();
    print(text.len());
}

// check-fails
// features: std-string allocator type-check
// error: expects allocator

// check
// load:
// fn main() { let text = std::string::new(); print(text.len()); }
// check:
// 4:5-4:30 type error: `std::string::new` expects allocator
