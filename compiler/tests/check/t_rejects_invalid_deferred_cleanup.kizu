fn main() {
    defer print("not cleanup");
}

// check
// load:
// fn main() { defer print("not cleanup"); }
// check:
// 2:5-2:32 type error: defer expects a call to a declared function or method, got builtin `print`
