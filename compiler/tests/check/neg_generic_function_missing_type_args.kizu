fn identity<T>(value: T) -> T {
    return value;
}

fn main() -> void {
    print(identity(1));
}

// check-fails
// features: generic type-check
// error: requires explicit static arguments

// check
// load:
// fn identity<T>(value: T) -> T { return value; }
// fn main() -> void { print(identity(1)); }
// check:
// 6:5-6:24 type error: `identity` requires explicit static arguments
