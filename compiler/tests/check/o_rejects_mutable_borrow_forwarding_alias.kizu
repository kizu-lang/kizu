struct User { name: []u8 }
fn use(left: &User, right: &var User) {
    print(left.name);
    print(right.name);
}
fn outer(user: &var User) {
    use(user, user);
}


// check
// load:
// struct User { name: []u8 }
// fn use(left: &User, right: &var User) { print(left.name); print(right.name); }
// fn outer(user: &var User) { use(user, user); }
// check:
// 7:5-7:21 borrow error: value `user` cannot be mutably borrowed while borrowed
