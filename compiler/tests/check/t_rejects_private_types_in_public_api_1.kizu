struct Secret {}
pub fn leak(secret: Secret) -> void {
    return;
}
fn main() {}

// check
// load:
// struct Secret {  }
// pub fn leak(secret: Secret) -> void { return; }
// fn main() {  }
// check:
// - type error: public function `leak` parameter exposes private type `Secret`
