fn main() -> !void {
    let x = 1 orelse 2;
    print(x);
    return;
}

// check-fails
// features: optional orelse diagnostics
// error: `orelse` expects an optional left operand, got i64

// check
// load:
// fn main() -> !void { let x = (1 orelse 2); print(x); return; }
// check:
// - type error: `orelse` expects an optional left operand, got i64
