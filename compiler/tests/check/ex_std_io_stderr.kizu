import std;

fn main() -> !void {
    let io = std::io::blocking();
    try std::io::write_stderr_line(io, "diagnostic");
    return;
}

// check
// features: std-io stderr explicit-io


// check
// load:
// fn main() -> !void { let io = std::io::blocking(); try std::io::write_stderr_line(io, "diagnostic"); return; }
// check:
// ok
