import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    users.deinit(allocator);
    defer users.deinit(allocator);
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); users.deinit(allocator); defer users.deinit(allocator); }
// check:
// 7:11-7:16 arena error: arena `users` was deinitialized
