import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    users.deinit();
    defer users.deinit();
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); users.deinit(); defer users.deinit(); }
// check:
// 7:11-7:16 arena error: arena `users` was deinitialized
