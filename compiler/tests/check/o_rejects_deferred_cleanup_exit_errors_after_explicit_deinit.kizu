import std;
struct UserArena {}

struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User, UserArena>(allocator);
    users.deinit(allocator);
    defer users.deinit(allocator);
}


// check
// load:
// struct UserArena {  }
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User, UserArena>(allocator); users.deinit(allocator); defer users.deinit(allocator); }
// check:
// 9:11-9:16 arena error: arena `users` was deinitialized
