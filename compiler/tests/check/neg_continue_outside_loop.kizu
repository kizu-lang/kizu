fn main() -> void {
    continue;
}

// check-fails
// features: continue
// error: `continue` used outside loop


// check
// load:
// fn main() -> void { continue; }
// check:
// - type error: `continue` used outside loop
