fn main() -> void {
    continue;
}

// check-fails
// features: continue
// error: `continue` used outside loop

// check
// load:
// fn main() -> void { continue; }
// check:
// 2:5-2:14 type error: `continue` used outside loop
