import std;
struct Name { value: []u8 }
fn check(values: std::array::Array<Name>) -> !void {
    defer values.deinit();
    if values.pop() |value| {
        print(value.value);
    }
    return;
}
fn main() {}


// check
// load:
// struct Name { value: []u8 }
// fn check(values: std::array::Array<Name>) -> !void { defer values.deinit(); if values.pop() { print(value.value); } return; }
// fn main() {  }
// check:
// ok
