import std;
struct Name { value: []u8 }
fn check(allocator: Allocator, values: std::array::Array<Name>) -> !void {
    defer values.deinit(allocator);
    if values.pop() |value| {
        print(value.value);
    }
    return;
}
fn main() {}


// check
// load:
// struct Name { value: []u8 }
// fn check(allocator: Allocator, values: std::array::Array<Name>) -> !void { defer values.deinit(allocator); if values.pop() { print(value.value); } return; }
// fn main() {  }
// check:
// ok
