struct Node { tag: i64 }
fn write(p: ptr<const Node>) -> void {
    // SAFETY: test source
    unsafe p.*.tag = 1;
    return;}

// check
// load:
// struct Node { tag: i64 }
// fn write(p: ptr<const Node>) -> void { unsafe p.*.tag = 1; return; }
// check:
// - type error: cannot assign through const raw pointer `ptr<const Node>`
