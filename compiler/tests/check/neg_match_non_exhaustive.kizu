enum Color {
    Red,
    Green,
}

fn main() {
    let color = Color::Red;
    match color {
        Red => print("red"),
    }
}

// check-fails
// features: match enum
// error: match on `Color` is not exhaustive

// check
// load:
// enum Color { Red, Green }
// fn main() { let color = Color::Red; match color { Red => print("red") } }
// check:
// 8:5-10:6 type error: match on `Color` is not exhaustive: missing Green
