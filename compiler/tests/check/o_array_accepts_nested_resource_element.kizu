import std;
struct UserArena {}

struct User { name: []u8 }
struct Parsed {
    users: std::arena::Arena<User, UserArena>,
    ids: std::array::Array<i64>,
}
fn (self: Parsed) deinit(allocator: Allocator) -> void {
    self.users.deinit(allocator);
    self.ids.deinit(allocator);
}
fn check(allocator: Allocator, values: std::array::Array<Parsed>) -> !void {
    defer values.deinit(allocator);
    if values.pop() |item| {
        item.deinit(allocator);
    }
    return;
}
fn main() {}


// check
// load:
// struct UserArena {  }
// struct User { name: []u8 }
// struct Parsed { users: std::arena::Arena<User, UserArena>, ids: std::array::Array<i64> }
// fn (self: Parsed) Parsed.deinit(allocator: Allocator) -> void { self.users.deinit(allocator); self.ids.deinit(allocator); }
// fn check(allocator: Allocator, values: std::array::Array<Parsed>) -> !void { defer values.deinit(allocator); if values.pop() { item.deinit(allocator); } return; }
// fn main() {  }
// check:
// ok
