struct Name {
    value: []u8,
}

fn take(name: Name) {
    print(name.value);
}

fn main() {
    let name = Name { value: "alice" };
    take(move name);
    print(name.value);
}

// check-fails
// features: move
// error: moved value `name` was used

// check
// load:
// struct Name { value: []u8 }
// fn take(name: Name) { print(name.value); }
// fn main() { let name = Name { value: "alice" }; take(move name); print(name.value); }
// check:
// 12:11-12:15 move error: moved value `name` was used
