struct Register { value: i64 }
/// test contract
unsafe fn (self: Register) read() -> i64 {
    return self.value;
}
fn main() {
    let register = Register { value: 1 };
    // SAFETY: test source
    print(unsafe register.read());}

// check
// load:
// struct Register { value: i64 }
// unsafe fn (self: Register) Register.read() -> i64 { return self.value; }
// fn main() { let register = Register { value: 1 }; print(unsafe register.read()); }
// check:
// ok
