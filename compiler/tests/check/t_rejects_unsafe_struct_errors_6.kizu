/// test invariant
unsafe struct Buf { data: ptr<u8>, len: usize }
fn wrap(p: ptr<u8>) -> Buf {
    return Buf { data: p, len: 0 };
}

// check
// load:
// unsafe struct Buf { data: ptr<u8>, len: usize }
// fn wrap(p: ptr<u8>) -> Buf { return Buf { data: p, len: 0 }; }
// check:
// 4:12-4:15 [unsafe.missing_marker] unsafe error: construction of `unsafe struct Buf` requires `unsafe`
//   help: `unsafe` here covers establishing or changing the invariant of an `unsafe struct`, which is its construction and its field writes.
