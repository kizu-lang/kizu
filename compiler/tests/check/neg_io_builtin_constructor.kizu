fn main() -> void {
    let io = Io();
    print(io);
}

// check-fails
// features: io namespace
// error: use `std::io::blocking()`

// check
// load:
// fn main() -> void { let io = Io(); print(io); }
// check:
// 2:5-2:19 type error: use `std::io::blocking()`
