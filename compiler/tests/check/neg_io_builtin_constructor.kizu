fn main() -> void {
    let io = Io();
    print(io);
}

// check-fails
// features: io namespace
// error: use `std::io::blocking()`

// check
// load:
// fn main() -> void { let io = Io(); print(io); }
// check:
// - type error: use `std::io::blocking()`
