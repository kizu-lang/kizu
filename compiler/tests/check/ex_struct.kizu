struct User {
    name: []u8,
    age: i64,
}

fn main() {
    let user = User {
        name: "alice",
        age: 30,
    };

    print(user.name);
    print(user.age);
}

// run
// features: struct field
// output:
// alice
// 30

// check
// load:
// struct User { name: []u8, age: i64 }
// fn main() { let user = User { name: "alice", age: 30 }; print(user.name); print(user.age); }
// check:
// ok
