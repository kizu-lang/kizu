import std;
fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let values = std::array::new<i64>(allocator);
    defer values.deinit(allocator);
    try values.append(1);
    values.pop();
    values.len();
    return;
}


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); let values = std::array::new<i64>(allocator); defer values.deinit(allocator); try values.append(1); values.pop(); values.len(); return; }
// check:
// ok
