fn done(ok: bool) -> void {
    if ok {
        print("done");
        return;
    }

    print("not done");
}

fn main() {
    done(true);
}

// run
// features: return void
// output:
// done

// check
// load:
// fn done(ok: bool) -> void { if ok { print("done"); return; } print("not done"); }
// fn main() { done(true); }
// check:
// ok
