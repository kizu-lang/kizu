import std::mem;

fn main() -> void {
    print(mem::is_ascii_space(32));
    return;
}

// check-fails
// features: std-mem visibility
// error: function `mem::is_ascii_space` is private

// check
// load:
// module error: function `mem::is_ascii_space` is private
