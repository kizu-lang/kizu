struct User { name: []u8 }
fn use(left: &var User, right: &var User) {
    print(left.name);
    print(right.name);
}
fn main() {
    let user = User { name: "alice" };
    use(user, user);
}


// check
// load:
// struct User { name: []u8 }
// fn use(left: &var User, right: &var User) { print(left.name); print(right.name); }
// fn main() { let user = User { name: "alice" }; use(user, user); }
// check:
// - type error: &var argument `user` must be mutable
