fn copy(dst: ptr<u8>, src: ptr<const u8>) -> void {
    // SAFETY: test source
    unsafe ptr_write(dst, ptr_read(src));
}

// check
// load:
// fn copy(dst: ptr<u8>, src: ptr<const u8>) -> void { unsafe ptr_write(dst, ptr_read(src)); }
// check:
// ok
