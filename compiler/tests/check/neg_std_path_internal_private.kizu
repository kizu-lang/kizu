import std::path;

fn main() -> void {
    print(std::path::internal::bits::basename("src/main.kizu"));
    return;
}

// check-fails
// features: std-path visibility
// error: `std::path::internal::bits` is internal to `std::path`

// check
// load:
// module error: `std::path::internal::bits` is internal to `std::path` and is not reachable from ``
