fn main() -> void {
    let held = type<i64>;
    print(1);
}

// check-fails
// features: comptime type-value let
// error: type value cannot be stored in local `held`


// check
// load:
// fn main() -> void { let held = type<i64>; print(1); }
// check:
// - type error: type value cannot be stored in local `held`
