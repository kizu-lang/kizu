extern "c" fn source() -> u8
fn main() {
    print(source());
}

// check
// load:
// extern "c" fn source() -> u8
// fn main() { print(source()); }
// check:
// 3:11-3:17 [unsafe.missing_marker] unsafe error: call to `source` requires `unsafe`
//   help: `unsafe` here covers calls to `extern "<abi>" fn` declarations.
