import std;
import std::map;
union Holder { Value(std::meta::element<map::Map<i64, bool>>) }
fn main() {}

// check
// load:
// union Holder { Value(std::meta::element<std::map::Map<i64, bool>>) }
// fn main() {  }
// check:
// - type error: std::map::Map key type must be []u8
