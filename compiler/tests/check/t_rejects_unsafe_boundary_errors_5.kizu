struct Register { value: i64 }
/// test contract
unsafe fn (self: Register) read() -> i64 {
    return self.value;
}
fn main() {
    let register = Register { value: 1 };
    print(register.read());
}

// check
// load:
// struct Register { value: i64 }
// unsafe fn (self: Register) Register.read() -> i64 { return self.value; }
// fn main() { let register = Register { value: 1 }; print(register.read()); }
// check:
// 8:11-8:24 [unsafe.missing_marker] unsafe error: call to `Register.read` requires `unsafe`
//   help: `unsafe` here covers calls to functions or methods declared `unsafe fn`.
