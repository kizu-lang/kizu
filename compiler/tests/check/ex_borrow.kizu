fn show(s: &[]u8) {
    print(s);
}

fn main() {
    let name = "alice";
    show(name);
    print(name);
}

// run
// features: borrow ownership
// output:
// alice
// alice

// check
// load:
// fn show(s: &[]u8) { print(s); }
// fn main() { let name = "alice"; show(name); print(name); }
// check:
// ok
