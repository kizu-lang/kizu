struct User {
    age: i64,
}

fn main() -> void {
    let user = User { age: 30 };
    user.age = 31;
}

// check-fails
// features: struct field assignment
// error: cannot assign field of immutable binding `user`


// check
// load:
// struct User { age: i64 }
// fn main() -> void { let user = User { age: 30 }; user.age = 31; }
// check:
// - type error: cannot assign field of immutable binding `user`
