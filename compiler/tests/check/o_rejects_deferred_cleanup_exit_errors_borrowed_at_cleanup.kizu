import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    let borrowed = &users;
    defer users.deinit();
    while false { print(borrowed); }
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); let borrowed = (&users); defer users.deinit(); while false { print(borrowed); } }
// check:
// - arena error: `Arena.deinit` cannot run while arena is borrowed
