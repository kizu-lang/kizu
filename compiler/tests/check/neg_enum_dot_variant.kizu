enum Color {
    Red,
}

fn main() -> void {
    print(Color.Red);
}

// check-fails
// features: enum namespace
// error: enum tag `Color.Red` must use `::`


// check
// load:
// enum Color { Red }
// fn main() -> void { print(Color.Red); }
// check:
// - type error: enum tag `Color.Red` must use `::`
