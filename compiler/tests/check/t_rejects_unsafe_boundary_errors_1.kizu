extern "c" fn source() -> ptr<i64>
fn main() {
    let p = source();
    print(ptr_read(p));
}

// check
// load:
// extern "c" fn source() -> ptr<i64>
// fn main() { let p = source(); print(ptr_read(p)); }
// check:
// 3:13-3:19 [unsafe.missing_marker] unsafe error: call to `source` requires `unsafe`
//   help: `unsafe` here covers calls to `extern "<abi>" fn` declarations.
