struct Point {
    x: i32,
    y: i32,
}

extern "browser" fn draw(point: Point) -> void

fn main() -> void {}

// check
// load:
// struct Point { x: i32, y: i32 }
// extern "browser" fn draw(point: Point) -> void
// fn main() -> void {  }
// check:
// - type error: browser host function `draw` parameter 1 has unsupported type Point
