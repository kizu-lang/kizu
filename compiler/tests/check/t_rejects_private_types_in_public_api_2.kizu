struct Secret {}
pub struct Token {
    pub secret: Secret,
}
fn main() {}

// check
// load:
// struct Secret {  }
// pub struct Token { pub secret: Secret }
// fn main() {  }
// check:
// - type error: public field `Token.secret` exposes private type `Secret`
