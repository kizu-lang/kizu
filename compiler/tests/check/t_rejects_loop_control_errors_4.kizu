fn main() -> void {
    for true..3 |i| { print(i); }
}

// check
// load:
// fn main() -> void { for true..3 |i| { print(i); } }
// check:
// - type error: for range expects i64 bounds, got bool..i64
