fn sized<n: i64>() -> i64 { return n ;}
fn main() {
    let x = 8;
    print(sized<x>());
}

// check
// load:
// fn sized<n: i64>() -> i64 { return n; }
// fn main() { let x = 8; print(sized<x>()); }
// check:
// - type error: `sized` static argument `n` expects i64, got `x`
