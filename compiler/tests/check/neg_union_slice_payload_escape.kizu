union Token {
    Word([]u8),
    Number(i64),
}

fn token_text(token: Token) -> []u8 {
    return match token {
        Word(text) => text,
        Number(_) => "",
    };
}

// check-fails
// features: union match borrow
// error: borrowed value `text` cannot escape


// check
// load:
// union Token { Word([]u8), Number(i64) }
// fn token_text(token: Token) -> []u8 { return match token { Word(text) => text, Number(_) => "" }; }
// check:
// ok
