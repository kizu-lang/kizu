fn bad(a: bool, b: bool) -> bool {
    return a + b;
}

// check
// load:
// fn bad(a: bool, b: bool) -> bool { return (a + b); }
// check:
// 2:14-2:15 type error: operator `+` expects numeric operands
//   note: left operand has type bool
//   note: right operand has type bool
