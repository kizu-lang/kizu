import std;

fn main() -> void {
    let io = std::io::evented();
    print(io);
}

// check-fails
// features: io evented
// error: std::io::evented` is not implemented


// check
// load:
// fn main() -> void { let io = std::io::evented(); print(io); }
// check:
// - type error: `std::io::evented` is not implemented
