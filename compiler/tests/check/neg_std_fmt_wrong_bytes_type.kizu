import std::fmt;
import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    try fmt::append_bytes_literal(text, 1);
    return;
}

// check-fails
// features: std-fmt type-check
// error: expects []u8


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); try std::fmt::append_bytes_literal(text, 1); return; }
// check:
// - type error: `std::fmt::append_bytes_literal` bytes expects []u8, got i64
