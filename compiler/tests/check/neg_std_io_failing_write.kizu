import std;

fn main() -> !void {
    let io = std::testing::failing_io();
    try std::io::write_stdout(io, "hello");
    return;
}

// run-fails
// features: std-io error-union
// error: std::io::Error::IoFailing

// check
// load:
// fn main() -> !void { let io = std::testing::failing_io(); try std::io::write_stdout(io, "hello"); return; }
// check:
// ok
