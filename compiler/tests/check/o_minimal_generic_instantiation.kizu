struct Name { value: []u8 }
fn pass<T>(value: T) -> T {
    return move value;
}
fn main() {
    let name = Name { value: "alice" };
    let other = pass<Name>(name);
    print(other.value);
}


// check
// load:
// struct Name { value: []u8 }
// fn pass<T>(value: T) -> T { return move value; }
// fn main() { let name = Name { value: "alice" }; let other = pass<Name>(name); print(other.value); }
// check:
// ok
