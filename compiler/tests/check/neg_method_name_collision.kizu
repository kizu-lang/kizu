struct Left {
    value: i64,
}

fn (self: Left) len() -> i64 {
    return self.value;
}

fn (self: Left) len() -> i64 {
    return 0;
}

fn main() {
    print(1);
}

// check-fails
// features: method receiver diagnostics
// error: duplicate method

// check
// load:
// struct Left { value: i64 }
// fn (self: Left) Left.len() -> i64 { return self.value; }
// fn (self: Left) Left.len() -> i64 { return 0; }
// fn main() { print(1); }
// check:
// - type error: duplicate method `Left.len`
