fn take(x: u8) -> u8 { return x ;}
fn main() {
    let x = 1;
    print(take(x));
}

// check
// load:
// fn take(x: u8) -> u8 { return x; }
// fn main() { let x = 1; print(take(x)); }
// check:
// 4:5-4:20 type error: arg 1 of `take` expects u8, got i64
