pub enum TokenKind {
    Ident,
}
pub struct Token {
    pub kind: TokenKind,
    start: i64,
}
pub fn take(token: Token) -> Token {
    return token;
}
fn main() {}

// check
// load:
// pub enum TokenKind { Ident }
// pub struct Token { pub kind: TokenKind, start: i64 }
// pub fn take(token: Token) -> Token { return token; }
// fn main() {  }
// check:
// ok
