fn main() {
    var count = 0;
    unsafe count = 1;
    print(count);
}

// check
// load:
// fn main() { var count = 0; unsafe count = 1; print(count); }
// check:
// 3:5-3:11 [unsafe.unused_marker] unsafe error: `unsafe` covers no operation that needs it
//   help: remove `unsafe`
