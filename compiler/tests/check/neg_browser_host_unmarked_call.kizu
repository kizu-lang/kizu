extern "browser" fn notify(value: i32) -> void

fn main() -> void {
    notify(cast<i32>(1));
}

// check
// load:
// extern "browser" fn notify(value: i32) -> void
// fn main() -> void { notify(cast<i32>(1)); }
// check:
// 4:5-4:11 [unsafe.missing_marker] unsafe error: call to `notify` requires `unsafe`
//   help: `unsafe` here covers calls to `extern "<abi>" fn` declarations.
