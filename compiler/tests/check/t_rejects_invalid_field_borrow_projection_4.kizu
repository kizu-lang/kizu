struct Facts { value: i64 }
fn take(value: i64) -> void { print(value); }
fn main() -> void {
    var facts = Facts { value: 1 };
    take(&facts.value);
}

// check
// load:
// struct Facts { value: i64 }
// fn take(value: i64) -> void { print(value); }
// fn main() -> void { var facts = Facts { value: 1 }; take((&facts.value)); }
// check:
// 5:5-5:24 type error: borrow argument cannot be passed to owning parameter
