contract Writer {
    fn write(text: []u8) -> !i64;
}

struct File {
    name: []u8,
}

impl Writer for File {
    fn write(self: &File, text: []u8) -> !i64 {
        return 1;
    }
}

fn main() {
    print(1);
}

// parse-fails
// features: contract impl diagnostics
// error: takes no body


// check
// load:
// `impl Writer for File` takes no body; write the methods as `fn (self: File) name(...)` and end this with `;` at 9:17
