import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    try text.append_byte(allocator, "x");
    return;
}

// check-fails
// features: std-string type-check
// error: expects u8

// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); try text.append_byte(allocator, "x"); return; }
// check:
// - type error: `String.append_byte` expects u8, got []u8
