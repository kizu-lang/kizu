struct Secret {}
pub union Result {
    Ok(Secret),
}
fn main() {}

// check
// load:
// struct Secret {  }
// pub union Result { Ok(Secret) }
// fn main() {  }
// check:
// - type error: public union variant `Result::Ok` exposes private type `Secret`
