error FsError {
    NotFound,
    Denied,
}

fn read() -> FsError!i64 {
    return FsError::Denied;
}

fn main() -> FsError!void {
    print(try read());
    return;
}

// run-fails
// features: error-set error-union diagnostic
// error: runtime error: FsError::Denied

// check
// load:
// error FsError { NotFound, Denied }
// fn read() -> FsError!i64 { return FsError::Denied; }
// fn main() -> FsError!void { print(try read()); return; }
// check:
// ok
