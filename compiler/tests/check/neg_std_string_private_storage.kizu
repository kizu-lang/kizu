import std::mem;
import std::string;

fn main() {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    print(text.bytes.len());
}

// check-fails
// features: std-string visibility type-check
// error: is private

// check
// load:
// fn main() { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); print(text.bytes.len()); }
// check:
// - type error: field `std::string::String.bytes` is private
