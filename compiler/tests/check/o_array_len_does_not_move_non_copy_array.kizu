import std;
struct Name { value: []u8 }
fn main(allocator: Allocator, values: std::array::Array<Name>) {
    let count = values.len();
    print(count);
    values.deinit(allocator);
}


// check
// load:
// struct Name { value: []u8 }
// fn main(allocator: Allocator, values: std::array::Array<Name>) { let count = values.len(); print(count); values.deinit(allocator); }
// check:
// ok
