fn read(p: ptr<u8>) -> u8 {
    return ptr_read(p);
}

// check
// load:
// fn read(p: ptr<u8>) -> u8 { return ptr_read(p); }
// check:
// 2:12-2:20 [unsafe.missing_marker] unsafe error: `ptr_read` requires `unsafe`
//   help: `unsafe` here covers raw pointer reads with `ptr_read(p)`.
