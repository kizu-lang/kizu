import std::arena;
import std::mem;

struct User {
    name: []u8,
}

fn main() {
    let allocator = mem::page_allocator();
    let users = arena::new<User>(allocator);
    users.deinit();
    users.add(User { name: "alice" });
}

// check-fails
// features: arena deinit
// error: arena `users` was deinitialized


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); users.deinit(); users.add(User { name: "alice" }); }
// check:
// 12:5-12:10 arena error: arena `users` was deinitialized
