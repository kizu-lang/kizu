fn main() -> !void {
    let bytes = "abc"[0..4];
    print(bytes);
    return;
}

// run-fails
// features: slice-syntax bounds trap
// error: range out of bounds


// check
// load:
// fn main() -> !void { let bytes = "abc"[0..4]; print(bytes); return; }
// check:
// ok
