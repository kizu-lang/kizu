fn take(count: i64) {
    print(count);
}

fn main() {
    let count = 30;
    take(move count);
    print(count);
}

// check-fails
// features: move diagnostic
// error: `count` is copy data and hands nothing off


// check
// load:
// fn take(count: i64) { print(count); }
// fn main() { let count = 30; take(move count); print(count); }
// check:
// ok
