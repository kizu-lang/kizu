import std::mem;
import std::string;

fn main() {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    text.deinit(allocator);
    print(text.len());
}

// check-fails
// features: std-string deinit move
// error: moved value

// check
// load:
// fn main() { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); text.deinit(allocator); print(text.len()); }
// check:
// 8:11-8:15 move error: moved value `text` was used
