enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let color = Color::Green;
    print(color);
    print(color == Color::Green);
}

// run
// features: enum
// output:
// Color::Green
// true


// check
// load:
// enum Color { Red, Green, Blue }
// fn main() { let color = Color::Green; print(color); print((color == Color::Green)); }
// check:
// ok
