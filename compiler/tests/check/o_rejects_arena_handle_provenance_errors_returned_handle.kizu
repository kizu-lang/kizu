import std;
struct User { name: []u8 }
fn make() -> std::arena::Handle<User> {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    let alice = users.add(User { name: "alice" });
    return alice;
}


// check
// load:
// struct User { name: []u8 }
// fn make() -> std::arena::Handle<User> { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); let alice = users.add(User { name: "alice" }); return alice; }
// check:
// 7:12-7:17 arena error: handle `alice` cannot outlive its arena
