import std;
import std::array;
union Holder { Value(std::meta::element<array::Array<i64>>) }
fn make() -> Holder { return Holder::Value(1); }
fn main() {}

// check
// load:
// union Holder { Value(std::meta::element<std::array::Array<i64>>) }
// fn make() -> Holder { return Holder::Value(1); }
// fn main() {  }
// check:
// ok
