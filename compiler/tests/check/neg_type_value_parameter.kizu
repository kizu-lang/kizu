fn takes(t: type) -> i64 {
    return 1;
}

fn main() -> void {
    print(takes(1));
}

// check-fails
// features: comptime type-value type-check
// error: parameter `t` cannot have type

// check
// load:
// fn takes(t: type) -> i64 { return 1; }
// fn main() -> void { print(takes(1)); }
// check:
// - type error: parameter `t` cannot have type
