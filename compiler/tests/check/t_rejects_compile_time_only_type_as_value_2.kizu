fn take(x: ?Field) -> void { return; }
fn main() -> void { print(1); }

// check
// load:
// fn take(x: ?Field) -> void { return; }
// fn main() -> void { print(1); }
// check:
// 1:1-1:39 type error: ?Field parameter `x` belongs in `<...>`, not `(...)`
