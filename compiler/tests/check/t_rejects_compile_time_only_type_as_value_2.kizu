fn take(x: ?Field) -> void { return; }
fn main() -> void { print(1); }

// check
// load:
// fn take(x: ?Field) -> void { return; }
// fn main() -> void { print(1); }
// check:
// - type error: ?Field parameter `x` belongs in `<...>`, not `(...)`
