import std::arena;
import std::mem;

struct User {
    name: []u8,
}

fn main() {
    let allocator = mem::page_allocator();
    let users = arena::new<User>(allocator);
    let alice = users.add(User { name: "alice" });
    let again = alice;
    users.deinit();
    print(again);
}

// check-fails
// features: arena handle deinit
// error: handle `again` cannot be used after arena `users` deinit


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); let alice = users.add(User { name: "alice" }); let again = alice; users.deinit(); print(again); }
// check:
// ok
