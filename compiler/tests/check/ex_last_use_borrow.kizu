fn take(s: []u8) -> void {
    print(s);
}

fn main() -> void {
    let name = "alice";
    let ref = &name;
    print(ref.*);
    take(name);
}

// run
// features: borrow last-use ownership
// output:
// alice
// alice

// check
// load:
// fn take(s: []u8) -> void { print(s); }
// fn main() -> void { let name = "alice"; let ref = (&name); print(ref.*); take(name); }
// check:
// ok
