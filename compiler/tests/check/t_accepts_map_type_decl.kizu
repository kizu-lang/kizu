import std;
fn use_table(table: std::map::Map<[]u8, i64>) -> void {
    return;
}
fn main() {}

// check
// load:
// fn use_table(table: std::map::Map<[]u8, i64>) -> void { return; }
// fn main() {  }
// check:
// - move error: owned value `table` is never deinitialized
