import std;
fn use_table(table: std::map::Map<[]u8, i64>) -> void {
    return;
}
fn main() {}

// check
// load:
// fn use_table(table: std::map::Map<[]u8, i64>) -> void { return; }
// fn main() {  }
// check:
// 3:5-3:12 move error: owned value `table` is never deinitialized
