struct Name {
    value: []u8,
}

fn take(name: Name) {
    print(name.value);
}

fn main() {
    let name = Name { value: "alice" };
    take(move name);
    take(name);
}

// check-fails
// features: move
// error: moved value `name` was used


// check
// load:
// struct Name { value: []u8 }
// fn take(name: Name) { print(name.value); }
// fn main() { let name = Name { value: "alice" }; take(move name); take(name); }
// check:
// 12:10-12:14 move error: moved value `name` was used
