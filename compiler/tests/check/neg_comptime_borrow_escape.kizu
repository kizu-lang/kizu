fn bad(s: &[]u8) -> []u8 {
    let alias = comptime s;
    return alias;
}

// check-fails
// features: borrow comptime
// error: runtime value cannot be used

// check
// load:
// fn bad(s: &[]u8) -> []u8 { let alias = comptime s; return alias; }
// check:
// 2:5-2:28 comptime error: runtime value cannot be used
