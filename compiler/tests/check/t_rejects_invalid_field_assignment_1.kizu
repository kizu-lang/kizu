struct User { name: []u8 }
fn main() -> void {
    let user = User { name: "alice" };
    user.name = "bob";
}

// check
// load:
// struct User { name: []u8 }
// fn main() -> void { let user = User { name: "alice" }; user.name = "bob"; }
// check:
// 4:5-4:23 type error: cannot assign field of immutable binding `user`
