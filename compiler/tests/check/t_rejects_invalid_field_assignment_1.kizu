struct User { name: []u8 }
fn main() -> void {
    let user = User { name: "alice" };
    user.name = "bob";
}

// check
// load:
// struct User { name: []u8 }
// fn main() -> void { let user = User { name: "alice" }; user.name = "bob"; }
// check:
// - type error: cannot assign field of immutable binding `user`
