struct User {
    name: []u8,
    age: i64,
}

fn main() {
    let user = User { name: "alice", age: 30, age: 31 };
    print(user.name);
}

// check-fails
// features: struct field
// error: duplicate field `User.age`

// check
// load:
// struct User { name: []u8, age: i64 }
// fn main() { let user = User { name: "alice", age: 30, age: 31 }; print(user.name); }
// check:
// - type error: duplicate field `User.age`
