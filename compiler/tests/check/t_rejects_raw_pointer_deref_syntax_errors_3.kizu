fn read(p: ?ptr<u8>) -> u8 {
    // SAFETY: test source
    return unsafe p.*;}

// check
// load:
// fn read(p: ?ptr<u8>) -> u8 { return unsafe p.*; }
// check:
// - type error: nullable raw pointer `?ptr<u8>` cannot be dereferenced
