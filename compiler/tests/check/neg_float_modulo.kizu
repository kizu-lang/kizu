fn main() {
    print(cast<i64>(7.5 % 2.0));
    return;
}

// check-fails
// features: float diagnostics
// error: operator `%` expects integer operands

// check
// load:
// fn main() { print(cast<i64>((7.5 % 2.0))); return; }
// check:
// 2:25-2:26 type error: operator `%` expects integer operands
//   note: left operand has type f64
//   note: right operand has type f64
