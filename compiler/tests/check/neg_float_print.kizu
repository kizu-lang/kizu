fn main() {
    print(1.5);
    return;
}

// check-fails
// features: float diagnostics
// error: `print` does not accept f64

// check
// load:
// fn main() { print(1.5); return; }
// check:
// - type error: `print` does not accept f64
//   help: cast the value to an integer to print it: `cast<i64>(value)`
