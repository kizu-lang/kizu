struct Bad {
    value: &[]u8,
}
fn main() { print(1); }


// check
// load:
// struct Bad { value: &[]u8 }
// fn main() { print(1); }
// check:
// - type error: borrow field `Bad.value` cannot store borrow
