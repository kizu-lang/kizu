import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    let borrowed = &users;
    users.deinit(allocator);
    print(borrowed);
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); let borrowed = (&users); users.deinit(allocator); print(borrowed); }
// check:
// 8:5-8:10 comptime error: `std::fmt::print` does not support type `std::arena::Arena<User>`
//   note: in `std::fmt::print<std::arena::Arena<User>>`
