fn bad(a: i32, b: u32) -> i32 {
    return a + b;
}

// check
// load:
// fn bad(a: i32, b: u32) -> i32 { return (a + b); }
// check:
// 2:14-2:15 [type.operator_type_mismatch] type error: operator `+` operands must have same type
//   note: left operand has type i32
//   note: right operand has type u32
