/// value は増やしても呼び出し側の不変条件を壊さない値でなければならない。
unsafe fn raw_increment(value: i64) -> i64 {
    return value + 1;
}

fn main() {
    print(raw_increment(1));
}

// check-fails
// features: unsafe caller-obligation
// error: call to `raw_increment` requires `unsafe`

// check
// load:
// unsafe fn raw_increment(value: i64) -> i64 { return (value + 1); }
// fn main() { print(raw_increment(1)); }
// check:
// 7:11-7:24 [unsafe.missing_marker] unsafe error: call to `raw_increment` requires `unsafe`
//   help: `unsafe` here covers calls to functions or methods declared `unsafe fn`.
