fn bad(a: bool) -> bool {
    return a and 1;
}

// check
// load:
// fn bad(a: bool) -> bool { return (a and 1); }
// check:
// 2:14-2:17 type error: operator `and` expects bool operands
//   note: left operand has type bool
//   note: right operand has type i64
