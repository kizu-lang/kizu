import std;
struct UserArena {}

struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User, UserArena>(allocator);
    users.deinit(1);
}

// check
// load:
// struct UserArena {  }
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User, UserArena>(allocator); users.deinit(1); }
// check:
// - type error: `Arena.deinit` expects Allocator, got i64
