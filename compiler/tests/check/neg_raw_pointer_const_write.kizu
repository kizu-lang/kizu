fn write(p: ptr<const u8>) -> void {
    unsafe p.* = 1;
}

// check-fails
// features: unsafe raw-pointer deref assignment
// error: cannot assign through const raw pointer

// check
// load:
// fn write(p: ptr<const u8>) -> void { unsafe p.* = 1; }
// check:
// 2:5-2:20 type error: cannot assign through const raw pointer `ptr<const u8>`
