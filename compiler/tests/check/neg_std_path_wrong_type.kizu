import std::path;

fn main() {
    print(path::basename(1));
}

// check-fails
// features: std-path type-check
// error: expects []u8


// check
// load:
// fn main() { print(std::path::basename(1)); }
// check:
// - type error: `std::path::basename` path expects []u8, got i64
