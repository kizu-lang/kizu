fn take(x: i32) -> i32 { return x ;}
fn main() {
    let x = cast<i32>(1);
    print(take(x));
}

// check
// load:
// fn take(x: i32) -> i32 { return x; }
// fn main() { let x = cast<i32>(1); print(take(x)); }
// check:
// ok
