struct Facts { value: i64 }
fn bump(value: &var i64) -> void { print(value); }
fn main() -> void {
    let facts = Facts { value: 1 };
    bump(&var facts.value);
}

// check
// load:
// struct Facts { value: i64 }
// fn bump(value: &var i64) -> void { print(value); }
// fn main() -> void { let facts = Facts { value: 1 }; bump((&varfacts.value)); }
// check:
// 5:5-5:28 type error: &var argument `facts` must be mutable
