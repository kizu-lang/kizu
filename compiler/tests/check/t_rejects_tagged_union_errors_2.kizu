union Shape { Point, Circle(i64), }
fn main() {
    let shape = Shape::Point;
    match shape { Point => print("point");, }
}

// check
// load:
// union Shape { Point, Circle(i64) }
// fn main() { let shape = Shape::Point; match shape { Point => print("point"); } }
// check:
// - type error: match on `Shape` is not exhaustive: missing Circle
