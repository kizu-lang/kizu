struct User { name: []u8 }
fn update(user: &var User) -> void { print(user.name); }
fn main() {
    let user = User { name: "alice" };
    update(user);
}

// check
// load:
// struct User { name: []u8 }
// fn update(user: &var User) -> void { print(user.name); }
// fn main() { let user = User { name: "alice" }; update(user); }
// check:
// 5:5-5:18 type error: &var argument `user` must be mutable
