fn main() {
    let p = ptr_from_int<ptr<u32>>(0);
    volatile_write(p, 1);
}

// check
// load:
// fn main() { let p = ptr_from_int<ptr<u32>>(0); volatile_write(p, 1); }
// check:
// 2:13-2:25 [unsafe.missing_marker] unsafe error: `ptr_from_int` requires `unsafe`
//   help: `unsafe` here covers `ptr_from_int<ptr<...>>(value)` and `int_from_ptr<usize>(value)`.
