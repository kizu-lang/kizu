fn pick(left: []u8, right: []u8, first: bool) -> []u8 {
    if first {
        return left;
    }
    return right;
}
fn main() {}

// check
// load:
// fn pick(left: []u8, right: []u8, first: bool) -> []u8 { if first { return left; } return right; }
// fn main() {  }
// check:
// ok
