import std;
fn use_table(table: std::map::Map<f64, i64>) -> void {
    return;
}
fn main() {}


// check
// load:
// fn use_table(table: std::map::Map<f64, i64>) -> void { return; }
// fn main() {  }
// check:
// - type error: std::map::Map key type must be one of []u8, i8, i16, i32, i64, u8, u16, u32, u64, isize, usize
