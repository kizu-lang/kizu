union Shape { Circle(i64), }
fn main() {
    let shape = Shape::Circle("large");
    print(shape);
}

// check
// load:
// union Shape { Circle(i64) }
// fn main() { let shape = Shape::Circle("large"); print(shape); }
// check:
// 3:5-3:40 type error: union variant `Shape::Circle` expects i64, got []u8
