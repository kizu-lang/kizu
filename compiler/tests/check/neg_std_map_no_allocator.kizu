import std::map;

fn main() {
    let table = map::new<[]u8, i64>();
    print(table.len());
}

// check-fails
// features: std-map allocator type-check
// error: expects allocator

// check
// load:
// fn main() { let table = std::map::new<[]u8, i64>(); print(table.len()); }
// check:
// 4:5-4:39 type error: `std::map::new` expects allocator
