export "browser" fn complete(status: i32) -> !i32 {
    return status;
}

fn main() -> void {}

// check
// load:
// export "browser" fn complete(status: i32) -> !i32 { return status; }
// fn main() -> void {  }
// check:
// - type error: browser host function `complete` has unsupported return type !i32
