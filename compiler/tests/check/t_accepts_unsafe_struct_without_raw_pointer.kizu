/// test invariant
unsafe struct Slot { index: usize }
fn take(s: &Slot) -> usize {
    return s.index;
}
fn main() {}

// check
// load:
// unsafe struct Slot { index: usize }
// fn take(s: &Slot) -> usize { return s.index; }
// fn main() {  }
// check:
// ok
