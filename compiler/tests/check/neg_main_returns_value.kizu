fn main() -> !i64 {
    return 1;
}

// check-fails
// features: fn error-union type-check
// error: `main` returns `!i64`, expected `void` or `!void`


// check
// load:
// fn main() -> !i64 { return 1; }
// check:
// - type error: `main` returns `!i64`, expected `void`, `!void` or `!std::process::ExitStatus`
