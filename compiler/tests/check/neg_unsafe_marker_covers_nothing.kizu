fn main() {
    unsafe print(1);
}

// check-fails
// features: unsafe
// error: `unsafe` covers no operation that needs it


// check
// load:
// fn main() { unsafe print(1); }
// check:
// 2:5-2:11 [unsafe.unused_marker] unsafe error: `unsafe` covers no operation that needs it
//   help: remove `unsafe`
