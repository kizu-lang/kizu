import std::arena;

struct User {
    name: []u8,
}

fn main() {
    let users = arena::new<User>(1);
    print(users);
}

// check-fails
// features: arena allocator
// error: expects Allocator, got i64


// check
// load:
// struct User { name: []u8 }
// fn main() { let users = std::arena::new<User>(1); print(users); }
// check:
// - type error: `std::arena::new` allocator expects Allocator, got i64
