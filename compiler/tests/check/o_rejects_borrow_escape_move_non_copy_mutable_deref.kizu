struct User { name: []u8 }
fn bad(user: &var User) -> User {
    return user.*;
}


// check
// load:
// struct User { name: []u8 }
// fn bad(user: &var User) -> User { return user.*; }
// check:
// 3:5-3:19 borrow error: value `user` cannot be moved out of borrow
