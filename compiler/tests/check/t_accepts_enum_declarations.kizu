enum Color {
    Red,
    Green,
    Blue,
}
fn take(color: Color) -> Color { return color ;}
fn main() {
    let red = Color::Red;
    print(take(red));
}

// check
// load:
// enum Color { Red, Green, Blue }
// fn take(color: Color) -> Color { return color; }
// fn main() { let red = Color::Red; print(take(red)); }
// check:
// ok
