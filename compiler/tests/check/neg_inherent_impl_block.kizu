struct Counter {
    count: i64,
}

impl Counter {
    fn next(self: Counter) -> i64 {
        return self.count + 1;
    }
}

fn main() {
    print(1);
}

// parse-fails
// features: method receiver diagnostics
// error: a method on a type is `fn (self: Counter) name(...)`

// check
// load:
// expected `impl <contract> for Counter;`; a method on a type is `fn (self: Counter) name(...)` at 5:6
