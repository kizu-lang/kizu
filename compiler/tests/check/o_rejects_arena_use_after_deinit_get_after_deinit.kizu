import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    let alice = users.add(User { name: "alice" });
    users.deinit(allocator);
    print(users.at(alice).name);
			}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); let alice = users.add(User { name: "alice" }); users.deinit(allocator); print(users.at(alice).name); }
// check:
// 8:11-8:16 arena error: arena `users` was deinitialized
