fn read(p: ?ptr<u8>) -> u8 {
    return unsafe p.*;
}

// check-fails
// features: unsafe raw-pointer deref
// error: nullable raw pointer

// check
// load:
// fn read(p: ?ptr<u8>) -> u8 { return unsafe p.*; }
// check:
// 2:5-2:23 type error: nullable raw pointer `?ptr<u8>` cannot be dereferenced
