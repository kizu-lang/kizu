fn is_i64<T>(value: T) -> bool {
    comptime if T == i64 {
        return true;
    } else {
        return false;
    }
}
fn main() {
    print(is_i64<i64>(1));
}

// check
// load:
// fn is_i64<T>(value: T) -> bool { comptime if (T == i64) { return true; } else { return false; } }
// fn main() { print(is_i64<i64>(1)); }
// check:
// - comptime error: runtime value cannot be used
