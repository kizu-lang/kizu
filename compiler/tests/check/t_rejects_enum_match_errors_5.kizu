enum Color { Red, Green }
fn main() {
    let color = Color::Red;
    match color { _ => print("other");, Red => print("red");, }
}

// check
// load:
// enum Color { Red, Green }
// fn main() { let color = Color::Red; match color { _ => print("other");, Red => print("red"); } }
// check:
// 4:5-4:64 type error: wildcard match arm must be last
