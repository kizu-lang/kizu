enum Color {
    Red,
    Green,
}

fn main() -> void {
    let color = Color::Red;
    match color {
        Red => print("red"),
        Red => print("again"),
        Green => print("green"),
    }
}

// check-fails
// features: match enum
// error: duplicate match tag `Color::Red`


// check
// load:
// enum Color { Red, Green }
// fn main() -> void { let color = Color::Red; match color { Red => print("red"), Red => print("again"), Green => print("green") } }
// check:
// - type error: duplicate match tag `Color::Red`
