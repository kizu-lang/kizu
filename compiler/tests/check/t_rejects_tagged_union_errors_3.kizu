union Shape { Point }
fn main() {
    let shape = Shape::Point;
    match shape { Point(x) => print(x);, }
}

// check
// load:
// union Shape { Point }
// fn main() { let shape = Shape::Point; match shape { Point(x) => print(x); } }
// check:
// 4:5-4:43 type error: union variant `Shape::Point` has no payload
