union Shape { Point }
fn main() {
    let shape = Shape::Point;
    match shape { Point(x) => print(x);, }
}

// check
// load:
// union Shape { Point }
// fn main() { let shape = Shape::Point; match shape { Point(x) => print(x); } }
// check:
// - type error: union variant `Shape::Point` has no payload
