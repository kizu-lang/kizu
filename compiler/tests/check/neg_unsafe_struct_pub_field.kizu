/// data は少なくとも len バイトの有効なメモリを指す。
unsafe struct Buf {
    pub data: ptr<u8>,
    len: usize,
}

fn size(b: &Buf) -> usize {
    return b.len;
}

// check-fails
// features: unsafe-struct raw-pointer visibility
// error: cannot have `pub` field

// check
// load:
// unsafe struct Buf { pub data: ptr<u8>, len: usize }
// fn size(b: &Buf) -> usize { return b.len; }
// check:
// - unsafe error: `unsafe struct Buf` cannot have `pub` field `data`
//   help: drop `pub` so only this file can break the invariant
