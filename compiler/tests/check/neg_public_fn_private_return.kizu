struct Secret {
    pub v: i64,
}

pub fn make(v: i64) -> Secret {
    return Secret { v: v };
}

fn main() -> void {
    return;
}

// check-fails
// features: visibility type-check
// error: return type exposes private type `Secret`


// check
// load:
// struct Secret { pub v: i64 }
// pub fn make(v: i64) -> Secret { return Secret { v: v }; }
// fn main() -> void { return; }
// check:
// - type error: public function `make` return type exposes private type `Secret`
