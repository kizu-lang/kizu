struct Name { value: []u8 }
fn use(a: &Name, b: Name) {
    print(a.value);
    print(b.value);
}
fn main() {
    let name = Name { value: "alice" };
    use(name, name);
}


// check
// load:
// struct Name { value: []u8 }
// fn use(a: &Name, b: Name) { print(a.value); print(b.value); }
// fn main() { let name = Name { value: "alice" }; use(name, name); }
// check:
// 8:15-8:19 borrow error: value `name` cannot be moved while borrowed
