struct User { name: []u8 }
fn rename(user: &User) -> void { user.name = "bob" ;}

// check
// load:
// struct User { name: []u8 }
// fn rename(user: &User) -> void { user.name = "bob"; }
// check:
// - type error: cannot assign field through shared borrow `user`
