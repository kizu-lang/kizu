struct User { name: []u8 }
fn rename(user: &User) -> void { user.name = "bob" ;}

// check
// load:
// struct User { name: []u8 }
// fn rename(user: &User) -> void { user.name = "bob"; }
// check:
// 2:34-2:53 type error: cannot assign field through shared borrow `user`
