extern "c" fn source() -> ptr<const i64>
fn main() {
    // SAFETY: test source
    let p = unsafe source();
    // SAFETY: test source
    unsafe ptr_write(p, 1);}

// check
// load:
// extern "c" fn source() -> ptr<const i64>
// fn main() { let p = unsafe source(); unsafe ptr_write(p, 1); }
// check:
// - type error: `ptr_write` expects mutable non-null raw pointer
