enum Color {
    Red,
    Green,
}

fn main() {
    let color = Color::Red;
    match color {
        _ => print("other"),
        Red => print("red"),
    }
}

// check-fails
// features: match wildcard enum
// error: wildcard match arm must be last


// check
// load:
// enum Color { Red, Green }
// fn main() { let color = Color::Red; match color { _ => print("other"), Red => print("red") } }
// check:
// - type error: wildcard match arm must be last
