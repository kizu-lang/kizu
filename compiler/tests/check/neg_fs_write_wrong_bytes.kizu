import std;

fn main() -> !void {
    let io = std::io::blocking();
    try std::fs::write_file(io, "target.txt", 1);
    return;
}

// check-fails
// features: io fs type-check
// error: expects []u8 bytes

// check
// load:
// fn main() -> !void { let io = std::io::blocking(); try std::fs::write_file(io, "target.txt", 1); return; }
// check:
// - type error: `std::fs::write_file` expects []u8 bytes, got i64
