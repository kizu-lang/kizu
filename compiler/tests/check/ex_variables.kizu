fn main() {
    let name = "alice";
    var age = 30;
    age = age + 1;
    print(name);
    print(age);
}

// run
// features: let var assignment
// output:
// alice
// 31

// check
// load:
// fn main() { let name = "alice"; var age = 30; age = (age + 1); print(name); print(age); }
// check:
// ok
