import std::map;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::new<i64, i64>(allocator);
    print(table.len());
}

// check-fails
// features: std-map type-check
// error: key type must be []u8


// check
// load:
// fn main() { let allocator = std::mem::page_allocator(); let table = std::map::new<i64, i64>(allocator); print(table.len()); }
// check:
// - type error: std::map::Map key type must be []u8
