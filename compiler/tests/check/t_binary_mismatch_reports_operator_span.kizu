enum Color { Red, Green }
enum Animal { Cat, Dog }

fn main() {
    let color = Color::Green;
    if color == Animal::Cat { return; }
}

// check
// load:
// enum Color { Red, Green }
// enum Animal { Cat, Dog }
// fn main() { let color = Color::Green; if (color == Animal::Cat) { return; } }
// check:
// 6:14-6:16 [type.operator_type_mismatch] type error: operator `==` operands must have same type
//   note: left operand has type Color
//   note: right operand has type Animal
