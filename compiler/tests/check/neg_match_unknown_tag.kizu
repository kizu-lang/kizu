enum Color {
    Red,
    Green,
}

fn main() -> void {
    let color = Color::Red;
    match color {
        Red => print("red"),
        Blue => print("blue"),
    }
}

// check-fails
// features: match enum
// error: unknown match tag `Color::Blue`

// check
// load:
// enum Color { Red, Green }
// fn main() -> void { let color = Color::Red; match color { Red => print("red"), Blue => print("blue") } }
// check:
// 8:5-11:6 type error: unknown match tag `Color::Blue`
