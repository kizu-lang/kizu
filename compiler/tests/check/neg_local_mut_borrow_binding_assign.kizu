fn main() {
    var count = 1;
    let b = &var count;
    b = 5;
    print(count);
}

// check-fails
// features: borrow mutable-borrow
// error: cannot assign to immutable binding `b`

// check
// load:
// fn main() { var count = 1; let b = (&varcount); b = 5; print(count); }
// check:
// 4:5-4:11 type error: cannot assign to immutable binding `b`
