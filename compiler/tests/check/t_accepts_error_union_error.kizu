error ParseError {
    Bad,
}
fn parse() -> !i64 {
    return ParseError::Bad;
}
fn main() -> !void {
    let value = try parse();
    print(value);
    return;
}

// check
// load:
// error ParseError { Bad }
// fn parse() -> !i64 { return ParseError::Bad; }
// fn main() -> !void { let value = try parse(); print(value); return; }
// check:
// ok
