import std;
struct UserArena {}

struct User { name: []u8 }
fn main() {
    let users = std::arena::new<User, UserArena>(1);
    print(users);
}

// check
// load:
// struct UserArena {  }
// struct User { name: []u8 }
// fn main() { let users = std::arena::new<User, UserArena>(1); print(users); }
// check:
// - type error: `std::arena::new` allocator expects Allocator, got i64
