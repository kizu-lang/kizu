import std;
struct User { name: []u8 }
fn main() {
    let users = std::arena::new<User>(1);
    print(users);
}

// check
// load:
// struct User { name: []u8 }
// fn main() { let users = std::arena::new<User>(1); print(users); }
// check:
// 4:5-4:42 type error: `std::arena::new` allocator expects Allocator, got i64
