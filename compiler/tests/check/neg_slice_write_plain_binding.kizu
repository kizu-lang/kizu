fn main() -> !void {
    var bytes = "AB";
    bytes[0] = 65;
    print(bytes);
    return;
}

// check-fails
// features: mutable-slice-view
// error: indexed assignment requires a writable slice view (`&var []u8`), `bytes` is not one

// check
// load:
// fn main() -> !void { var bytes = "AB"; bytes[0] = 65; print(bytes); return; }
// check:
// 3:5-3:19 type error: indexed assignment requires a writable slice view (`&var []T`), `bytes` is not one
