union CompileError {
    Message([]u8),
}
fn parse() -> CompileError!i64 {
    return 1;
}

// check
// load:
// union CompileError { Message([]u8) }
// fn parse() -> CompileError!i64 { return 1; }
// check:
// 4:1-6:2 type error: the error of `E!T` must be an `error` set, got `CompileError`
