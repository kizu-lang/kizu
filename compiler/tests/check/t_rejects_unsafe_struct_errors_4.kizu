/// test invariant
unsafe struct Buf { pub data: ptr<u8> }
fn main() {}

// check
// load:
// unsafe struct Buf { pub data: ptr<u8> }
// fn main() {  }
// check:
// 2:1-2:40 unsafe error: `unsafe struct Buf` cannot have `pub` field `data`
//   help: drop `pub` so only this file can break the invariant
