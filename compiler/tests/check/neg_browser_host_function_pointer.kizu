extern "browser" fn notify(value: i32) -> void

fn main() -> void {
    let callback = notify;
}

// check
// load:
// extern "browser" fn notify(value: i32) -> void
// fn main() -> void { let callback = notify; }
// check:
// 4:20-4:26 type error: browser host function `notify` cannot be used as a function pointer
