fn main() -> void {
    while true {
        break :missing;
    }
}

// check-fails
// features: label break
// error: unknown loop label `missing`

// check
// load:
// fn main() -> void { while true { break :missing; } }
// check:
// - type error: unknown loop label `missing`
