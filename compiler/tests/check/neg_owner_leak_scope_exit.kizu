import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    let text = string::new(allocator);
    print(text.len());
    return;
}

// check-fails
// features: std-string ownership linear-consume
// error: owned value `text` is never deinitialized

// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); let text = std::string::new(allocator); print(text.len()); return; }
// check:
// 6:16-6:27 move error: owned value `text` is never deinitialized
