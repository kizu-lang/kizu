fn shared(value: &i64) -> &i64 {
    return value;
}
fn main() {
    var value = 1;
    let read = shared(value);
    value = 2;
    print(read.*);
}


// check
// load:
// fn shared(value: &i64) -> &i64 { return value; }
// fn main() { var value = 1; let read = shared(value); value = 2; print(read.*); }
// check:
// - borrow error: value `value` cannot be assigned while borrowed
