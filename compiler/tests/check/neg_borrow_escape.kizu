fn bad(s: &[]u8) -> []u8 {
    return s;
}

// check-fails
// features: borrow
// error: borrowed value `s` cannot escape


// check
// load:
// fn bad(s: &[]u8) -> []u8 { return s; }
// check:
// ok
