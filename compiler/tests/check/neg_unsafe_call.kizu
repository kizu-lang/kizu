extern "c" fn source() -> u8

fn main() {
    print(source());
}

// check-fails
// features: unsafe extern-c
// error: call to `source` requires `unsafe`

// check
// load:
// extern "c" fn source() -> u8
// fn main() { print(source()); }
// check:
// 4:11-4:17 [unsafe.missing_marker] unsafe error: call to `source` requires `unsafe`
//   help: `unsafe` here covers calls to `extern "<abi>" fn` declarations.
