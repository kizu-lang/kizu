import std::map;
import std::mem;

// A missing key is an optional miss, not an error: `try` no longer applies.
fn main() -> !void {
    let allocator = mem::page_allocator();
    let table = map::new<[]u8, i64>(allocator);
    defer table.deinit(allocator);

    print(try table.get("missing"));

    return;
}

// check-fails
// features: std-map optional diagnostics
// error: try expects !T, got ?i64

// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); let table = std::map::new<[]u8, i64>(allocator); defer table.deinit(allocator); print(try table.get("missing")); return; }
// check:
// - type error: try expects !T, got ?i64
