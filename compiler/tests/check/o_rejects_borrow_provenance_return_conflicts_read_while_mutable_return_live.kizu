fn mutable(value: &var i64) -> &var i64 {
    return value;
}
fn main() {
    var value = 1;
    let write = mutable(value);
    print(value);
    write.* = 2;
}


// check
// load:
// fn mutable(value: &var i64) -> &var i64 { return value; }
// fn main() { var value = 1; let write = mutable(value); print(value); write.* = 2; }
// check:
// 7:11-7:16 borrow error: value `value` cannot be read while mutably borrowed
