import std;
error BuildError { Boom }
struct User { name: []u8 }
fn build() -> !std::arena::Arena<User> {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    errdefer users.deinit();
    return move users;
}


// check
// load:
// error BuildError { Boom }
// struct User { name: []u8 }
// fn build() -> !std::arena::Arena<User> { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); errdefer users.deinit(); return move users; }
// check:
// ok
