import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    users.deinit(allocator);
    users.add(User { name: "alice" });
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); users.deinit(allocator); users.add(User { name: "alice" }); }
// check:
// 7:5-7:10 arena error: arena `users` was deinitialized
