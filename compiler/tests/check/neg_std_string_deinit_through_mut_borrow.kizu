import std::mem;
import std::string;

fn kill(allocator: Allocator, text: &var string::String) {
    text.deinit(allocator);
}

fn main() {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    kill(allocator, text);
    print(text.len());
}

// check-fails
// features: std-string mutable-borrow deinit
// error: requires owned String receiver

// check
// load:
// fn kill(allocator: Allocator, text: &var std::string::String) { text.deinit(allocator); }
// fn main() { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); kill(allocator, text); print(text.len()); }
// check:
// - type error: `String.deinit` requires owned String receiver
