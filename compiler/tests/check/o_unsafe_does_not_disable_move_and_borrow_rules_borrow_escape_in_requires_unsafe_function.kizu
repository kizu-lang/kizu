unsafe fn bad(s: &[]u8) -> []u8 {
    return s;
}


// check
// load:
// unsafe fn bad(s: &[]u8) -> []u8 { return s; }
// check:
// - unsafe error: `unsafe fn bad` needs a `///` comment stating what the caller must uphold
//   help: write `/// <what the caller must uphold>` above the declaration
