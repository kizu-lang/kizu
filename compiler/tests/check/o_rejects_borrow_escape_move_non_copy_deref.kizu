struct User { name: []u8 }
fn bad(user: &User) -> User {
    return user.*;
}


// check
// load:
// struct User { name: []u8 }
// fn bad(user: &User) -> User { return user.*; }
// check:
// - borrow error: value `user` cannot be moved out of borrow
