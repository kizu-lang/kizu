fn main() -> !void {
    var buf = [8]u8{};
    let b = &var buf;
    print(b);
    return;
}

// check-fails
// features: stack-buffer
// error: cannot borrow a stack buffer; use `as_bytes()` / `as_mut_bytes()`

// check
// load:
// fn main() -> !void { var buf = [8]u8{}; let b = (&varbuf); print(b); return; }
// check:
// 3:5-3:22 type error: cannot borrow a stack buffer; use `as_bytes()` / `as_mut_bytes()`
