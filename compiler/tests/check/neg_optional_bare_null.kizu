fn main() -> !void {
    let x = null;
    print(x);
    return;
}

// check-fails
// features: optional null diagnostics
// error: `null` needs an optional context

// check
// load:
// fn main() -> !void { let x = null; print(x); return; }
// check:
// 2:5-2:18 type error: `null` needs an optional context (a `?T` return or argument)
