fn take(buf: [8]u8) -> void {
    return;
}

fn main() -> !void {
    var buf = [8]u8{};
    take(buf);
    return;
}

// check-fails
// features: stack-buffer
// error: stack buffer parameter `buf` is not supported; pass a view (`[]u8` or `&var []u8`)


// check
// load:
// fn take(buf: [8]u8) -> void { return; }
// fn main() -> !void { var buf = [8]u8{}; take(buf); return; }
// check:
// - type error: stack buffer parameter `buf` is not supported; pass a view (`[]u8` or `&var []u8`)
