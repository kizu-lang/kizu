struct BytesIter {
    pub bytes: []u8,
    pub index: i64,
}

fn make() -> BytesIter {
    var buf = [4]u8{};
    let view = buf.as_bytes();
    let it = BytesIter { bytes: view, index: 0 };
    return it;
}

fn main() -> !void {
    let it = make();
    print(it.index);
    return;
}

// check-fails
// features: borrow view-capture diagnostics
// error: borrowed value `it` cannot escape

// check
// load:
// struct BytesIter { pub bytes: []u8, pub index: i64 }
// fn make() -> BytesIter { var buf = [4]u8{}; let view = buf.as_bytes(); let it = BytesIter { bytes: view, index: 0 }; return it; }
// fn main() -> !void { let it = make(); print(it.index); return; }
// check:
// 10:12-10:14 borrow error: borrowed value `it` cannot escape
