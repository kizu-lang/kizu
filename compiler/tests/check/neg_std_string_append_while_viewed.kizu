import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    defer text.deinit();
    try text.append_bytes("a");
    let bytes = text.as_bytes();
    try text.append_bytes("b");
    print(bytes);
    return;
}

// check-fails
// features: std-string borrow mutation
// error: cannot run while string is borrowed


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); defer text.deinit(); try text.append_bytes("a"); let bytes = text.as_bytes(); try text.append_bytes("b"); print(bytes); return; }
// check:
// ok
