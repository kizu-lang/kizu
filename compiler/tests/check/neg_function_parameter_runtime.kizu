fn worker(i: i64) -> void {
    print(i);
}

fn call(worker: Function) -> void {
    worker;
}

fn main() -> void {
    call(worker);
}

// check-fails
// features: comptime function-name
// error: belongs in `<...>`, not `(...)`

// check
// load:
// fn worker(i: i64) -> void { print(i); }
// fn call(worker: Function) -> void { worker; }
// fn main() -> void { call(worker); }
// check:
// 5:1-7:2 type error: Function parameter `worker` belongs in `<...>`, not `(...)`
