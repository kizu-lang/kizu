fn take(a: i64) -> i64 { return a ;}
fn main() { print(take("no")); }

// check
// load:
// fn take(a: i64) -> i64 { return a; }
// fn main() { print(take("no")); }
// check:
// - type error: arg 1 of `take` expects i64, got []u8
