fn take(a: i64) -> i64 { return a ;}
fn main() { print(take("no")); }

// check
// load:
// fn take(a: i64) -> i64 { return a; }
// fn main() { print(take("no")); }
// check:
// 2:13-2:31 type error: arg 1 of `take` expects i64, got []u8
