fn identity<T>(value: T) -> T {
    return value;
}
fn main() {
    print(identity(7));
}

// check
// load:
// fn identity<T>(value: T) -> T { return value; }
// fn main() { print(identity(7)); }
// check:
// - type error: `identity` requires explicit static arguments
