fn identity<T>(value: T) -> T {
    return value;
}
fn main() {
    print(identity(7));
}

// check
// load:
// fn identity<T>(value: T) -> T { return value; }
// fn main() { print(identity(7)); }
// check:
// 5:5-5:24 type error: `identity` requires explicit static arguments
