import std::array;
import std::mem;

fn main() -> !void {
    let allocator = mem::page_allocator();
    let values = array::new<i64>(allocator);

    values.deinit();
    print(values.len());
    return;
}

// check-fails
// features: std-array deinit move
// error: moved value


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); let values = std::array::new<i64>(allocator); values.deinit(); print(values.len()); return; }
// check:
// 9:11-9:17 move error: moved value `values` was used
