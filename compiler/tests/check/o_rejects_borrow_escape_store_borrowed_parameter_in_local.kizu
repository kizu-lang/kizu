fn bad(s: &[]u8) {
    let alias = s;
    print(alias);
}


// check
// load:
// fn bad(s: &[]u8) { let alias = s; print(alias); }
// check:
// 2:17-2:18 borrow error: borrowed value `s` cannot escape
