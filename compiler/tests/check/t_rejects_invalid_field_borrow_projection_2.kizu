struct Facts { value: i64 }
fn borrow_value(facts: &var Facts) -> &var i64 {
    return &var facts.value;
}

// check
// load:
// struct Facts { value: i64 }
// fn borrow_value(facts: &var Facts) -> &var i64 { return (&varfacts.value); }
// check:
// - type error: return expects &var i64, got i64
