fn main() -> void {
    let byte = 1[0];
    print(byte);
}

// check-fails
// features: slice-syntax type-check
// error: index/slice target expects []u8

// check
// load:
// fn main() -> void { let byte = 1[0]; print(byte); }
// check:
// 2:5-2:21 type error: index/slice target expects a view (`[]T`), got i64
