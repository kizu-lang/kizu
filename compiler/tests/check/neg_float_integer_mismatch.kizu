fn main() {
    let x = 1.5;
    print(cast<i64>(x * 2));
    return;
}

// check-fails
// features: float diagnostics
// error: operator `*` operands must have same type

// check
// load:
// fn main() { let x = 1.5; print(cast<i64>((x * 2))); return; }
// check:
// 3:23-3:24 [type.operator_type_mismatch] type error: operator `*` operands must have same type
//   note: left operand has type f64
//   note: right operand has type i64
