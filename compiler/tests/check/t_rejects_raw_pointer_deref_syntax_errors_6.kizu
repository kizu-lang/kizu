struct Node { tag: i64 }
fn read(p: ptr<Node>) -> i64 {
    return p.tag;}

// check
// load:
// struct Node { tag: i64 }
// fn read(p: ptr<Node>) -> i64 { return p.tag; }
// check:
// - type error: `ptr<Node>` has no fields
