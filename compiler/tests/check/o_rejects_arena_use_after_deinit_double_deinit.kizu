import std;
struct UserArena {}

struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User, UserArena>(allocator);
    users.deinit(allocator);
    users.deinit(allocator);
}


// check
// load:
// struct UserArena {  }
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User, UserArena>(allocator); users.deinit(allocator); users.deinit(allocator); }
// check:
// 9:5-9:10 arena error: arena `users` was deinitialized
