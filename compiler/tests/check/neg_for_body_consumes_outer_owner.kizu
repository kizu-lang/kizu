import std::mem;
import std::string;

fn take(allocator: Allocator, name: string::String) -> void {
    name.deinit(allocator);
    return;
}

fn main() -> !void {
    let allocator = mem::page_allocator();
    let name = try string::from_bytes(allocator, "abc");
    for 0..3 |_| {
        take(allocator, move name);
    }
    return;
}

// check-fails
// features: for ownership move
// error: owned value `name` is consumed inside a loop

// check
// load:
// fn take(allocator: Allocator, name: std::string::String) -> void { name.deinit(allocator); return; }
// fn main() -> !void { let allocator = std::mem::page_allocator(); let name = try std::string::from_bytes(allocator, "abc"); for 0..3 |_| { take(allocator, move name); } return; }
// check:
// 11:20-11:38 move error: owned value `name` is consumed inside a loop; the body runs an unknown number of times
