union Slot {
    Empty,
    Held(type),
}

fn main() -> void {
    print(1);
}

// check-fails
// features: comptime type-value union
// error: union variant `Slot::Held` cannot store type value

// check
// load:
// union Slot { Empty, Held(type) }
// fn main() -> void { print(1); }
// check:
// 1:1-4:2 type error: union variant `Slot::Held` cannot store type value
