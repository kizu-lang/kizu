struct Node { tag: i64 }
fn read(p: ptr<Node>) -> i64 {
    return p.*.tag;
}

// check
// load:
// struct Node { tag: i64 }
// fn read(p: ptr<Node>) -> i64 { return p.*.tag; }
// check:
// 3:14-3:15 [unsafe.missing_marker] unsafe error: raw pointer dereference requires `unsafe`
//   help: `unsafe` here covers raw pointer dereference such as `p.*` and `p.*.field`.
