///
unsafe fn poke() -> i64 { return 1; }
fn main() {}

// check
// load:
// unsafe fn poke() -> i64 { return 1; }
// fn main() {  }
// check:
// - unsafe error: `unsafe fn poke` needs a `///` comment stating what the caller must uphold
//   help: write `/// <what the caller must uphold>` above the declaration
