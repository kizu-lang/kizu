import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    try text.append_bytes(allocator, "lexer");
    try text.truncate("bad");
    return;
}

// check-fails
// features: std-string type-check
// error: expects i64

// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); try text.append_bytes(allocator, "lexer"); try text.truncate("bad"); return; }
// check:
// - type error: `String.truncate` expects i64, got []u8
