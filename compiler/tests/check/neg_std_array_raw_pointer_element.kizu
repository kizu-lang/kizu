import std::array;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let values = array::new<ptr<u8>>(allocator);
    print(values.len());
}

// check-fails
// features: std-array raw-pointer
// error: raw pointer

// check
// load:
// fn main() { let allocator = std::mem::page_allocator(); let values = std::array::new<ptr<u8>>(allocator); print(values.len()); }
// check:
// 6:18-6:28 type error: Array element is not safe: type error: Array element cannot be raw pointer
//   note: in `std::array::new<ptr<u8>>`
