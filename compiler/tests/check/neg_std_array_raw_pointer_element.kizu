import std::array;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let values = array::new<ptr<u8>>(allocator);
    print(values.len());
}

// check-fails
// features: std-array raw-pointer
// error: raw pointer


// check
// load:
// fn main() { let allocator = std::mem::page_allocator(); let values = std::array::new<ptr<u8>>(allocator); print(values.len()); }
// check:
// - type error: Array element is not safe: type error: Array element cannot be raw pointer
