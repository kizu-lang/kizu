import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let left = std::arena::new<User>(allocator);
    let right = std::arena::new<User>(allocator);
    print(right.at(left.add(User { name: "alice" })).name);
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let left = std::arena::new<User>(allocator); let right = std::arena::new<User>(allocator); print(right.at(left.add(User { name: "alice" })).name); }
// check:
// - arena error: handle from `left` does not belong to arena `right`
