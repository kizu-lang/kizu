struct User { name: []u8 }
fn rename(user: &var User) { user.name = "bob"; }
fn outer(user: &var User) {
    rename(user);
    user.name = "carol";
}
fn main() {
    var user = User { name: "alice" };
    outer(user);
}


// check
// load:
// struct User { name: []u8 }
// fn rename(user: &var User) { user.name = "bob"; }
// fn outer(user: &var User) { rename(user); user.name = "carol"; }
// fn main() { var user = User { name: "alice" }; outer(user); }
// check:
// ok
