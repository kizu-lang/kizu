import std;
fn main() -> !void {
    let allocator = std::mem::page_allocator();
    std::mem::box<i64>(allocator, 1);
    return;
}


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); std::mem::box<i64>(allocator, 1); return; }
// check:
// 4:5-4:18 move error: this expression produces owned `!std::mem::Box<i64>` and discards it; bind the value and consume it
