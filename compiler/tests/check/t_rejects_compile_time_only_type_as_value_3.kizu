fn take(x: ?Function) -> void { return; }
fn main() -> void { print(1); }

// check
// load:
// fn take(x: ?Function) -> void { return; }
// fn main() -> void { print(1); }
// check:
// 1:1-1:42 type error: ?Function parameter `x` belongs in `<...>`, not `(...)`
