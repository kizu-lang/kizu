fn take(x: ?Function) -> void { return; }
fn main() -> void { print(1); }

// check
// load:
// fn take(x: ?Function) -> void { return; }
// fn main() -> void { print(1); }
// check:
// - type error: ?Function parameter `x` belongs in `<...>`, not `(...)`
