extern "c" fn source() -> ptr<i64>
fn main() {
    // SAFETY: test source
    let p = unsafe source();
    // SAFETY: test source
    unsafe ptr_write(p, 1);
    // SAFETY: test source
    unsafe print(ptr_read(p));}

// check
// load:
// extern "c" fn source() -> ptr<i64>
// fn main() { let p = unsafe source(); unsafe ptr_write(p, 1); unsafe print(ptr_read(p)); }
// check:
// ok
