fn write(p: ptr<const u8>) -> void {
    // SAFETY: test source
    unsafe p.* = 1;
    return;}

// check
// load:
// fn write(p: ptr<const u8>) -> void { unsafe p.* = 1; return; }
// check:
// - type error: cannot assign through const raw pointer `ptr<const u8>`
