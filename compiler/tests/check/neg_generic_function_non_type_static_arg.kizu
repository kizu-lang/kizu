fn identity<T>(value: T) -> T {
    return value;
}

fn main() {
    print(identity<1>(1));
}

// check-fails
// features: generic comptime type-check
// error: unknown type `1`


// check
// load:
// fn identity<T>(value: T) -> T { return value; }
// fn main() { print(identity<1>(1)); }
// check:
// - type error: unknown type `1`
