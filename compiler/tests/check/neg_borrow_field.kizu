struct Bad {
    value: &[]u8,
}

fn main() {}

// check-fails
// features: borrow
// error: borrow field `Bad.value` cannot store borrow


// check
// load:
// struct Bad { value: &[]u8 }
// fn main() {  }
// check:
// - type error: borrow field `Bad.value` cannot store borrow
