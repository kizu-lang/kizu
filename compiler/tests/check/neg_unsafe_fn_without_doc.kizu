unsafe fn raw_increment(value: i64) -> i64 {
    return value + 1;
}

fn main() {}

// check-fails
// features: unsafe caller-obligation
// error: `unsafe fn raw_increment` needs a `///` comment

// check
// load:
// unsafe fn raw_increment(value: i64) -> i64 { return (value + 1); }
// fn main() {  }
// check:
// - unsafe error: `unsafe fn raw_increment` needs a `///` comment stating what the caller must uphold
//   help: write `/// <what the caller must uphold>` above the declaration
