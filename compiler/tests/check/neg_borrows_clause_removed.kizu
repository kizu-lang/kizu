fn first(bytes: []u8) -> []u8 borrows bytes {
    return bytes;
}

fn main() {}

// check-fails
// features: borrow-provenance parse
// error: expected next token

// check
// load:
// expected next token `{`, got identifier "borrows" at 1:28
