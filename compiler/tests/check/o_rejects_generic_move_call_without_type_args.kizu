fn pass<T>(value: T) -> T {
    return value;
}
fn main() {
    print(pass(1));
}


// check
// load:
// fn pass<T>(value: T) -> T { return value; }
// fn main() { print(pass(1)); }
// check:
// - type error: `pass` requires explicit static arguments
