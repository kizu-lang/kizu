fn main() {
    defer print("not cleanup");
}

// check-fails
// features: defer type-check
// error: defer expects cleanup method call


// check
// load:
// fn main() { defer print("not cleanup"); }
// check:
// - type error: defer expects cleanup method call
