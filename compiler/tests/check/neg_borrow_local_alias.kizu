fn bad(s: &[]u8) -> void {
    let alias = s;
    print(alias);
}

fn main() -> void {}

// check-fails
// features: borrow
// error: borrowed value `s` cannot escape


// check
// load:
// fn bad(s: &[]u8) -> void { let alias = s; print(alias); }
// fn main() -> void {  }
// check:
// 2:17-2:18 borrow error: borrowed value `s` cannot escape
