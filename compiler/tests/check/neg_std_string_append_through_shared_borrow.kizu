import std::mem;
import std::string;

fn add(text: &string::String) -> !void {
    try text.append_bytes("x");
    return;
}

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    try add(text);
    print(text.len());
    return;
}

// check-fails
// features: std-string borrow mutation
// error: requires mutable String receiver


// check
// load:
// fn add(text: &std::string::String) -> !void { try text.append_bytes("x"); return; }
// fn main() -> !void { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); try add(text); print(text.len()); return; }
// check:
// - type error: `String.append_bytes` requires mutable String receiver
