import std;
fn main() {
    let allocator = std::mem::page_allocator();
    std::string::new(allocator);
}


// check
// load:
// fn main() { let allocator = std::mem::page_allocator(); std::string::new(allocator); }
// check:
// 4:5-4:21 move error: this expression produces owned `std::string::String` and discards it; bind the value and consume it
