import std;
struct User { pub name: []u8 }
fn label<T, f: Field>() -> []u8 { return std::meta::field_name<T, f>(); }
fn main() -> void { print(label<User, nope>()); }

// check
// load:
// struct User { pub name: []u8 }
// fn label<T, f: Field>() -> []u8 { return std::meta::field_name<T, f>(); }
// fn main() -> void { print(label<User, nope>()); }
// check:
// - type error: `User` has no public field `nope`
