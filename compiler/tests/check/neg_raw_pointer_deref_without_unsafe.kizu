fn read(p: ptr<u8>) -> u8 {
    return p.*;
}

// check-fails
// features: unsafe raw-pointer deref
// error: raw pointer dereference requires `unsafe`


// check
// load:
// fn read(p: ptr<u8>) -> u8 { return p.*; }
// check:
// 2:14-2:15 [unsafe.missing_marker] unsafe error: raw pointer dereference requires `unsafe`
//   help: `unsafe` here covers raw pointer dereference such as `p.*` and `p.*.field`.
