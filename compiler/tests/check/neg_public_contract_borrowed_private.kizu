struct Secret {
    pub v: i64,
}

pub contract Peekable {
    fn peek() -> &[]Secret;
}

fn main() -> void {
    return;
}

// check-fails
// features: visibility contract borrow type-check
// error: return type exposes private type `Secret`


// check
// load:
// struct Secret { pub v: i64 }
// pub contract Peekable { fn peek() -> &[]Secret; }
// fn main() -> void { return; }
// check:
// - type error: public function `peek` return type exposes private type `Secret`
