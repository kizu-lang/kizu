fn take(s: []u8) -> []u8 {
    return s;
}
fn bad(s: &[]u8) {
    let escaped = take(s);
    print(escaped);
}


// check
// load:
// fn take(s: []u8) -> []u8 { return s; }
// fn bad(s: &[]u8) { let escaped = take(s); print(escaped); }
// check:
// 5:24-5:25 borrow error: borrowed value `s` cannot escape
