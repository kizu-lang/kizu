import std::process;

fn main() -> !void {
    let value = try process::arg(99);
    print(value);
    return;
}

// run-fails
// features: std-process bounds
// error: std::process::Error::ArgIndexOutOfBounds

// check
// load:
// fn main() -> !void { let value = try std::process::arg(99); print(value); return; }
// check:
// ok
