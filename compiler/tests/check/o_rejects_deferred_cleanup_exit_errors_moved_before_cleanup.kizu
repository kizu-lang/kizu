import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    defer users.deinit();
    let moved = move users;
    print(moved);
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); defer users.deinit(); let moved = move users; print(moved); }
// check:
// 6:11-6:16 move error: moved value `users` was used
