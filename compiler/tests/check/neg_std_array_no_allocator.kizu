import std::array;

fn main() {
    let values = array::new<i64>();
    print(values.len());
}

// check-fails
// features: std-array allocator type-check
// error: expects allocator

// check
// load:
// fn main() { let values = std::array::new<i64>(); print(values.len()); }
// check:
// 4:5-4:36 type error: `std::array::new` expects allocator
