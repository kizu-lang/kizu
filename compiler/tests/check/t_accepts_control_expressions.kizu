enum Color { Red, Green }
fn main() -> void {
    let color = Color::Green;
    let value = if true { 1 } else { 2 };
    let name = match color { Red => "red", Green => "green", };
    print(value);
    print(name);
}

// check
// load:
// enum Color { Red, Green }
// fn main() -> void { let color = Color::Green; let value = if true { 1; } else { 2; }; let name = match color { Red => "red", Green => "green" }; print(value); print(name); }
// check:
// ok
