unsafe struct Buf {
    data: ptr<u8>,
    len: usize,
}

fn main() {}

// check-fails
// features: unsafe-struct raw-pointer
// error: `unsafe struct Buf` needs a `///` comment

// check
// load:
// unsafe struct Buf { data: ptr<u8>, len: usize }
// fn main() {  }
// check:
// - unsafe error: `unsafe struct Buf` needs a `///` comment stating the invariant its fields carry
//   help: write `/// <the invariant its fields carry>` above the declaration
