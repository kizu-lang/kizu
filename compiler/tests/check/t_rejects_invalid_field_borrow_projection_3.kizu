struct Holder { value: &var i64 }
fn main() -> void {}

// check
// load:
// struct Holder { value: &var i64 }
// fn main() -> void {  }
// check:
// - type error: borrow field `Holder.value` cannot store borrow
