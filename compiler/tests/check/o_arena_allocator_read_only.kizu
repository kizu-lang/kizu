import std;
struct User {
    name: []u8,
}
fn main() {
    let allocator = std::mem::page_allocator();
    let left = std::arena::new<User>(allocator);
    let right = std::arena::new<User>(allocator);
    print(left);
    print(right);
    left.deinit(allocator);
    right.deinit(allocator);
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let left = std::arena::new<User>(allocator); let right = std::arena::new<User>(allocator); print(left); print(right); left.deinit(allocator); right.deinit(allocator); }
// check:
// 9:5-9:10 comptime error: `std::fmt::print` does not support type `std::arena::Arena<User>`
//   note: in `std::fmt::print<std::arena::Arena<User>>`
