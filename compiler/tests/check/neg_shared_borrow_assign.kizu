fn peek(n: &i64) -> i64 {
    n = n + 1;
    return n;
}

fn main() {
    let count = 1;
    print(peek(count));
}

// check-fails
// features: borrow
// error: cannot assign to immutable binding `n`


// check
// load:
// fn peek(n: &i64) -> i64 { n = (n + 1); return n; }
// fn main() { let count = 1; print(peek(count)); }
// check:
// - type error: cannot assign to immutable binding `n`
