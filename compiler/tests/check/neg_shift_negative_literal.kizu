fn main() {
    print(1 << -1);
    return;
}

// check-fails
// features: shift diagnostics
// error: shift amount `-1` is negative

// check
// load:
// fn main() { print((1 << (-1))); return; }
// check:
// 2:13-2:15 type error: shift amount `-1` is negative
//   note: `<<` shifts by the right operand, which must be 0 or more
//   help: shift the other way with `>>`
