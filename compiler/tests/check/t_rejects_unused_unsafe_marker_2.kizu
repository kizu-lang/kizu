fn write(p: ptr<u8>, value: u8) -> void {
    // SAFETY: test source
    unsafe (unsafe ptr_write(p, value));
}

// check
// load:
// fn write(p: ptr<u8>, value: u8) -> void { unsafe unsafe ptr_write(p, value); }
// check:
// 3:5-3:11 [unsafe.unused_marker] unsafe error: `unsafe` covers no operation that needs it
//   help: remove `unsafe`
