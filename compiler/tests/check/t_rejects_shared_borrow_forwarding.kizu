struct User { name: []u8 }
fn rename(user: &var User) -> void { user.name = "bob" ;}
fn outer(user: &User) -> void {
    rename(user);
}

// check
// load:
// struct User { name: []u8 }
// fn rename(user: &var User) -> void { user.name = "bob"; }
// fn outer(user: &User) -> void { rename(user); }
// check:
// 4:5-4:18 type error: &var argument `user` must be mutable
