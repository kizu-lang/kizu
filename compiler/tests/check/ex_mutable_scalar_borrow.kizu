fn bump(n: &var i64) -> void {
    n = n + 1;
    print(n);
}

fn main() {
    var count = 41;
    bump(count);
    print(count);
}

// run
// features: borrow mutable-borrow ownership
// output:
// 42
// 42


// check
// load:
// fn bump(n: &var i64) -> void { n = (n + 1); print(n); }
// fn main() { var count = 41; bump(count); print(count); }
// check:
// ok
