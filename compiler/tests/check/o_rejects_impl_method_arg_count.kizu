struct Counter { value: i64 }
fn (self: Counter) add(value: i64) -> i64 {
    return value;
}
fn main() {
    let counter = Counter { value: 1 };
    print(counter.add());
}


// check
// load:
// struct Counter { value: i64 }
// fn (self: Counter) Counter.add(value: i64) -> i64 { return value; }
// fn main() { let counter = Counter { value: 1 }; print(counter.add()); }
// check:
// - type error: `Counter.add` expects 1 args, got 0
