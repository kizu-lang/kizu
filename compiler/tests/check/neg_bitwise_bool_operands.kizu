fn main() {
    print(true & false);
    return;
}

// check-fails
// features: bitwise diagnostics
// error: operator `&` expects numeric operands

// check
// load:
// fn main() { print((true & false)); return; }
// check:
// 2:16-2:17 type error: operator `&` expects numeric operands
//   note: left operand has type bool
//   note: right operand has type bool
