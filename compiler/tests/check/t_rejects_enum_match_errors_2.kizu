enum Color { Red, Green }
fn main() {
    let color = Color::Red;
    match color { Red => print("red");, Blue => print("blue");, }
}

// check
// load:
// enum Color { Red, Green }
// fn main() { let color = Color::Red; match color { Red => print("red");, Blue => print("blue"); } }
// check:
// 4:5-4:66 type error: unknown match tag `Color::Blue`
