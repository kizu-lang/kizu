import std::array;
import std::mem;

fn main() -> !void {
    let allocator = mem::page_allocator();
    let values = array::new<i64>(allocator);

    try values.append(allocator, "no");
    return;
}

// check-fails
// features: std-array type-check
// error: Array.append

// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); let values = std::array::new<i64>(allocator); try values.append(allocator, "no"); return; }
// check:
// - type error: `Array.append` expects i64, got []u8
