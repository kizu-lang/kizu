struct User {
    name: []u8,
}

fn main() {
    let user = User { name: "alice" };
    print(user.age);
}

// check-fails
// features: struct field
// error: unknown field `User.age`

// check
// load:
// struct User { name: []u8 }
// fn main() { let user = User { name: "alice" }; print(user.age); }
// check:
// - type error: unknown field `User.age`
