import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = move string::new(allocator);
    defer text.deinit();
    return;
}

// check-fails
// features: move diagnostic std-string
// error: `move` marks a hand-off from a named place, and `std::string::new(allocator)` is not one


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); var text = move std::string::new(allocator); defer text.deinit(); return; }
// check:
// ok
