fn parse() -> !i64 {
    return 1;
}

fn main() {
    let x = try parse();
    print(x);
}

// check-fails
// features: try
// error: try requires function to return !T

// check
// load:
// fn parse() -> !i64 { return 1; }
// fn main() { let x = try parse(); print(x); }
// check:
// - type error: try requires function to return !T
