fn parse() -> !i64 {
    return 1;
}

fn main() -> !void {
    let x = try parse();
    print(x);
    return;
}

// check-fails
// features: try
// error: try requires function to return !T

// check
// load:
// fn parse() -> !i64 { return 1; }
// fn main() -> !void { let x = try parse(); print(x); return; }
// check:
// ok
