struct User { name: []u8 }
fn rename(user: &var User) -> void { user.name = "bob" ;}
fn main() -> void {
    var user = User { name: "alice" };
    user.name = "carol";
    rename(user);
}

// check
// load:
// struct User { name: []u8 }
// fn rename(user: &var User) -> void { user.name = "bob"; }
// fn main() -> void { var user = User { name: "alice" }; user.name = "carol"; rename(user); }
// check:
// ok
