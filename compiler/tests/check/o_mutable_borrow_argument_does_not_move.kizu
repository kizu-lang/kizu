struct User { name: []u8 }
fn show(user: &var User) { print(user.name); }
fn main() {
    let user = User { name: "alice" };
    show(user);
    print(user.name);
}


// check
// load:
// struct User { name: []u8 }
// fn show(user: &var User) { print(user.name); }
// fn main() { let user = User { name: "alice" }; show(user); print(user.name); }
// check:
// - type error: &var argument `user` must be mutable
