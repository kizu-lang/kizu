import std::string;

fn bad(text: string::String) -> []u8 {
    let bytes = text.as_bytes();
    return bytes;
}

fn main() {}

// check-fails
// features: borrow-provenance borrow-check std-string
// error: borrowed value `bytes` cannot escape


// check
// load:
// fn bad(text: std::string::String) -> []u8 { let bytes = text.as_bytes(); return bytes; }
// fn main() {  }
// check:
// ok
