import std;
error ViewError { Bad }
fn fail(allocator: Allocator, text: std::string::String) -> !void {
    let bytes = text.as_bytes();
    print(bytes);
    text.deinit(allocator);
    return ViewError::Bad;
}


// check
// load:
// error ViewError { Bad }
// fn fail(allocator: Allocator, text: std::string::String) -> !void { let bytes = text.as_bytes(); print(bytes); text.deinit(allocator); return ViewError::Bad; }
// check:
// ok
