import std;
error ViewError { Bad }
fn fail(text: std::string::String) -> !void {
    let bytes = text.as_bytes();
    print(bytes);
    text.deinit();
    return ViewError::Bad;
}


// check
// load:
// error ViewError { Bad }
// fn fail(text: std::string::String) -> !void { let bytes = text.as_bytes(); print(bytes); text.deinit(); return ViewError::Bad; }
// check:
// ok
