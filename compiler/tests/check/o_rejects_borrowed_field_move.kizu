struct User { name: []u8 }
struct Box { user: User }
fn take(user: User) { print(user.name); }
fn bad(box: &Box) {
    take(box.user);
}


// check
// load:
// struct User { name: []u8 }
// struct Box { user: User }
// fn take(user: User) { print(user.name); }
// fn bad(box: &Box) { take(box.user); }
// check:
// 5:10-5:18 borrow error: field `box.user` cannot be moved out of borrowed value `box`
