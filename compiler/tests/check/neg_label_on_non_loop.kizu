fn main() -> void {
    bad: print("no");
}

// check-fails
// features: label
// error: label `bad` must be attached to while or for

// check
// load:
// label `bad` must be attached to while or for at 2:10
