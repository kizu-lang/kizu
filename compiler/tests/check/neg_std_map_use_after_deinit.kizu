import std::map;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::new<[]u8, i64>(allocator);
    table.deinit();
    print(table.len());
}

// check-fails
// features: std-map deinit move
// error: moved value


// check
// load:
// fn main() { let allocator = std::mem::page_allocator(); let table = std::map::new<[]u8, i64>(allocator); table.deinit(); print(table.len()); }
// check:
// 8:11-8:16 move error: moved value `table` was used
