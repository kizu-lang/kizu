fn main() -> void {
    break;
}

// check-fails
// features: break
// error: `break` used outside loop

// check
// load:
// fn main() -> void { break; }
// check:
// 2:5-2:11 type error: `break` used outside loop
