fn main() -> void {
    break;
}

// check-fails
// features: break
// error: `break` used outside loop


// check
// load:
// fn main() -> void { break; }
// check:
// - type error: `break` used outside loop
