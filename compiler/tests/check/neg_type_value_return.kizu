fn element() -> type {
    return type<i64>;
}

fn main() -> void {
    print(1);
}

// check-fails
// features: comptime type-value return
// error: function `element` cannot return type

// check
// load:
// fn element() -> type { return type<i64>; }
// fn main() -> void { print(1); }
// check:
// 1:1-3:2 type error: function `element` cannot return type
