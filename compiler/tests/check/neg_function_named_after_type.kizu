struct Point {
    x: i64,
    y: i64,
}

fn Point(x: i64) -> i64 {
    return x + 100;
}

fn main() {
    print(Point(3));
}

// check-fails
// features: struct fn type-check
// error: `Point` is a type and cannot name a function

// check
// load:
// struct Point { x: i64, y: i64 }
// fn Point(x: i64) -> i64 { return (x + 100); }
// fn main() { print(Point(3)); }
// check:
// - type error: `Point` is a type and cannot name a function
