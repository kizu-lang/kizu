import std::mem;

fn main() {
    print(mem::equal_bytes("a", 1));
}

// check-fails
// features: std-mem type-check
// error: expects []u8


// check
// load:
// fn main() { print(std::mem::equal_bytes("a", 1)); }
// check:
// - type error: `std::mem::equal_bytes` right expects []u8, got i64
