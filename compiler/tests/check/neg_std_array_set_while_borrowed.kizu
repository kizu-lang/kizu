import std::array;
import std::mem;

struct Token {
    text: []u8,
}

fn main() -> !void {
    let allocator = mem::page_allocator();
    var tokens = array::new<Token>(allocator);
    defer tokens.deinit();

    try tokens.append(Token { text: "let" });
    if tokens.at(0) |first| {
        try tokens.set(0, Token { text: "var" });
        print(first.text);
    }
    return;
}

// check-fails
// features: std-array borrow mutation
// error: cannot run while array is borrowed


// check
// load:
// struct Token { text: []u8 }
// fn main() -> !void { let allocator = std::mem::page_allocator(); var tokens = std::array::new<Token>(allocator); defer tokens.deinit(); try tokens.append(Token { text: "let" }); if tokens.at(0) { try tokens.set(0, Token { text: "var" }); print(first.text); } return; }
// check:
// ok
