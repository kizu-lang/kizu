struct Counter { count: i64 }
fn (self: &var Counter) bump() -> void { self.count = self.count + 1; }
struct Holder { counter: Counter }
fn main() -> void {
    let h = Holder { counter: Counter { count: 0 } };
    h.counter.bump();
}

// check
// load:
// struct Counter { count: i64 }
// fn (self: &var Counter) Counter.bump() -> void { self.count = (self.count + 1); }
// struct Holder { counter: Counter }
// fn main() -> void { let h = Holder { counter: Counter { count: 0 } }; h.counter.bump(); }
// check:
// - type error: method `Counter.bump` takes `&var self`, receiver `h` must be mutable
