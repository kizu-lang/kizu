unsafe struct Buf { data: ptr<u8> }
fn main() {}

// check
// load:
// unsafe struct Buf { data: ptr<u8> }
// fn main() {  }
// check:
// 1:1-1:36 unsafe error: `unsafe struct Buf` needs a `///` comment stating the invariant its fields carry
//   help: write `/// <the invariant its fields carry>` above the declaration
