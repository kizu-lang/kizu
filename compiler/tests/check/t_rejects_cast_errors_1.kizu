fn bad(p: ptr<const u8>) -> ptr<u8> {
    return cast<ptr<u8>>(p);
}

// check
// load:
// fn bad(p: ptr<const u8>) -> ptr<u8> { return cast<ptr<u8>>(p); }
// check:
// 2:12-2:16 [unsafe.missing_marker] unsafe error: pointer cast requires `unsafe`
//   help: `unsafe` here covers raw pointer casts with `cast<ptr<...>>(value)`.
