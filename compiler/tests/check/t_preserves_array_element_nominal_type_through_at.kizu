import std;
struct MirCallArg {
    string_index: i64,
}
fn read(args: std::array::Array<MirCallArg>) -> !i64 {
    if args.at(0) |arg| {
        return arg.string_index;
    }
    return 0;
}
fn main() {}

// check
// load:
// struct MirCallArg { string_index: i64 }
// fn read(args: std::array::Array<MirCallArg>) -> !i64 { if args.at(0) { return arg.string_index; } return 0; }
// fn main() {  }
// check:
// - move error: owned value `args` is never deinitialized
