fn write_byte(p: ptr<u8>, value: u8) -> void {
    // SAFETY:
    unsafe ptr_write(p, value);
}

fn main() {}

// check-fails
// features: unsafe raw-pointer
// error: needs a `// SAFETY:` comment


// check
// load:
// fn write_byte(p: ptr<u8>, value: u8) -> void { unsafe ptr_write(p, value); }
// fn main() {  }
// check:
// 3:5-3:11 [unsafe.missing_safety_comment] unsafe error: `unsafe` needs a `// SAFETY:` comment on its statement
//   help: write `// SAFETY: <why this holds>` on the line above
