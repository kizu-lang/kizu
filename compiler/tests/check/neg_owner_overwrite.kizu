import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    text = string::new(allocator);
    text.deinit();
    return;
}

// check-fails
// features: std-string ownership linear-consume
// error: owned value `text` is overwritten before cleanup


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); var text = std::string::new(allocator); text = std::string::new(allocator); text.deinit(); return; }
// check:
// 7:12-7:23 move error: owned value `text` is overwritten before cleanup
