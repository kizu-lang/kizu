fn reject(value: ?ptr<?u8>) -> void {
    return;
}

fn main() {}

// check-fails
// features: optional diagnostics
// error: optional `?u8` cannot be a static argument yet

// check
// load:
// fn reject(value: ?ptr<?u8>) -> void { return; }
// fn main() {  }
// check:
// 1:1-3:2 type error: optional `?u8` cannot be a static argument yet
