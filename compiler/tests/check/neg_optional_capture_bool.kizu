fn main() -> !void {
    if true |v| {
        print(v);
    }
    return;
}

// check-fails
// features: optional if-capture diagnostics
// error: if capture `|v|` requires an optional condition, got bool

// check
// load:
// fn main() -> !void { if true { print(v); } return; }
// check:
// - type error: if capture `|v|` requires an optional condition, got bool
