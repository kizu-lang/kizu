import std;
struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    users.deinit();
    let borrowed = &users;
    print(borrowed);
}


// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); users.deinit(); let borrowed = (&users); print(borrowed); }
// check:
// 7:21-7:26 arena error: arena `users` was deinitialized
