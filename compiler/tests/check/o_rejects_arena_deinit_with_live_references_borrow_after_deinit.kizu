import std;
struct UserArena {}

struct User { name: []u8 }
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User, UserArena>(allocator);
    users.deinit(allocator);
    let borrowed = &users;
    print(borrowed);
}


// check
// load:
// struct UserArena {  }
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User, UserArena>(allocator); users.deinit(allocator); let borrowed = (&users); print(borrowed); }
// check:
// 9:21-9:26 arena error: arena `users` was deinitialized
