enum Color { Red, Green }
fn main() {
    let color = Color::Red;
    match color { Red => print("red");, Red => print("again");, Green => print("green");, }
}

// check
// load:
// enum Color { Red, Green }
// fn main() { let color = Color::Red; match color { Red => print("red");, Red => print("again");, Green => print("green"); } }
// check:
// 4:5-4:92 type error: duplicate match tag `Color::Red`
