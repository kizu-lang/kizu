import std::arena;

struct User {
    name: []u8,
}

fn main() {
    let users = arena::new<User>();
    print(users);
}

// check-fails
// features: arena allocator
// error: `std::arena::new` expects allocator

// check
// load:
// struct User { name: []u8 }
// fn main() { let users = std::arena::new<User>(); print(users); }
// check:
// - type error: `std::arena::new` expects allocator
