import std;
struct User { name: []u8 }
struct Registry { users: std::arena::Arena<User> }
fn (self: Registry) deinit() -> void {
    self.users.deinit();
    return;
}
fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::new<User>(allocator);
    let registry = Registry { users: users };
    registry.deinit();
}

// check
// load:
// struct User { name: []u8 }
// struct Registry { users: std::arena::Arena<User> }
// fn (self: Registry) Registry.deinit() -> void { self.users.deinit(); return; }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); let registry = Registry { users: users }; registry.deinit(); }
// check:
// 11:38-11:43 move error: `users` is handed off here; write `move users`
