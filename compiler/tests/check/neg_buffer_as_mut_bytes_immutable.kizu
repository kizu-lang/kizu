fn main() -> !void {
    let buf = [8]u8{};
    let view = buf.as_mut_bytes();
    view[0] = 65;
    return;
}

// check-fails
// features: stack-buffer
// error: `buffer.as_mut_bytes` requires mutable buffer binding

// check
// load:
// fn main() -> !void { let buf = [8]u8{}; let view = buf.as_mut_bytes(); view[0] = 65; return; }
// check:
// 3:5-3:35 type error: `buffer.as_mut_bytes` requires mutable buffer binding
