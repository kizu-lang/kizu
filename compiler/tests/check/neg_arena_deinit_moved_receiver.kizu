import std::arena;
import std::mem;

struct User {
    name: []u8,
}

fn main() {
    let allocator = mem::page_allocator();
    let users = arena::new<User>(allocator);
    let moved = move users;
    users.deinit(allocator);
    moved.deinit(allocator);
}

// check-fails
// features: arena move deinit
// error: moved value `users` was used

// check
// load:
// struct User { name: []u8 }
// fn main() { let allocator = std::mem::page_allocator(); let users = std::arena::new<User>(allocator); let moved = move users; users.deinit(allocator); moved.deinit(allocator); }
// check:
// 12:5-12:10 move error: moved value `users` was used
