fn take(s: []u8) -> []u8 {
    return s;
}

fn bad(s: &[]u8) -> void {
    let escaped = take(s);
    print(escaped);
}

fn main() -> void {}

// check-fails
// features: borrow ownership
// error: borrowed value `s` cannot escape


// check
// load:
// fn take(s: []u8) -> []u8 { return s; }
// fn bad(s: &[]u8) -> void { let escaped = take(s); print(escaped); }
// fn main() -> void {  }
// check:
// ok
