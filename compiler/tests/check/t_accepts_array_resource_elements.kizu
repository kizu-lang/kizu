import std;
struct User { name: []u8 }
struct Parsed {
    users: std::arena::Arena<User>,
    ids: std::array::Array<i64>,
}
fn (self: Parsed) deinit(allocator: Allocator) -> void {
    self.users.deinit(allocator);
    self.ids.deinit(allocator);
}
fn check(allocator: Allocator, values: std::array::Array<Parsed>) -> !void {
    if values.pop() |item| {
        item.deinit(allocator);
    }
    values.deinit(allocator);
    return;
}
fn main() {}

// check
// load:
// struct User { name: []u8 }
// struct Parsed { users: std::arena::Arena<User>, ids: std::array::Array<i64> }
// fn (self: Parsed) Parsed.deinit(allocator: Allocator) -> void { self.users.deinit(allocator); self.ids.deinit(allocator); }
// fn check(allocator: Allocator, values: std::array::Array<Parsed>) -> !void { if values.pop() { item.deinit(allocator); } values.deinit(allocator); return; }
// fn main() {  }
// check:
// ok
