struct User {
    age: i64,
}

fn update(user: &User) -> void {
    user.age = 31;
}

fn main() -> void {
    let user = User { age: 30 };
    update(user);
}

// check-fails
// features: borrow field-access assignment
// error: cannot assign field through shared borrow `user`

// check
// load:
// struct User { age: i64 }
// fn update(user: &User) -> void { user.age = 31; }
// fn main() -> void { let user = User { age: 30 }; update(user); }
// check:
// 6:5-6:19 type error: cannot assign field through shared borrow `user`
