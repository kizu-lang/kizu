enum Color { Red, Green }
fn main() {
    let color = Color::Red;
    match color { Red => print("red");, }
}

// check
// load:
// enum Color { Red, Green }
// fn main() { let color = Color::Red; match color { Red => print("red"); } }
// check:
// 4:5-4:42 type error: match on `Color` is not exhaustive: missing Green
