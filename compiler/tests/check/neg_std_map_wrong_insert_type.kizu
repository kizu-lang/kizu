import std::map;
import std::mem;

fn main() -> !void {
    let allocator = mem::page_allocator();
    let table = map::new<[]u8, i64>(allocator);

    try table.insert("main", "function");

    table.deinit();
    return;
}

// check-fails
// features: std-map type-check
// error: Map.insert


// check
// load:
// fn main() -> !void { let allocator = std::mem::page_allocator(); let table = std::map::new<[]u8, i64>(allocator); try table.insert("main", "function"); table.deinit(); return; }
// check:
// - type error: `Map.insert` expects i64 value, got []u8
