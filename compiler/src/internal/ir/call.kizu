import compiler::internal::ast;
import compiler::internal::diagnostic;

// STUB: port of the call half of internal/ir/lower.go pending.

fn (self: &var Lowerer) lower_call_expr(tree: &var ast::Ast, call: ast::CallExpr, out: &var Value) -> !?diagnostic::Diagnostic {
    return try self.fail0("ir error: unported lower_call_expr");
}

fn (self: &var Lowerer) function_callee_name(tree: &var ast::Ast, callee: ast::Expression) -> !?Name {
    return null;
}

fn (self: &var Lowerer) lower_try_expr(tree: &var ast::Ast, expression: ast::Expression, attempt: ast::TryExpr, out: &var Value) -> !?diagnostic::Diagnostic {
    return try self.fail0("ir error: unported lower_try_expr");
}

/// Is the call op of one std container method instance and its signature,
/// the Go `(string, Signature)` pair `stdContainerCallOp` returns.
pub struct ContainerCallOp {
    pub symbol: Name,
    pub signature: SignatureHandle
}

fn (self: &var Lowerer) std_container_call_op(tree: &var ast::Ast, receiver: []u8, method: Name, type_arg: Name, out: &var ContainerCallOp) -> !?diagnostic::Diagnostic {
    return try self.fail0("ir error: unported std_container_call_op");
}

fn (self: &var Lowerer) impl_method_callee_name(receiver: Name, method: Name) -> !?Name {
    return null;
}

fn (self: &var Lowerer) method_takes_self_storage(receiver_type: Name, method: Name) -> !bool {
    return false;
}
