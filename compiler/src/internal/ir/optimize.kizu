import compiler::internal::diagnostic;

// STUB: port of internal/ir/optimize.go pending.

/// Applies bounded local cleanup passes to a module, the Go `Optimize`.
pub fn optimize(allocator: Allocator, module: &var Module) -> !?diagnostic::Diagnostic {
    return verify(allocator, module);
}
