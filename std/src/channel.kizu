pub fn Channel<T>() -> Channel<T> {
    return std::builtin::channel<T>();
}

fn send<T>(self: Channel<T>, value: T) -> void {
    std::builtin::channel_send<T>(self, value);
}

fn recv<T>(self: Channel<T>) -> T {
    return std::builtin::channel_recv<T>(self);
}
