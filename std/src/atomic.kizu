pub fn Atomic<T>(value: T) -> Atomic<T> {
    return std::builtin::atomic<T>(value);
}

fn load<T>(self: Atomic<T>) -> T {
    return std::builtin::atomic_load<T>(self);
}

fn store<T>(self: Atomic<T>, value: T) -> void {
    std::builtin::atomic_store<T>(self, value);
}
