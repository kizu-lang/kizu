struct User {
    name: []const u8;
}

fn rename(user: &mut User) -> void {
    user.*.name = "bob";
    print(user.*.name);
}

fn main() {
    var user = User { name: "alice" };
    rename(user);
    print(user.name);
}
