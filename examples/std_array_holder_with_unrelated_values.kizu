struct Holder {
    text: []const u8;
}

fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let holders = std::array::Array<Holder>(allocator);
    let values = 1;

    print(holders.len());
    print(values);
    return void;
}
