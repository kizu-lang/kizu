import app::ast;

pub error CompileError {
    ParseFailed,
    CheckFailed,
}

pub fn parse_module(ok: bool) -> CompileError!ast::AstModule {
    if ok {
        return ast::AstModule { nodes: 7 };
    }
    return CompileError::ParseFailed;
}
