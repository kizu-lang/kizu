import app::checker;
import app::parser;

pub fn main() -> parser::CompileError!void {
    let checked = try checker::check_module(true);
    print(checked.nodes);
    return;
}

// run
// features: modules typed-error parser-phase checker-phase diagnostic
// output:
// 7
