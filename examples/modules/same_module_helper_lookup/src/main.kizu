struct Summary {
    next: i64,
    typed_nodes: i64,
    diagnostics: i64,
}

fn summary(next: i64, typed_nodes: i64, diagnostics: i64) -> Summary {
    return Summary {
        next: next,
        typed_nodes: typed_nodes,
        diagnostics: diagnostics
    };
}

fn merge_summaries(left: Summary, right: Summary) -> Summary {
    return summary(
        right.next,
        left.typed_nodes + right.typed_nodes,
        left.diagnostics + right.diagnostics
    );
}

pub fn main() -> !void {
    var total = summary(0, 0, 0);
    total = merge_summaries(total, summary(1, 2, 3));

    try std::testing::expect_equal_i64(1, total.next);
    try std::testing::expect_equal_i64(2, total.typed_nodes);
    try std::testing::expect_equal_i64(3, total.diagnostics);
    return;
}
