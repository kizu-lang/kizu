import app::checker;
import app::parser;

pub fn main() -> parser::CompileError!void {
    let checked = try checker::check_module(false);
    print("lowered");
    print(checked.nodes);
    return;
}

// run-fails
// features: modules typed-error parser-phase checker-phase diagnostic
// error: app::parser::CompileError::ParseFailed
