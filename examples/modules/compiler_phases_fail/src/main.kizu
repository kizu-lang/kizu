import app::checker;
import app::parser;

pub fn main() -> parser::CompileError!void {
    let checked = try checker::check_module(false);
    print("lowered");
    print(checked.nodes);
    return;
}
