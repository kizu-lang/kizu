import app::checker;
import app::parser;

pub fn main() -> parser::CompileError!void {
    let allocator = std::mem::page_allocator();
    let checked = try checker::check_module(allocator, false);
    print("lowered");
    print(checked.nodes);
    return;
}
