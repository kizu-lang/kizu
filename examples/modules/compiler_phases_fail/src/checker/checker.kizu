import app::ast;
import app::parser;

pub fn check_module(ok: bool) -> parser::CompileError!ast::CheckedModule {
    let parsed = try parser::parse_module(ok);
    if parsed.nodes < 1 {
        return parser::CompileError::CheckFailed;
    }
    return ast::CheckedModule { nodes: parsed.nodes };
}
