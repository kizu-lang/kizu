fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let values = std::array::Array<i64>(allocator);

    try values.append(10);
    try values.append(32);

    print(values.len());
    print(values.capacity());
    print(try values.get(0));
    print(try values.get(1));

    values.deinit();
    return;
}
