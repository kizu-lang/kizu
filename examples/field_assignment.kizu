struct User {
    name: []const u8;
    age: i64;
}

fn main() -> void {
    var user = User { name: "alice", age: 30 };
    user.name = "bob";
    user.age = user.age + 1;
    print(user.name);
    print(user.age);
}
