fn main() -> !void {
    let bytes = "lexer token";
    let first = try bytes[0];
    let lexer = try bytes[0..5];
    let token = try bytes[6..];
    let prefix = try bytes[..5];

    print(cast<i64>(first));
    print(lexer);
    print(token);
    print(prefix);
    return void;
}
