fn main() -> !void {
    std::testing::expect(true);
    std::testing::expect_equal<i64>(3, 1 + 2);
    std::testing::expect(std::mem::starts_with("lexer", "lex"));
    std::testing::expect_equal<[]u8>("token", "token");
    if false {
        try std::testing::fail("unreachable");
    }

    return;
}
