fn main() -> !void {
    let io = std::io::blocking();
    var entries = try std::fs::read_dir(io, "selfhost/src");
    std::testing::expect(entries.len() >= 16);
    let first = try entries.get(0);
    std::testing::expect_equal<[]const u8>("ast.kizu", first.name);
    std::testing::expect_equal<[]const u8>("selfhost/src/ast.kizu", first.path);
    std::testing::expect(!first.is_dir);
    entries.deinit();
    return;
}
