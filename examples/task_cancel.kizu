fn fail(io: Io) -> !i64 {
    let ch = std::channel::Channel<i64>();
    return ch.recv();
}

fn main() -> void {
    let io = std::io::threaded();
    let group = std::task::Group(io);
    let task = group.spawn(fail);
    task.cancel();
    print("canceled");
}
