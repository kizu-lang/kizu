import std;

fn main() -> !void {
    let code = try std::process::spawn_wait8(
        1,
        "/bin/true",
        "",
        "",
        "",
        "",
        "",
        "",
        ""
    );
    print(code);
    return;
}

// check
// features: std-process error-union try
