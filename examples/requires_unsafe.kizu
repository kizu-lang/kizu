@requires_unsafe() fn raw_increment(value: i64) -> i64 {
    return value + 1;
}

fn main() {
    @unsafe(unsafe_call) {
        print(raw_increment(1));
    }
}

// check
// features: unsafe caller-obligation
