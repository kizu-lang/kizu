struct User {
    name: []const u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let users = arena<User>(allocator);
    users.deinit();
    users.add(User { name: "alice" });
}
