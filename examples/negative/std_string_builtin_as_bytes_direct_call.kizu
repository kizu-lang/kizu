import std::mem;
import std::string;

fn main() {
    let allocator = mem::page_allocator();
    var text = string::String(allocator);
    let bytes = std::builtin::string_as_bytes(text);
    print(bytes);
}

// check-fails
// features: std-string builtin-boundary borrow type-check
// error: was removed
