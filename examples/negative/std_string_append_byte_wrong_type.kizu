fn main() -> !void {
    let allocator = std::mem::page_allocator();
    var text = std::string::String(allocator);
    try text.append_byte("x");
    return;
}

// check-fails
// features: std-string type-check
// error: expects u8
