fn worker(i: i64) -> i64 {
    return i;
}

fn main() -> void {
    let io = std::io::blocking();
    var out = std::task::partition_mut(0, 2);
    std::task::parallel_map(io, out, 0, 2, worker(0));
}
