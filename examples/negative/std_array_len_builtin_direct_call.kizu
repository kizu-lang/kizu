import std::array;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let values = array::Array<i64>(allocator);
    print(std::internal::builtin::array_len<i64>(values));
}

// check-fails
// features: std-array builtin-boundary
// error: reserved
