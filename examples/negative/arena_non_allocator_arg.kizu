import std::arena;

struct UserArena {}

struct User {
    name: []u8,
}

fn main() {
    let users = arena::new<User, UserArena>(1);
    print(users);
}

// check-fails
// features: arena allocator
// error: expects Allocator, got i64
