import std::arena;

struct User {
    name: []u8,
}

fn main() {
    let users = arena::Arena<User>(1);
    print(users);
}

// check-fails
// features: arena allocator
// error: expects Allocator, got i64
