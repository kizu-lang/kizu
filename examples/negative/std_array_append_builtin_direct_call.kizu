fn main() {
    let allocator = std::mem::page_allocator();
    let values = std::array::Array<i64>(allocator);
    std::builtin::array_append<i64>(values, 1);
}

// check-fails
// features: std-array builtin-boundary
// error: reserved
