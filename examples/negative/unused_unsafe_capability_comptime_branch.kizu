fn probe(p: ptr<const u8>) -> void {
    @unsafe(ptr_read) {
        comptime if 1 + 1 == 3 {
            print(ptr_read(p));
        } else {
            print(0);
        }
    }
}

fn main() {}

// check-fails
// features: unsafe raw-pointer comptime
// error: capability `ptr_read` is declared but never used
