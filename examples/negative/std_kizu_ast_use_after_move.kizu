fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let source = std::kizu::ast::source_file("main.kizu", "");
    var ast = std::kizu::ast::Ast(allocator, source);
    let moved = ast;
    let start = ast.begin_children();
    print(start);
    moved.deinit();
    return;
}
