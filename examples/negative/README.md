# Negative Examples

These files are expected to fail. Each file demonstrates a v0.1 safety rule or
static policy that must keep producing a readable diagnostic.

They are verified by `TestV01NegativeExamples` in `cmd/kizu/conformance_test.go`.
