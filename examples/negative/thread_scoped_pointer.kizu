extern "c" fn source() -> ptr<i64>

fn worker(p: ptr<i64>) -> ptr<i64> {
    return p;
}

fn main() -> void {
    let io = std::io::blocking();
    @unsafe(extern_call) {
        let p = source();
        std::thread::scoped<ptr<i64>, worker>(io, p);
    }
}
