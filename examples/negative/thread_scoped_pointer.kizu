extern "c" fn source() -> ptr<i64>

fn worker(p: ptr<i64>) -> void {
    print(p);
}

fn main() -> void {
    let io = Io();
    unsafe {
        let p = source();
        std::thread::scoped(io, worker, p);
    }
}
