struct User {
    name: []const u8;
}

fn main() {
    let users = arena<User>();
    let alice = users.add(User { name: "alice" });
    let p = cast<ptr<User>>(alice);
    unsafe {
        print(ptr_read(p).name);
    }
}
