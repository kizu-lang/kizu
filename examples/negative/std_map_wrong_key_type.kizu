import std::map;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::Map<i64, i64>(allocator);
    print(table.len());
}

// check-fails
// features: std-map type-check
// error: key type must be []u8
