fn main() {
    let allocator = std::mem::page_allocator();
    let table = std::map::Map<i64, i64>(allocator);
    print(table.len());
}

// check-fails
// features: std-map type-check
// error: key type must be []u8
