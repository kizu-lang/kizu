import std::mem;

fn main() -> !void {
    let byte = try mem::byte_at("abc", 3);
    print(cast<i64>(byte));
    return;
}

// run-fails
// features: std-mem bounds error-union
// error: std::mem::Error::OutOfBounds
