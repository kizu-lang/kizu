struct User {
    name: []const u8;
}

fn main() {
    let users = arena<User>();
    let user = User { name: "alice" };
    let alice = users.add(user);
    print(user.name);
    print(users.get(alice).name);
}
