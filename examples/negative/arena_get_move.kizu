struct User {
    name: string
}

fn take(user: User) {
    print(user.name)
}

fn main() {
    let users = arena<User>()
    let alice = users.add(User { name: "alice" })
    take(users.get(alice))
}
