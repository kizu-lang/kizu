fn main() -> !void {
    let exists = try std::fs::exists("examples/fixtures/config.txt", "unused");
    print(exists);
    return;
}

// check-fails
// features: std-fs explicit-io
// error: expects Io
