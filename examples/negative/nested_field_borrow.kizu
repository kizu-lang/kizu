struct Profile {
    name: []u8,
}

struct User {
    profile: Profile,
}

fn main() -> void {
    let user = User { profile: Profile { name: "alice" } };
    let name = &user.profile.name;
    print(name.*);
}

// check-fails
// features: borrow field-borrow
// error: field borrow only supports one direct field
