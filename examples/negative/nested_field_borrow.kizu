struct Profile {
    name: []const u8;
}

struct User {
    profile: Profile;
}

fn main() -> void {
    let user = User { profile: Profile { name: "alice" } };
    let name = &user.profile.name;
    print(name.*);
}
