fn write(p: ptr<const u8>) -> void {
    @unsafe(ptr_deref) {
        p.* = 1;
        return;
    }
}

// check-fails
// features: unsafe raw-pointer deref assignment
// error: cannot assign through const raw pointer
