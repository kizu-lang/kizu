// `@link_library` and `@link_framework` are the only attributes; a spelling the
// compiler does not know is refused rather than ignored.
@link_name("cbrt")
extern "c" fn cube_root(value: f64) -> f64

fn main() -> void {
    return;
}

// parse-fails
// features: link-library diagnostic
// error: unknown attribute @link_name
