fn worker(i: i64) -> i64 {
    return i;
}

fn main() -> void {
    let io = std::io::blocking();
    var parts = std::task::partition_mut(0, 2);
    std::task::parallel_map(io, parts, 0, 3, worker);
}
