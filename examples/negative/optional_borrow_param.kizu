fn take(v: &?u8) -> void {
    return;
}

fn main() -> !void {
    print(1);
    return;
}

// check-fails
// features: optional borrow diagnostics
// error: parameter `v` cannot borrow an optional yet
