fn worker(i: i64) -> !void {
    if i == 1 {
        return error("parallel failed");
    }
    return;
}

fn main() -> !void {
    let io = std::io::blocking();
    try std::task::parallel_for<worker>(io, 0, 3);
    return;
}
