fn load(io: Io, name: borrow []const u8) -> !i64 {
    print(name)
    return 1
}

fn main() -> !void {
    let io = Io()
    let group = TaskGroup()
    let name = "config"
    let task = group.spawn(io, load, name)
    let value = try task.await()
    print(value)
    return void
}
