fn kill(symbols: &std::map::Map<[]u8, i64>) {
    symbols.deinit();
}

fn main() {
    let allocator = std::mem::page_allocator();
    let symbols = std::map::Map<[]u8, i64>(allocator);
    kill(&symbols);
}
