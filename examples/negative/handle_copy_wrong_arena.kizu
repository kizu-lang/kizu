import std::arena;
import std::mem;

struct User {
    name: []u8,
}

fn main() -> !void {
    let allocator = mem::page_allocator();
    let left = arena::new<User>(allocator);
    defer left.deinit(allocator);
    let right = arena::new<User>(allocator);
    defer right.deinit(allocator);
    let alice = try left.add(allocator, User { name: "alice" });
    let again = alice;
    print(right.at(again).name);
    return;
}

// check-fails
// features: arena handle
// error: handle `again` does not belong to arena `right`
