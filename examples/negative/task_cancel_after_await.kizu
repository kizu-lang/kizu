fn load(io: Io) -> !i64 {
    return 1;
}

fn main() -> !void {
    let io = std::io::blocking();
    let group = std::task::Group(io);
    let task = group.spawn(load);
    let value = try task.await();
    task.cancel();
    print(value);
    return;
}
