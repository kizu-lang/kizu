struct Name {
    value: []const u8,
}

fn take(name: Name) -> void {
    print(name.value);
}

fn main() -> void {
    let name = Name { value: "alice" };
    if true {
        take(name);
    } else {
        take(Name { value: "bob" });
    }
    print(name.value);
}
