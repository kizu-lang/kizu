import std::array;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let values = array::Array<i64>(allocator);
    std::internal::builtin::array_set<i64>(values, 0, 1);
}

// check-fails
// features: std-array builtin-boundary
// error: reserved
