union Shape {
    Point,
    Circle(i64),
}

fn main() {
    let shape = Shape::Circle(1);
    match shape {
        _(value) => print(value),
    }
}

// check-fails
// features: match wildcard union
// error: wildcard match arm cannot bind payload
