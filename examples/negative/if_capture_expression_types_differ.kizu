fn maybe_count(value: i64) -> ?i64 {
    if value > 0 {
        return value;
    }
    return null;
}

fn main() -> void {
    let shown = if maybe_count(3) |n| { n } else { "none" };
    print(shown);
    return;
}

// check-fails
// features: if-expression capture optional
// error: if expression branch types differ: i64 vs []u8
