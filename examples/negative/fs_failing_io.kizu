import std::fs;
import std::testing;

fn main() -> !void {
    let io = testing::failing_io();
    let text = try fs::read_file(io, "examples/fixtures/config.txt");
    print(text);
    return;
}

// run-fails
// features: io fs error-union
// error: std::fs::Error::IoFailing
