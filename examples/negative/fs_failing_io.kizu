fn main() -> !void {
    let io = std::testing::failing_io();
    let text = try std::fs::read_file(io, "examples/fixtures/config.txt");
    print(text);
    return;
}

// run-fails
// features: io fs error-union
// error: std::fs::Error::IoFailing
