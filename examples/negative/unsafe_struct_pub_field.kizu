/// data は少なくとも len バイトの有効なメモリを指す。
unsafe struct Buf {
    pub data: ptr<u8>,
    len: usize,
}

fn size(b: &Buf) -> usize {
    return b.len;
}

// check-fails
// features: unsafe-struct raw-pointer visibility
// error: cannot have `pub` field
