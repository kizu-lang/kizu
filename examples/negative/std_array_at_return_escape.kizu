import std::array;

struct Token {
    text: []u8,
}

// A borrow optional exists only as a capture condition: a signature returning
// `?&T` would let the element borrow outlive the borrow the capture holds.
fn first(tokens: &array::Array<Token>) -> ?&Token {
    return null;
}

fn main() -> !void {
    return;
}

// check-fails
// features: std-array borrow escape
// error: cannot return a borrow optional
