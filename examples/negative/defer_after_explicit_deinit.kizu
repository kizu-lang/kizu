struct User {
    name: []u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::Arena<User>(allocator);
    defer users.deinit();
    users.deinit();
}

// check-fails
// features: defer arena deinit
// error: arena `users` was deinitialized
