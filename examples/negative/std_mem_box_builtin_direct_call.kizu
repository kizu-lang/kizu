struct Item {
    value: i64,
}

fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let value = Item { value: 1 };
    let item_result = std::builtin::box<Item>(allocator, value);
    let item = try item_result;
    print(item.borrow().value);
    return;
}
