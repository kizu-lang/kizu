fn main() -> void {
    // A declared type is a context, not a conversion: what the initializer
    // is has to be what was declared.
    let count: i64 = "seven";
    print(count);
    return;
}

// check-fails
// features: type-check diagnostics
// error: type error: `count` is declared i64, got []u8
