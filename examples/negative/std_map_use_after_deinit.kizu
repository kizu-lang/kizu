import std::map;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::Map<[]u8, i64>(allocator);
    table.deinit();
    print(table.len());
}

// check-fails
// features: std-map deinit move
// error: moved value
