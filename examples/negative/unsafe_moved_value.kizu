struct Name {
    value: []u8,
}

fn take(name: Name) {
    print(name.value);
}

fn main() {
    let name = Name { value: "alice" };
    take(name);
    @unsafe(ptr_read) {
        print(name.value);
    }
}
