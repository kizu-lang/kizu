fn load(io: Io, name: string) -> !i64 {
    print(name)
    return 1
}

fn main() -> !void {
    let io = Io()
    let group = TaskGroup()
    let name = "config"
    let task = group.spawn(io, load, name)
    print(name)
    let value = try task.await()
    print(value)
    return void
}
