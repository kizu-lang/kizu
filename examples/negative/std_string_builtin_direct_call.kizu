import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let text = std::builtin::string_new(allocator);
    print(text.len());
}

// check-fails
// features: std-string builtin-boundary type-check
// error: was removed
