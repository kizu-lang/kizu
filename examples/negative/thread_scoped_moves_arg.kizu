struct Job {
    id: i64,
}

fn worker(job: Job) -> Job {
    return job;
}

fn main() -> void {
    let io = std::io::blocking();
    let job = Job { id: 1 };
    let done = std::thread::scoped<Job, worker>(io, job);
    print(done.id);
    print(job.id);
}
