struct User {
    name: []const u8;
}

union Slot {
    Empty;
    UserHandle(handle<User>);
}

fn main() {
    let allocator = std::mem::page_allocator();
    let slots = std::array::Array<Slot>(allocator);
    print(slots.len());
}
