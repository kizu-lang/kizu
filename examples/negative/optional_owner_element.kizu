struct Big {
    pub x: i64,
}

fn pick() -> ?Big {
    return null;
}

fn main() -> !void {
    print(1);
    return;
}

// check-fails
// features: optional diagnostics
// error: optional element must be a scalar or enum, got `Big`
