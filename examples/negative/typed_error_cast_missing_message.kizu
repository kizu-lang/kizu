union CompileError {
    Diagnostic(i64);
}

fn lower() -> !i64 {
    return error("bad");
}

fn parse() -> CompileError!i64 {
    return try cast<CompileError!i64>(lower());
}

fn main() -> CompileError!void {
    let value = try parse();
    print(value);
    return;
}
