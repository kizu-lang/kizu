// A vector literal names every lane: f64x2 has two.
fn main() -> void {
    let v = f64x2 { 1.0, 2.0, 3.0 };
    print(cast<i64>(v[0]));
    return;
}

// check-fails
// features: vector simd
// error: has 2 lanes, got 3 values
