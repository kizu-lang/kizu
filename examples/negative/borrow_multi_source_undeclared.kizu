fn pick(a: []u8, b: []u8, c: []u8, take_first: bool) -> []u8 borrows a, b {
    if take_first {
        return a;
    }
    return c;
}

fn main() {}

// check-fails
// features: borrow-provenance multi-source
// error: returned value may be tied to `c`, which `borrows a, b` does not declare
