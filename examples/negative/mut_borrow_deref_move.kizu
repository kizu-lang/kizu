struct User {
    name: []const u8
}

fn clone_from_borrow(user: &mut User) -> User {
    return user.*
}

fn main() -> void {
    var user = User { name: "alice" }
    let cloned = clone_from_borrow(user)

    print(cloned.name)
}
