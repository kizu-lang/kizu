import std::fs;

fn main() -> !void {
    let text = try fs::read_file("examples/fixtures/config.txt", "unused");
    print(text);
    return;
}

// check-fails
// features: io fs type-check
// error: expects Io
