fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let symbols = std::map::Map<[]const u8, i64>(allocator);
    let channel = std::channel::Channel<std::map::Map<[]const u8, i64>>();

    try channel.send(symbols);
    return;
}
