struct Bytes {
    text: []u8,
}

struct File {
    name: []u8,
}

contract Writer {
    fn write(self: &Self, bytes: &Bytes) -> !i64;
}

impl Writer for File {
}

fn main() {}

// check-fails
// features: contract
// error: missing method `write`
