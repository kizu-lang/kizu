struct Name {
    value: []u8,
}

fn take(name: Name) {
    print(name.value);
}

fn main() {
    let name = Name { value: "alice" };
    take(name);
}

// check-fails
// features: move diagnostic
// error: `name` is handed off here; write `move name`
