import std::array;

struct Holder {
    pub items: ?array::Array<i64>,
}

fn main() -> !void {
    print(1);
    return;
}

// check-fails
// features: optional diagnostics
// error: struct field `Holder.items` cannot store an optional owner yet
