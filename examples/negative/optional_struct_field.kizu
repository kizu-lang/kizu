struct Holder {
    pub value: ?u8,
}

fn main() -> !void {
    print(1);
    return;
}

// check-fails
// features: optional diagnostics
// error: struct field `Holder.value` cannot store an optional yet
