fn worker(i: i64) -> []const u8 {
    return "bad";
}

fn main() -> void {
    let io = Io();
    let out = std::task::partition_mut(0, 2);
    std::task::parallel_map(io, out, 0, 2, worker);
    return void;
}
