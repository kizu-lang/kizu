import std::arena;

struct User {
    name: []u8,
}

fn show(users: arena::Arena<User>, user: arena::Handle<User>) {
    print(users.at(user).name);
}

fn main() {}

// check-fails
// features: arena handle
// error: arena `users` has unknown provenance
