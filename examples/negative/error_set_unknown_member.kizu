error FsError {
    NotFound,
}

fn read() -> FsError!i64 {
    return FsError::Denied;
}

fn main() -> FsError!void {
    print(try read());
    return;
}

// check-fails
// features: error-set type-check
// error: unknown error `FsError::Denied`
