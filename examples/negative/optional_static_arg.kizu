import std::array;
import std::mem;

fn main() -> !void {
    var xs = array::new<?u8>(mem::page_allocator());
    defer xs.deinit();
    print(1);
    return;
}

// check-fails
// features: optional diagnostics
// error: optional `?u8` cannot be a static argument yet
