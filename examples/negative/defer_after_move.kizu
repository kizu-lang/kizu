struct User {
    name: []u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::Arena<User>(allocator);
    defer users.deinit();
    let moved = users;
    print(moved);
}

// check-fails
// features: defer move deinit
// error: moved value
