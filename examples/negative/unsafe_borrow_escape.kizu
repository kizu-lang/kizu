@requires_unsafe() fn bad(s: &[]u8, p: ptr<const u8>) -> []u8 {
    @unsafe(ptr_read) {
        print(ptr_read(p));
        return s;
    }
}

fn main() {}

// check-fails
// features: unsafe borrow
// error: borrowed value `s` cannot escape
