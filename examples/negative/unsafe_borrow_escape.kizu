/// p は 1 バイト以上読める有効なメモリを指していなければならない。
unsafe fn bad(s: &[]u8, p: ptr<const u8>) -> []u8 {
    // SAFETY: p の有効性は呼び出し側が保証する
    print(unsafe ptr_read(p));
    return s;
}

fn main() {}

// check-fails
// features: unsafe borrow
// error: borrowed value `s` cannot escape
