import std::map;
import std::mem;
import std::string;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::new<[]u8, string::String>(allocator);
    print(table.len());
}

// check-fails
// features: std-map copy type-check
// error: value type must be copy
