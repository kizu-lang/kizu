fn main() {
    let allocator = std::mem::page_allocator();
    let table = std::map::Map<[]u8, std::string::String>(allocator);
    print(table.len());
}

// check-fails
// features: std-map copy type-check
// error: value type must be copy
