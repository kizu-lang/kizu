// A stack buffer holds fixed-width numbers. A view over it is a run of
// equal-sized cells, and `bool` is not what an index into one reads.
fn main() -> void {
    var flags = [4]bool{};
    return;
}

// check-fails
// features: stack_buffer typed_view
// error: type error: buffer element must be a fixed-width number, got bool
