import std::mem;

fn main() -> !void {
    let bytes = try mem::slice("abc", 0, 4);
    print(bytes);
    return;
}

// run-fails
// features: std-mem bounds error-union
// error: std::mem::Error::OutOfBounds
