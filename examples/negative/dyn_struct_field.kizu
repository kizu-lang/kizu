contract Writer {
    fn write() -> !i64;
}

struct Holder {
    writer: dyn Writer,
}

fn main() -> void {
    print(1);
}

// check-fails
// features: dyn contract struct field
// error: struct field `Holder.writer` cannot store dyn value
