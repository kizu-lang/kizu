fn send_name(ch: Channel<[]const u8>, name: &[]const u8) -> void {
    ch.send(name);
}

fn main() -> void {
    let ch = std::channel::Channel<[]const u8>();
    let name = "config";
    send_name(ch, name);
}
