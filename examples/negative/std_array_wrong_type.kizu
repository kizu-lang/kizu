fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let values = std::array::Array<i64>(allocator);

    try values.append("no");
    return;
}

// check-fails
// features: std-array type-check
// error: Array.append
