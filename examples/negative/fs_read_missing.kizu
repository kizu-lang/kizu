import std;

fn main() -> !void {
    let io = std::io::blocking();
    let text = try std::fs::read_file(io, "examples/fixtures/missing.txt");
    print(text);
    return;
}

// run-fails
// features: io fs error-union
// error: std::fs::Error::NotFound
