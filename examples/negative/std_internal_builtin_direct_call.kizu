import std::mem;

fn main() -> !void {
    let allocator = mem::page_allocator();
    print(std::internal::builtin::mem_len("abc"));
    return;
}

// check-fails
// features: std-mem visibility builtin-boundary
// error: `std::internal::builtin` is internal to `std`
