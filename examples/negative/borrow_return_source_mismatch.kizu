fn bad(left: []u8, right: []u8) -> []u8 borrows left {
    return right;
}

fn main() {}

// check-fails
// features: borrow-provenance borrow-check
// error: returned value may be tied to `right`, which `borrows left` does not declare
