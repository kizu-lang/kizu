fn bad(left: []u8, right: []u8) -> []u8 borrows left {
    return right;
}

fn main() {}

// check-fails
// features: borrow-provenance borrow-check
// error: return borrows `left` but returned value is not tied to that source
