import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    try text.append_bytes(allocator, 1);
    return;
}

// check-fails
// features: std-string type-check
// error: expects []u8
