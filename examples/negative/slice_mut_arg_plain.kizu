fn fill(buf: &var []u8) -> void {
    buf[0] = 65;
    return;
}

fn main() -> !void {
    var bytes = "AB";
    fill(bytes);
    print(bytes);
    return;
}

// check-fails
// features: mutable-slice-view
// error: `&var []u8` argument must be a writable view binding
