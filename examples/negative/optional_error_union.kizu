fn pick() -> ?!u8 {
    return null;
}

fn main() -> !void {
    print(1);
    return;
}

// check-fails
// features: optional error-union diagnostics
// error: optional cannot wrap an error union `!u8`; spell it `E!?T`
