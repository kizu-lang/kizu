fn pick() -> ?!u8 {
    return null;
}

fn main() -> !void {
    print(1);
    return;
}

// check-fails
// features: optional error-union diagnostics
// error: optional element must be a scalar or enum, got `!u8`
