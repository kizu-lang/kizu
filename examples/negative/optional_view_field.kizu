struct Holder {
    pub bytes: ?[]u8,
}

fn main() -> !void {
    print(1);
    return;
}

// check-fails
// features: optional diagnostics
// error: struct field `Holder.bytes` cannot store an optional owner yet
