fn load(io: Io, locked: Mutex<i64>) -> void {
    print(locked.get());
}

fn main() -> void {
    let io = std::io::blocking();
    let locked = std::sync::Mutex<i64>(1);
    let group = std::task::Group(io);
    let task = group.spawn(load, locked);
    task.cancel();
}
