fn load(io: Io, name: []u8) -> !i64 {
    print(name);
    return 1;
}

fn main() -> !void {
    let io = std::io::blocking();
    let group = std::task::Group(io);
    let task = group.spawn(io, load, "config");
    let value = try task.await();
    print(value);
    return;
}
