import std::mem;
import std::string;

fn main() {
    let allocator = mem::page_allocator();
    var text = string::String(allocator);
    text.deinit();
    print(text.len());
}

// check-fails
// features: std-string deinit move
// error: moved value
