union Shape {
    Point,
    Circle(i64),
}

fn main() -> void {
    let shape = Shape.Circle(10);
    print(shape);
}

// check-fails
// features: union namespace
// error: union variant `Shape.Circle` must use `::`
