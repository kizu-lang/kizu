fn read(locked: Mutex<i64>) -> Mutex<i64> {
    return locked;
}

fn main() -> void {
    let io = std::io::blocking();
    let locked = std::sync::Mutex<i64>(1);
    print(std::thread::scoped<Mutex<i64>>(io, read, locked));
}
