fn read(locked: Mutex<i64>) -> i64 {
    return locked.get();
}

fn main() -> void {
    let io = std::io::blocking();
    let locked = std::sync::Mutex<i64>(1);
    print(std::thread::scoped(io, read, locked));
}
