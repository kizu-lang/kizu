fn read_byte(p: ptr<const u8>) -> u8 {
    @unsafe(ptr_read, ptr_write) {
        return ptr_read(p);
    }
}

fn main() {}

// check-fails
// features: unsafe raw-pointer
// error: capability `ptr_write` is declared but never used
