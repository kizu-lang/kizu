fn main() -> !void {
    let allocator = std::mem::page_allocator();
    var text = std::string::String(allocator);
    try text.append_bytes("owned");
    let view = text.as_bytes();
    let ch = std::channel::Channel<[]u8>();
    ch.send(view);
    return;
}
