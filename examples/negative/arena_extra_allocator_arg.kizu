import std::arena;
import std::mem;

struct User {
    name: []u8,
}

fn main() {
    let allocator = mem::page_allocator();
    let users = arena::Arena<User>(allocator, allocator);
    print(users);
}

// check-fails
// features: arena allocator
// error: expects exactly one allocator argument
