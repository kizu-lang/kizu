import std::arena;
import std::mem;

struct UserArena {}

struct User {
    name: []u8,
}

fn main() {
    let allocator = mem::page_allocator();
    let users = arena::new<User, UserArena>(allocator, allocator);
    print(users);
}

// check-fails
// features: arena allocator
// error: `std::arena::new` expects allocator
