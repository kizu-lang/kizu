fn main() {
    let allocator = std::mem::page_allocator();
    let values = std::array::Array<ptr<u8>>(allocator);
    print(values.len());
}

// check-fails
// features: std-array raw-pointer
// error: raw pointer
