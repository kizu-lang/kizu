struct User {
    name: []const u8;
}

fn use(left: &User, right: &mut User) -> void {
    print(left.name);
    print(right.name);
}

fn main() {
    var user = User { name: "alice" };
    use(user, user);
}
