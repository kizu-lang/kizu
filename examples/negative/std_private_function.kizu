import std::mem;
import std::array;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var values = array::Array<i64>(allocator);
    print(array::len<i64>(values));
    values.deinit();
    return;
}

// check-fails
// features: std-array visibility
// error: function `array::len` is private
