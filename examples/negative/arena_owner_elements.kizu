import std::arena;
import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    let names = arena::new<string::String>(allocator);
    defer names.deinit();
    return;
}

// check-fails
// features: arena ownership deinit
// error: Arena cannot hold owner values; `std::string::String` needs a deinit Arena never runs
