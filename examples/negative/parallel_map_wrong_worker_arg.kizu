fn worker(flag: bool) -> i64 {
    if flag {
        return 1;
    }
    return 0;
}

fn main() -> void {
    let io = std::io::blocking();
    var out = std::task::partition_mut(0, 2);
    std::task::parallel_map(io, out, 0, 2, worker);
}
