struct Token {
    text: []const u8;
}

fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let tokens = std::array::Array<Token>(allocator);
    var counter = 0;

    let slot = try tokens.at_mut(counter);
    print(slot.text);
    return void;
}
