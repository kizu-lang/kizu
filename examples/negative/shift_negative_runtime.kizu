fn shift(value: i64, amount: i64) -> i64 {
    return value << amount;
}

fn main() {
    print(shift(1, 3));
    print(shift(1, -1));
    return;
}

// run-fails
// features: shift trap
// error: negative shift amount
