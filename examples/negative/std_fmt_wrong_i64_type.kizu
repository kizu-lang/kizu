fn main() -> !void {
    let allocator = std::mem::page_allocator();
    var text = std::string::String(allocator);
    try std::fmt::append_i64(text, "1");
    return;
}

// check-fails
// features: std-fmt type-check
// error: expects i64
