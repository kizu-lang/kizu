import std::fmt;
import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    try fmt::append_i64(allocator, text, "1");
    return;
}

// check-fails
// features: std-fmt type-check
// error: expects i64
