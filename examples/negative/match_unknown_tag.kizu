enum Color {
    Red,
    Green,
}

fn main() -> void {
    let color = Color::Red;
    match color {
        Red => print("red"),
        Blue => print("blue"),
    }
}

// check-fails
// features: match enum
// error: unknown match tag `Color::Blue`
