struct Point {
    x: i64,
    y: i64,
}

fn main() -> void {
    let point = Point { x: 1, y: 2 };
    print(point);
    return;
}

// check-fails
// features: print struct diagnostic
// error: `std::fmt::print` does not support type `Point`
