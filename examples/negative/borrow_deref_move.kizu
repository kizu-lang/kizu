struct User {
    name: []u8,
}

fn clone_from_borrow(user: &User) -> User {
    return user.*;
}

fn main() -> void {
    let user = User { name: "alice" };
    let cloned = clone_from_borrow(user);
    print(cloned.name);
}
