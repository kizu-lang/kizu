fn view(text: &std::string::String) -> []const u8 {
    return text.as_bytes();
}

fn main() {
    let allocator = std::mem::page_allocator();
    var text = std::string::String(allocator);
    let bytes = view(text);
    print(bytes);
}
