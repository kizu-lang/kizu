fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let values = std::array::Array<i64>(allocator);

    values.deinit();
    print(values.len());
    return;
}

// check-fails
// features: std-array deinit move
// error: moved value
