import std::array;
import std::mem;

struct Token {
    text: []u8,
}

fn main() -> !void {
    let allocator = mem::page_allocator();
    var tokens = array::Array<Token>(allocator);

    try tokens.append(Token { text: "let" });
    let first = try tokens.at(0);
    try tokens.append(Token { text: "var" });
    print(first.text);
    return;
}

// check-fails
// features: std-array borrow reallocation-safety
// error: cannot run while array is borrowed
