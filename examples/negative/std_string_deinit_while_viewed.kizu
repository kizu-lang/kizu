import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    defer text.deinit();
    try text.append_bytes("a");
    let bytes = text.as_bytes();
    text.deinit();
    print(bytes);
    return;
}

// check-fails
// features: std-string borrow deinit
// error: cannot run while string is borrowed
