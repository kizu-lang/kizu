struct User {
    name: []const u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let users = arena<User>(allocator);
    let borrowed = &users;
    defer users.deinit();
    while false {
        print(borrowed);
    }
}
