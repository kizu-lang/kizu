import std::array;
import std::mem;

struct User {
    name: []u8,
}

fn main() {
    let allocator = mem::page_allocator();
    let users = array::new<User>(allocator);
    let borrowed = &users;
    defer users.deinit(allocator);
    while false {
        print(borrowed.len());
    }
}

// check-fails
// features: defer borrow deinit
// error: cannot run while array is borrowed
