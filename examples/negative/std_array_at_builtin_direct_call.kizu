import std::array;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let values = array::Array<i64>(allocator);
    std::internal::builtin::array_at<i64>(values, 0);
}

// check-fails
// features: std-array builtin-boundary
// error: reserved
