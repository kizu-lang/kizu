struct File {
    name: []u8,
}

contract Writer {
    fn write() -> !i64;
}

fn save(writer: dyn Writer) -> !void {
    return;
}

fn main() {}

// check-fails
// features: dyn
// error: dyn parameter `writer` must be borrowed
