import std::map;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::Map<[]u8, i64>(allocator);
    std::internal::builtin::map_deinit<[]u8, i64>(table);
}

// check-fails
// features: std-map builtin-boundary
// error: reserved
