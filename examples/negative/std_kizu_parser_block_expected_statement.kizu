fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let source = std::kizu::ast::source_file(
        "bad-block.kizu",
        "fn main() { return x; y }"
    );
    let result = try std::kizu::parser::parse_first_node(allocator, source);
    print(result.root);
    return;
}
