struct User {
    name: []u8,
}

fn make() -> std::arena::Arena<User> {
    let allocator = std::mem::page_allocator();
    return std::arena::Arena<User>(allocator);
}

fn main() {
    make().deinit();
}
