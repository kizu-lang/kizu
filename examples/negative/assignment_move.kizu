struct Name {
    value: []u8,
}

fn main() {
    let a = Name { value: "hello" };
    let b = move a;
    print(a.value);
    print(b.value);
}

// check-fails
// features: move
// error: moved value `a` was used
