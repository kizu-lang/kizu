import std::mem;

fn main() -> void {
    let allocator = mem::page_allocator();
    let values = std::builtin::array<i64>(allocator);
    print(values.len());
}

// check-fails
// features: array builtin-boundary
// error: reserved; use std::array
