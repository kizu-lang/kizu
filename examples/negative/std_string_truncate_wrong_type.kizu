import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::String(allocator);
    try text.append_bytes("lexer");
    try text.truncate("bad");
    return;
}

// check-fails
// features: std-string type-check
// error: expects i64
