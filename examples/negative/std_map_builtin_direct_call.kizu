import std::mem;

fn main() -> void {
    let allocator = mem::page_allocator();
    let table = std::internal::builtin::map<[]u8, i64>(allocator);
    print(table.len());
}

// check-fails
// features: map builtin-boundary
// error: reserved; use std::map
