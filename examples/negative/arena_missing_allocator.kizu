import std::arena;

struct UserArena {}

struct User {
    name: []u8,
}

fn main() {
    let users = arena::new<User, UserArena>();
    print(users);
}

// check-fails
// features: arena allocator
// error: `std::arena::new` expects allocator
