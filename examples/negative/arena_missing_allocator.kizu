import std::arena;

struct User {
    name: []u8,
}

fn main() {
    let users = arena::Arena<User>();
    print(users);
}

// check-fails
// features: arena allocator
// error: expects exactly one allocator argument
