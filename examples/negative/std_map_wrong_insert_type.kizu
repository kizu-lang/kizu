fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let table = std::map::Map<[]u8, i64>(allocator);

    try table.insert("main", "function");

    table.deinit();
    return;
}

// check-fails
// features: std-map type-check
// error: Map.insert
