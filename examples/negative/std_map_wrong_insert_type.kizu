import std::map;
import std::mem;

fn main() -> !void {
    let allocator = mem::page_allocator();
    let table = map::new<[]u8, i64>(allocator);

    try table.insert(allocator, "main", "function");

    table.deinit(allocator);
    return;
}

// check-fails
// features: std-map type-check
// error: Map.insert
