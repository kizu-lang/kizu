import std::map;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::Map<[]u8, i64>(allocator);
    print(std::builtin::map_contains<[]u8, i64>(table, "answer"));
}

// check-fails
// features: std-map builtin-boundary
// error: reserved
