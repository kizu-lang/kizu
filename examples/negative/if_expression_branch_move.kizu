struct Name {
    value: []const u8;
}

fn main() -> void {
    let name = Name { value: "alice" };
    let chosen = if true {
        name;
    } else {
        Name { value: "bob" };
    };

    print(chosen.value);
    print(name.value);
}
