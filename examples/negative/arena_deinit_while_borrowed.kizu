struct User {
    name: []const u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let users = arena<User>(allocator);
    let borrowed = &users;
    users.deinit();
    print(borrowed);
}
