// A comparison of vectors would be a mask, which the language does not have
// yet; two vectors compare lane by lane through their lanes.
fn main() -> void {
    let v = f64x2 { 1.0, 2.0 };
    if v == v {
        print(1);
    }
    return;
}

// check-fails
// features: vector simd
// error: operator `==` is not defined on a vector
