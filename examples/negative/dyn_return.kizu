contract Writer {
    fn write(self: &Self) -> !i64;
}

fn pick() -> dyn Writer {
    return 1;
}

fn main() -> void {
    print(1);
}

// check-fails
// features: dyn contract return
// error: function `pick` cannot return dyn
