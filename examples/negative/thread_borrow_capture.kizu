fn worker(name: &[]const u8) -> []const u8 {
    return "blocked";
}

fn main() -> void {
    let io = std::io::blocking();
    let name = "config";
    std::thread::scoped<[]const u8>(io, worker, name);
}
