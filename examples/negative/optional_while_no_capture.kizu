fn pick() -> ?u8 {
    return null;
}

fn main() -> !void {
    while pick() {
        print(1);
    }
    return;
}

// check-fails
// features: optional while-capture diagnostics
// error: while condition must be bool, got ?u8
