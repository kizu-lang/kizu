struct Token {
    value: i64,
}

fn (self: Token) reset() -> void = fields;

fn main() -> !void {
    return;
}

// check-fails
// features: ownership fields-deinit
// error: `= fields` is only for `deinit() -> void`
