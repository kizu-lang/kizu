import std::map;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    let table = map::Map<[]u8, i64>(allocator);
    std::internal::builtin::map_insert<[]u8, i64>(table, "answer", 42);
}

// check-fails
// features: std-map builtin-boundary
// error: reserved
