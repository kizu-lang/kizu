fn main() {
    let allocator = std::mem::page_allocator();
    let table = std::map::Map<[]u8, i64>(allocator);
    std::builtin::map_insert<[]u8, i64>(table, "answer", 42);
}

// check-fails
// features: std-map builtin-boundary
// error: reserved
