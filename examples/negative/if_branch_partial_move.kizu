struct Name {
    value: []const u8;
}

fn take(name: Name) -> void {
    print(name.value);
}

fn main() -> void {
    let name = Name { value: "alice" };
    if false {
        take(name);
    } else {
        print("kept");
    }
    print(name.value);
}
