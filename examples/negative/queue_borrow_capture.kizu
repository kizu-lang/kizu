fn job(io: Io, name: &[]u8) -> void {
    print(name);
}

fn main() -> void {
    let io = std::io::blocking();
    let queue = std::task::Queue();
    let name = "config";
    queue.enqueue(io, job, name);
    queue.drain();
}
