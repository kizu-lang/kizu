fn job(io: Io, name: &[]const u8) -> void {
    print(name);
}

fn main() -> void {
    let io = Io();
    let queue = std::task::Queue();
    let name = "config";
    queue.enqueue(io, job, name);
    queue.drain();
}
