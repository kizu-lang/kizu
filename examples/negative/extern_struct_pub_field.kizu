// The fields of an extern struct are visible wherever the struct is, because
// C already names them; `pub` on one says nothing.
extern "c" struct Point {
    pub x: i64,
}

pub fn main() -> void {
    return;
}

// parse-fails
// features: extern-struct diagnostic
// error: extern struct field `Point.x` is visible already; drop `pub`
