import std::mem;
import std::string;

fn main() -> !void {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    try text.reserve("more");
    return;
}

// check-fails
// features: std-string type-check
// error: expects i64
