struct User {
    name: []u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let users = std::arena::Arena<User>(allocator);
    let borrowed = &users;
    borrowed.deinit();
}

// check-fails
// features: arena borrow deinit
// error: requires owned arena receiver
