fn worker(i: i64) -> i64 {
    return i;
}

fn main() -> void {
    let io = std::io::blocking();
    let parts = std::task::partition_mut(0, 1);
    std::builtin::task_parallel_map<worker>(io, parts, 0, 1);
}
