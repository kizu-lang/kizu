struct File {
    name: []u8,
}

impl Nowhere for File {
    fn write(self: &Self) -> !i64 {
        return 1;
    }
}

fn main() -> void {
    print(1);
}

// check-fails
// features: contract impl
// error: unknown contract `Nowhere`
