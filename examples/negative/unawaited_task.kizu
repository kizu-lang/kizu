fn load(io: Io, name: string) -> result<int> {
    print(name)
    return ok(1)
}

fn main() -> result<void> {
    let io = Io()
    let group = TaskGroup()
    let task = group.spawn(io, load, "config")
    return ok(void)
}
