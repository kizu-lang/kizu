fn load(io: Io, name: string) -> !int {
    print(name)
    return 1
}

fn main() -> !void {
    let io = Io()
    let group = TaskGroup()
    let task = group.spawn(io, load, "config")
    return void
}
