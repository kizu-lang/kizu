struct Name {
    value: []u8,
}

fn take(name: Name) {
    print(name.value);
}

fn main() {
    let name = Name { value: "alice" };
    take(name);
    take(name);
}

// check-fails
// features: move
// error: moved value `name` was used
