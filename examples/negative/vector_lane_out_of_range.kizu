// A lane index is a literal, so a lane that does not exist is refused here
// rather than trapped at run time.
fn main() -> void {
    let v = f64x2 { 1.0, 2.0 };
    print(cast<i64>(v[2]));
    return;
}

// check-fails
// features: vector simd
// error: lane 2 is outside `f64x2`
