import std::array;
import std::mem;

fn main() {
    let allocator = mem::page_allocator();
    var values = array::Array<i64>(allocator);
    std::builtin::array_at_mut<i64>(values, 0);
}

// check-fails
// features: std-array builtin-boundary
// error: reserved
