extern "c" fn source() -> ptr<i64>

fn job(io: Io, p: ptr<i64>) -> void {
    print(p);
}

fn main() -> void {
    let io = Io();
    let queue = std::task::Queue();
    unsafe {
        let p = source();
        queue.enqueue(io, job, p);
    }
    queue.drain();
}
