fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let source = std::kizu::ast::source_file("bad.kizu", "add(1");
    let parsed = try std::kizu::parser::parse_first_node(allocator, source);
    print(parsed.root);
    return;
}
