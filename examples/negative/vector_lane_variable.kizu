// A lane is named by a literal: a vector is not indexed by a value.
fn main() -> void {
    let v = f64x2 { 1.0, 2.0 };
    let i = 1;
    print(cast<i64>(v[i]));
    return;
}

// check-fails
// features: vector simd
// error: a vector lane is named by an integer literal
