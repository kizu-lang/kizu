import std::mem;
import std::string;

fn main() {
    let allocator = mem::page_allocator();
    var text = string::new(allocator);
    print(text.bytes.len());
}

// check-fails
// features: std-string visibility type-check
// error: is private
