fn load(io: Io, name: string) -> result<int> {
    print(name)
    return ok(42)
}

fn main() -> result<void> {
    let io = Io()
    let group = TaskGroup()
    let task = group.spawn(io, load, "config")
    let value = try task.await()
    print(value)
    return ok(void)
}
