fn load(io: Io, name: string) -> !int {
    print(name)
    return 42
}

fn main() -> !void {
    let io = Io()
    let group = TaskGroup()
    let task = group.spawn(io, load, "config")
    let value = try task.await()
    print(value)
    return void
}
