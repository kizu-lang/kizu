fn load(io: Io, name: []const u8) -> !i64 {
    print(name)
    return 42
}

fn main() -> !void {
    let io = Io()
    let group = std.task.Group()
    let task = group.spawn(io, load, "config")
    let value = try task.await()
    print(value)
    return void
}
