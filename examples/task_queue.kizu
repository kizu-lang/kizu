fn job(io: Io, value: i64) -> void {
    print(value)
}

fn main() -> void {
    let io = Io()
    let queue = std.task.Queue()

    queue.enqueue(io, job, 1)
    queue.enqueue(io, job, 2)
    queue.drain()
}
