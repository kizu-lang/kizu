fn main() -> !void {
    let age = 30;
    let admin = false;

    try std::testing::expect(age >= 20 and age < 130);
    try std::testing::expect(age < 20 or age >= 30);
    try std::testing::expect((age < 20 and admin) or age == 30);
    return;
}
