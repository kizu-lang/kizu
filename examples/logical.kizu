fn main() -> !void {
    let age = 30;
    let admin = false;

    std::testing::expect(age >= 20 and age < 130);
    std::testing::expect(age < 20 or age >= 30);
    std::testing::expect((age < 20 and admin) or age == 30);
    return;
}
