fn main() -> !void {
    let age = 30;
    let admin = false;

    try std::testing::expect(age >= 20 && age < 130);
    try std::testing::expect(age < 20 || age >= 30);
    try std::testing::expect((age < 20 && admin) || age == 30);
    return void;
}
