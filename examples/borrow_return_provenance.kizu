fn first_byte(bytes: []const u8) -> []const u8 borrows bytes {
    return bytes[0..1];
}

fn main() -> !void {
    let first = first_byte("abc");
    try std::testing::expect_equal_bytes(first, "a");
    return;
}
