fn first_byte(bytes: []const u8) -> []const u8 borrows bytes {
    return bytes[0..1];
}

fn main() -> !void {
    let first = first_byte("abc");
    std::testing::expect_equal<[]const u8>(first, "a");
    return;
}
