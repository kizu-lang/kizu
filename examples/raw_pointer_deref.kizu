struct Node {
    tag: i64,
    name: []u8,
}

fn read_tag(node: ptr<const Node>) -> i64 {
    @unsafe(ptr_deref) {
        return node.*.tag;
    }
}

fn write_tag(node: ptr<Node>, tag: i64) -> void {
    @unsafe(ptr_deref) {
        node.*.tag = tag;
        return;
    }
}

fn replace(node: ptr<Node>, value: Node) -> void {
    @unsafe(ptr_deref) {
        node.* = value;
        return;
    }
}

fn main() -> void {}

// check
// features: unsafe raw-pointer deref field-access
