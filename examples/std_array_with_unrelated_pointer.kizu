extern "c" fn source() -> ptr<u8>

fn main() -> !void {
    let allocator = std::mem::page_allocator();
    let values = std::array::Array<i64>(allocator);

    unsafe {
        let p = source();
        print(ptr_read(p));
    }

    print(values.len());
    return void;
}
