fn main() {
    let source = std::path::join("examples", "fixtures/config.txt");
    print(source);
    print(std::path::dirname(source));
    print(std::path::basename(source));
    print(std::path::extension(source));
    print(std::path::clean("examples/fixtures/../fixtures/config.txt"));
}
