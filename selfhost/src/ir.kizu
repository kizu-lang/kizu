import selfhost::ownership;

pub struct IrSummary {
    pub blocks: i64;
}

pub fn lower(owned: ownership::OwnershipSummary) -> IrSummary {
    return IrSummary {
        blocks: owned.checked_nodes
    };
}
