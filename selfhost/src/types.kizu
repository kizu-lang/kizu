import selfhost::resolver;
import selfhost::source;
import selfhost::types::checker;
import selfhost::types::model;

pub fn check(resolved: resolver::ResolveSummary) -> model::TypeSummary {
    return checker::check(resolved);
}

pub fn check_sources(
    allocator: Allocator,
    files: &std::array::Array<source::SourceFile>,
    resolved: resolver::ResolveSummary
) -> !model::TypeSummary {
    return checker::check_sources(allocator, files, resolved);
}

pub fn first_function_call_error(
    allocator: Allocator,
    files: &std::array::Array<source::SourceFile>,
    target_path: []u8
) -> !model::FunctionCallSummary {
    return checker::first_function_call_error(allocator, files, target_path);
}

pub fn source_has_package_function_call(
    allocator: Allocator,
    path: []u8,
    text: []u8
) -> !bool {
    return checker::source_has_package_function_call(allocator, path, text);
}

pub fn first_pre_move_check_diagnostic(
    allocator: Allocator,
    path: []u8,
    text: []u8
) -> !model::CheckDiagnosticSummary {
    return checker::first_pre_move_check_diagnostic(allocator, path, text);
}

pub fn first_post_move_check_diagnostic(
    allocator: Allocator,
    path: []u8,
    text: []u8
) -> !model::CheckDiagnosticSummary {
    return checker::first_post_move_check_diagnostic(allocator, path, text);
}
