// void_call_return -- `return g(x);` where g returns void, and a void call statement.
// site: append_call_return_value / call_produces_value.
// history: a5d61350 emitted a module LLVM rejects (instructions returning void
// cannot have a name). Closed at 34f0d258.
// probe-calls: probe_void_call_return(5)

pub fn touch(a: i64) -> void {
    return;
}

pub fn relay(a: i64) -> void {
    return touch(a);
}

pub fn probe_void_call_return(a: i64) -> i64 {
    relay(a);
    return a * 2;
}

fn main() -> void {
    print(probe_void_call_return(5));
    return;
}
