// structlit_call_argument -- a struct literal passed straight as a call argument, with
// no let binding to name it.
// site: the call argument position; the call used to name a value nothing defined.
// history: 225aec97 emitted a module LLVM rejects (use of undefined value '%t0').
// Closed at a8d6ffbd.
// probe-calls: probe_structlit_call_argument(3)

pub struct Pair {
    pub left: i64,
    pub right: i64,
}

pub fn weigh(p: Pair) -> i64 {
    return p.left * 10 + p.right;
}

pub fn probe_structlit_call_argument(a: i64) -> i64 {
    return weigh(Pair { left: a, right: a + 1 });
}

fn main() -> void {
    print(probe_structlit_call_argument(3));
    return;
}
