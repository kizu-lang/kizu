enum Color {
    Red,
    Green,
    Blue,
}

fn main() -> !void {
    var palette = std::array::Array<Color>(std::mem::page_allocator());
    try palette.append(Color::Red);
    try palette.append(Color::Green);
    try palette.append(Color::Blue);
    print(palette.len());
    palette.deinit();
    return;
}
