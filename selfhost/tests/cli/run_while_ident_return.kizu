fn first_over(limit: i64) -> i64 {
    var i = 0;
    while i < limit {
        if i > 2 {
            return i;
        }
        i = i + 1;
    }
    return 0 - 1;
}

fn main() {
    print(first_over(5));
}
