fn main() -> !void {
    var items = std::array::Array<i64>(std::mem::page_allocator());
    try items.append(10);
    try items.append(32);
    let first = try items.get(0);
    let second = try items.get(1);
    print(first + second);
    return;
}
