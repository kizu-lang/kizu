fn make() -> !std::string::String {
    let a = std::mem::page_allocator();
    var s = std::string::String(a);
    try s.append_bytes("hi");
    return s;
}
fn main() -> !void {
    var s = try make();
    let b = s.as_bytes();
    print(b);
    s.deinit();
    return;
}
