fn bump(n: &var i64) {
    n.* = n.* + 1;
}

fn add(n: &var i64, k: i64) {
    n.* = n.* + k;
}

fn main() {
    var x = 41;
    bump(&var x);
    print(x);
    add(&var x, 100);
    print(x);
    bump(x);
    print(x);
}
