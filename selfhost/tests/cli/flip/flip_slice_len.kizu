fn slen(s: []u8) -> i64 {
    return std::mem::len(s);
}

fn main() {
    let b = "hello";
    print(std::mem::len(b));
    print(slen(b));
    print(std::mem::len("kizu"));
    print(std::mem::len(""));
}
