fn main() -> !void {
    var s = std::string::String(std::mem::page_allocator());
    print(s.capacity());
    try s.append_byte(65);
    print(s.capacity());
    try s.append_byte(66);
    print(s.capacity() >= s.len());
    print(s.capacity() < s.len());
    return;
}
