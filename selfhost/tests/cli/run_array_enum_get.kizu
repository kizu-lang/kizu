enum TokenKind {
    Ident,
    Number,
}

fn main() -> !void {
    var tokens = std::array::Array<TokenKind>(std::mem::page_allocator());
    try tokens.append(TokenKind::Number);
    let v = try tokens.get(0);
    print(v);
    tokens.deinit();
    return;
}
