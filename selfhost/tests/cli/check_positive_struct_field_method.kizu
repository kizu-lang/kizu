struct Counter { value: i64 }

impl Counter {
    fn bump(self: Counter, amount: i64) -> i64 {
        return self.value + amount;
    }
}

fn main() {
    let counter = Counter { value: 40 };
    print(counter.bump(2));
}
