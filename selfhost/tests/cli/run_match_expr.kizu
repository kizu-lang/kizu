enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let color = Color::Blue;
    let name = match color {
        Red => "red",
        _ => "other",
    };
    print(name);
    let rank = match color {
        Red => 1,
        Green => 2,
        Blue => 3,
    };
    print(rank);
}
