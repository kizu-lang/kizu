struct Point {
    x: i64,
    y: i64,
}

struct Box {
    name: []u8,
    point: Point,
}

fn main() {
    var box = Box { name: "old", point: Point { x: 1, y: 2 } };
    let next = Point { x: 11, y: 12 };
    box.point = next;
    print(box.name);
    print(box.point.x + box.point.y);
}
