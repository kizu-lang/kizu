fn main() -> !void {
    var flags = std::array::Array<bool>(std::mem::page_allocator());
    try flags.append(true);
    try flags.append(false);
    if try flags.get(0) {
        print("first-on");
    } else {
        print("first-off");
    }
    return;
}
