struct Name {
    value: []u8,
}

fn take(name: Name) {
    print(name.value);
}

fn main() {
    comptime if true {
        print(1);
    } else {
        let missing_value = nosuchvar;
        let name = Name { value: "dead" };
        take(name);
        print(name.value);
    }
}
