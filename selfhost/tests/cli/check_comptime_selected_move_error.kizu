struct Name {
    value: []u8,
}

fn take(name: Name) {
    print(name.value);
}

fn main() {
    comptime if false {
        print(1);
    } else {
        let name = Name { value: "selected" };
        take(name);
        print(name.value);
    }
}
