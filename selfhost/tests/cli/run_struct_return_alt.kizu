struct Packet {
    ok: bool,
    name: []u8,
}

fn build(code: i64, name: []u8) -> Packet {
    return Packet { name: name, ok: code == 3 };
}

fn main() {
    let second = build(3, "skip");
    print(second.ok);
    let third = build(5, "ready");
    print(third.name);
}
