struct Pair {
    left: i64,
    right: i64,
}

fn make(a: i64, b: i64) -> Pair {
    return Pair { left: a, right: b };
}

fn main() {
    let pair = make(19, 23);
    print(pair.left + pair.right);
}
