struct Adder {
    base: i64,
}

impl Adder {
    fn plus(self: Adder, x: i64) -> i64 {
        return self.base + x;
    }
}

fn main() {
    let a = Adder { base: 10 };
    print(a.plus(5) + a.plus(2));
}
