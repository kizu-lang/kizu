struct Trace {
    label: []u8,
}

impl Trace {
    fn deinit(self: Trace) -> void {
        print(self.label);
    }
}

fn run() -> i64 {
    let only = Trace { label: "only" };
    defer only.deinit();
    return 1;
}

fn main() {
    print(run());
}
