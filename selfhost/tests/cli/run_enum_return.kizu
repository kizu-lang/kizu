enum Grade {
    Pass,
    Fail,
}

fn judge(points: i64) -> Grade {
    if points >= 60 {
        return Grade::Pass;
    }
    return Grade::Fail;
}

fn main() {
    let grade = judge(80);
    match grade {
        Pass => print("pass"),
        Fail => print("fail"),
    }
}
