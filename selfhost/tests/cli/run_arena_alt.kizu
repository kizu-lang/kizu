struct Item {
    label: []u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let items = std::arena::Arena<Item>(allocator);
    let found = items.add(Item { label: "bravo" });
    print(items.get(found).label);
    items.deinit();
}
