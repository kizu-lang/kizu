fn main() -> !void {
    let io = std::io::blocking();
    try std::io::write_stdout(io, "stdout first");
    try std::io::write_stderr(io, "stderr only");
    let bytes = try std::fs::read_file(io, "selfhost/tests/cli/fixtures/fs_alt.txt");
    try std::io::write_stdout(io, bytes);
    return;
}
