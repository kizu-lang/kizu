fn main() -> !void {
    var words = std::array::Array<[]u8>(std::mem::page_allocator());
    let first = "red";
    let second = "green";
    try words.append(first);
    try words.append(second);
    try words.append("blue");
    print(words.get_or_panic(2));
    return;
}
