fn main() -> !void {
    let allocator = std::mem::page_allocator();
    var text = std::string::String(allocator);
    try text.append_bytes("hi");
    text.clear();
    print(text.len());
    text.deinit();
    return;
}
