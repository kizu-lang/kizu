struct Gauge {
    value: i64,
}

impl Gauge {
    fn over(self: Gauge, limit: i64) -> bool {
        return self.value > limit;
    }
}

fn main() {
    let gauge = Gauge { value: 20 };
    print(gauge.over(10));
    print(gauge.over(50));
}
