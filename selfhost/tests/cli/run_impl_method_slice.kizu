struct Label {
    name: []u8,
}

impl Label {
    fn text(self: Label) -> []u8 {
        return self.name;
    }
}

fn main() {
    let label = Label { name: "green" };
    print(label.text());
    let color = label.text();
    print(color);
}
