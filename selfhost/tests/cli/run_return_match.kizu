enum Mode {
    Fast,
    Slow,
}

fn pick(m: Mode) -> i64 {
    return match m {
        Fast => 10,
        Slow => 20,
    };
}

fn main() {
    print(pick(Mode::Fast));
    print(pick(Mode::Slow));
}
