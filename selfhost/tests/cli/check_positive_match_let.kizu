enum Mode { Fast, Slow }

fn pick() -> Mode {
    return Mode::Fast;
}

fn main() {
    let mode = pick();
    let label = match mode {
        Fast => "fast",
        Slow => "slow",
    };
    print(label);
}
