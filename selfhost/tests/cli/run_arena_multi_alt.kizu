struct Entry {
    active: bool,
    id: i64,
    label: []u8,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let entries = std::arena::Arena<Entry>(allocator);
    print("entry arena");
    entries.deinit();
}
