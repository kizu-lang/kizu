struct Point {
    id: i64,
    active: bool,
}

fn main() {
    let allocator = std::mem::page_allocator();
    let points = std::arena::Arena<Point>(allocator);
    print("point arena");
    points.deinit();
}
