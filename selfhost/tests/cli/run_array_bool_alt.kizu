fn main() -> !void {
    var switches = std::array::Array<bool>(std::mem::page_allocator());
    try switches.append(false);
    try switches.append(true);
    if switches.get_or_panic(1) {
        print("panic-true");
    } else {
        print("panic-false");
    }
    return;
}
