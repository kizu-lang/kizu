.direnv/
.claude/
bin/
target/
result
# release.yml builds into dist/ inside the checkout; untracked files there
# would make Go stamp the release binary as vcs.modified.
dist/

# VSCode extension tooling
editors/vscode/node_modules/
editors/vscode/out/
editors/vscode/*.vsix
editors/vscode/.vscode-test/

# Go test binaries
*.test

# LSP server build artifact
kizu-lsp

# backend-matrix build artifact (source: scripts/backend-matrix/)
/backend-matrix

# Generated with the selfhost compiler: the line names the revision, so it
# changes with every commit (source: scripts/gen-selfhost-version/).
compiler/src/internal/version/version_source.kizu
